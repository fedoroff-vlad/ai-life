/**
 * Reactive client for llm-gateway. Agents call by channel name (default/fast/vision/embedding);
 * provider mapping is owned by llm-gateway. Populated in PR3.
 */
package dev.fedorov.ailife.llm;
