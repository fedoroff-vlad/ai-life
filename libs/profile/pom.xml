<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>dev.fedorov.ailife</groupId>
        <artifactId>ai-life-parent</artifactId>
        <version>0.0.1-SNAPSHOT</version>
        <relativePath>../../pom.xml</relativePath>
    </parent>

    <artifactId>profile</artifactId>
    <name>ai-life :: libs :: profile</name>
    <description>Shared per-member personalization-profile capability (ADR-0005): the ProfileScope write-scope helper (self vs household-default) + the ProfileScopeResolver read engine (self → own-default → family-default → empty, reusing libs/sharing's identity read) + the generic PersonalizationProfileClient. One resolver + one client shape, N thin per-domain adapters. Light leaf: contracts + libs/sharing + WebClient, no LLM.</description>

    <dependencies>
        <dependency>
            <groupId>dev.fedorov.ailife</groupId>
            <artifactId>contracts</artifactId>
        </dependency>
        <!-- Reuse the one identity read (household-routing) rather than re-wrapping profile-service:
             the family-default resolution needs the caller's personal/shared household split, already
             served by libs/sharing's ProfileSharingClient (ADR-0005 §Reuse the identity reads). -->
        <dependency>
            <groupId>dev.fedorov.ailife</groupId>
            <artifactId>sharing</artifactId>
        </dependency>
        <!-- WebClient + reactor for the generic typed /internal/<path> client shape. Consumers own the
             base-URL-bound WebClient bean; this module is just the request shape + the resolve engine. -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-webflux</artifactId>
        </dependency>
        <!-- slf4j-api only (no spring-boot-starter in this light leaf) for the resolver's soft-fail logs;
             version managed by the Spring Boot BOM, the logging backend supplied by the consuming service. -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.projectreactor</groupId>
            <artifactId>reactor-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.squareup.okhttp3</groupId>
            <artifactId>mockwebserver</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-webclient</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
