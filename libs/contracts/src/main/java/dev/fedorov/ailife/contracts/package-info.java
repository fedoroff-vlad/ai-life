/**
 * Shared contracts: DTOs, JSON Schemas, message envelopes used across services.
 * Populated in subsequent PRs (NormalizedMessage, AgentRequest, EventBusMessage, etc.).
 */
package dev.fedorov.ailife.contracts;
