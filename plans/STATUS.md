# STATUS — current in-flight work (MUTABLE: update at the end of each PR)

## Now
- **🚧 IN PROGRESS — docs-agent (#188), second future-agent (started 2026-07-02).** Personal document archive: photo → OCR (`mcp-media-processing`) → `mcp-docs` (`docs` schema) store + text/semantic index → "find my X" search. Plan + slices D-a..e in [docs.md](docs.md). **Slice order:** D-a (`mcp-docs` + `docs` schema, port 8116) → D-b (`/internal/ocr` passthrough on mcp-media-processing) → D-c (`docs-agent` + `doc-archiver` ingest, port 8117) → D-d (`doc-finder` search) → D-e (memory-service semantic index + E2E closer). Mirrors the briefing/creator pattern (no new layer). **Currently: writing the plan + D-a.**
- **✅ DONE — briefing-agent (#186), owner-chosen first future-agent (2026-06-29 → closed 2026-07-02).** Owner build order for the future-agents: **briefing → docs (#188) → health (#187) → family-memory (#189)**, then travel/email/smart-home. **Next up: docs-agent (#188) — now IN PROGRESS, see the top bullet.** Plan + slices in [briefing.md](briefing.md) (BR-a..f2, all merged). **Personalization is first-class** (owner steer 2026-06-29): per-person `briefing_profile` + a NL config skill + golden-tests-from-the-start. **Merged (through BR-f2, PR235–242, 2026-07-02):**
  - **BR-a (PR235)** — `shared/mcp/mcp-weather` (port 8113): `forecast(lat, lon)` over **Open-Meteo** (free, no key) behind a swappable `WeatherSource`; `/internal/forecast`. No DB/no backing container, mirrors `mcp-market-data`.
  - **BR-b (PR236)** — `domains/briefing/mcp-briefing` (port 8114) + `briefing` schema: **the personalization store**. `briefing_profile` keyed `(household, owner)` — location (label + geocoded lat/lon + timezone), interests[], sections[] (weather|agenda|finance|news), schedule (time + enabled). `set/get/listScheduled` tools + `/internal/briefing-profile` (+ `/scheduled`). Liquibase `070-briefing.yml`, `briefing` schema in `postgres/init.sql`. Mirrors `mcp-creator`.
  - **BR-c1 (PR237)** — `geocode(name, language)` → `GeoLocation` added to `mcp-weather` (Open-Meteo Geocoding, free) + `/internal/geocode`. City→coordinates for the profiler.
  - **BR-c2 (PR238)** — `domains/briefing/briefing-agent` (port 8115) + `briefing-profiler` skill. NL config cue → LLM extract → geocode the city → upsert prefs via `/internal/briefing-profile`. Registered in orchestrator as `briefing`; binds mcp-briefing + mcp-weather + mcp-web. Golden `GoldenBriefingProfileTest` **verified live against local Ollama `qwen2.5:7b`** (39s, structure-not-text) — the config extraction works on the real model, not just mock.
  - **BR-d (PR239)** — the **digest flow** in `briefing-agent`. A produce-now cue → resolve the profile (self → household-default → all-sections default) → **gather in parallel** on the shared `Coordinator`: weather (`mcp-weather /internal/forecast` for the profile coords), today's agenda (`mcp-caldav /internal/events`), yesterday's spend (`mcp-finance /internal/spending-by-category`), news (`mcp-web /internal/search`, one per interest) → **one** `briefing-composer` LLM synthesis → reply text. Per-source soft-fail; weather needs coords + news needs ≥1 interest else skipped; household-scoped agenda/finance. The first multi-domain **read** coordinator. `flow/BriefingComposer` + `briefing-composer` SKILL; adds `mcp-caldav`/`mcp-finance` outbound (reads only, no MCP-SSE bind). Tests: `BriefingComposerTest` (2) + `GoldenBriefingComposerTest` **verified live against local Ollama `qwen2.5:7b`** (76s, structure-not-text: the digest cites only corpus links, no hallucinated source).
  - **BR-e (PR240)** — the **HTML digest board**. The `briefing-composer` synthesis → a `doc-render` `Doc` (synthesized text as a section + the gathered news headlines as grounded provenance links) via the shared `DeliverablePublisher` → stored in media-service → the open-link appended to the reply. Render/store hiccup soft-fails to text-only. Same board seam as creator/chef/nutrition (`MediaStoreClient` + `DeliverablePublisher`, default editorial theme); adds `media-service` outbound. `flow/BriefingComposer` (`publishBoard`/`newsLinks`). `BriefingComposerTest` extended (board stored + link in reply); golden re-verified live on Ollama qwen2.5:7b (72s).
  - **BR-f1 (PR241)** — the **proactive wake receiver**. `POST /agents/briefing/triggers/{kind}` (`web/TriggerController`); a `briefing.digest` wake reuses the `BriefingComposer` digest flow (empty user text) and delivers via `notifier-service` — to the payload's `ownerId`, else household fan-out. Manifest trigger `briefing.digest` declared. Orchestrator dispatch is already generic (agent name → `/agents/<name>/triggers/<kind>`), so a manually-created schedule already drives this end to end. `TriggerControllerTest` (owner + household fan-out + unknown-kind 404). Scheduler registration + E2E closer = BR-f2.
  - **BR-f2 (PR242) — the briefing closer. ✅ DONE (2026-07-02).** `mcp-briefing.setBriefingProfile` now registers/updates/deletes a per-profile cron in scheduler-service (agent=`briefing`, kind=`briefing.digest`, payload `{ownerId}`, cron from the profile's `schedule` time+timezone converted to UTC via `toUtcDailyCron`) and stores the returned `schedule_id` on the row (migration `070-briefing-schedule-id`; column also in the entity + test-schema). Register-before-delete + soft-fail mirror mcp-finance's `set_budget`; DST caveat noted. New `scheduler/SchedulerClient` + `config/{McpBriefingProperties,HttpConfig}` (`SCHEDULER_URL` env). Tests: the register/replace/delete lifecycle in `McpBriefingIntegrationTest` + `CronConversionTest` (UTC conversion) + the mandatory closer `E2EBriefingWakeFlowTest` (scheduler → orchestrator → briefing-agent trigger, `{ownerId}` payload survives each hop). **This closes the briefing project (#186).**
- **✅ briefing-agent (#186) DONE (2026-07-02).** Per-person morning digest shipped end to end (BR-a…f2). **Next future-agent per the owner build order: docs-agent (#188)** — receipts/contracts archive, OCR+search — then health (#187), family-memory (#189).
- **📌 Active work is now issue-driven (owner directive 2026-06-25).** When picking next work or doing it, look at open GitHub issues. **Just closed: [#199](https://github.com/fedoroff-vlad/ai-life/issues/199) — Stage 5 unblocked** (real model + golden tests; PR1–PR11 detail below). **Next: owner's pick from the open issues** — a future-agent (#186–#192). **Also just shipped: [#195](https://github.com/fedoroff-vlad/ai-life/issues/195) — calendar read-only view, done as per-person ICS feeds** (owner wants events visible in their own Apple/Google/Yandex calendar; Google/Yandex only subscribe by ICS URL, so a feed — not a custom page — is the right tool). Sliced: (1) `mcp-caldav GET /internal/events` read passthrough; (2) `platform/calendar-web` (`GET /ics/{token}.ics`, hand-rolled RFC-5545 writer, no DB); (B1) `calendar.calendar_feed` token store + `mcp-caldav /internal/feeds` (mint/resolve/list/revoke); (B2) calendar-web resolves tokens from the store (env feeds = fallback); (B4) Tailscale Funnel sidecar in compose (`tunnel` profile, one-time `TS_AUTHKEY`) for public HTTPS; (B3) **calendar-agent auto-issues a feed on a user's first calendar message + replies with the subscribe link** (`CALENDAR_PUBLIC_BASE_URL`). Net UX: set `TS_AUTHKEY` once → `docker compose --profile tunnel up` → message the bot → get your personal subscribe link. **Follow-up:** per-person *content* filtering (own vs shared) — feeds are household-level today (`events_cache.person_id` exists, no visibility flag yet). Real-model access is **unblocked via local Ollama** on the owner's Windows box (`qwen2.5:7b` chat + `nomic-embed-text` embeddings, free; owner's Ollama app had `OLLAMA_MODELS` pointing one level too deep — fixed for the session by `OLLAMA_MODELS=…\.ollama\models ollama serve`; owner to fix the app's model-dir setting for persistence — see `project_ailife_ollama_local` memory). PR1: first **golden harness** — `finance-agent` `GoldenRoutingTest` (opt-in `@Tag("golden")` + `@EnabledIfEnvironmentVariable(GOLDEN_LLM)`, **skipped in CI**, runs vs a live llm-gateway→Ollama). Asserts **structure, not text** (router emits parseable routing JSON; action ∈ contract enum; tool name real; unambiguous requests route right). Validated the finance routing spine on `qwen2.5:7b` and **fixed two real format-drift findings** it surfaced: (a) the 7B flattens `{"action":"tool","name":X}`→`{"action":X}` — `IntentRouter` now tolerates a flattened known-tool action; (b) it invented `"analysis"` for the advice flow — the classifier prompt now pins `action` to an exact enum. Runbook in `platform/llm-gateway/README.md` §Golden tests; env block in `infra/.env.example`. PR2: **orchestrator agent-routing golden harness** — `routing/GoldenRoutingTest` (same opt-in gate, skipped in CI) validates the top-of-spine decision: the real `LlmIntentClassifier` over the 8 real agent manifests must route 8 crisp domain messages to the right agent and resolve every probe to a known agent name (structure, not text; memory recall stubbed empty so only message + manifests drive it). **Both tests passed on `qwen2.5:7b`, no fixes needed** — the FAST-channel router holds on the real model. PR3: **first skill-output golden harness** — `tasks-agent` `intent/GoldenInboxClarifyTest` (same opt-in gate, skipped in CI) drives a concrete 3-item inbox through the real `inbox-clarify` skill (real AGENT.md + real SKILL.md, `TaskReviewClient`/`ClarifyClient` mocked) and asserts the production `InboxClarifier` parses the model's reply into a non-empty `proposals` array where every entry has a **verbatim** taskId (∈ the inbox ids, never hallucinated) + a valid GTD status (`next/waiting/scheduled/dropped`). **Surfaced + fixed one real finding (#199 part 3):** a 7B on CPU generating multi-item JSON blew past the gateway's previously-**hardcoded 60 s** upstream timeout (`OpenAiCompatibleProvider`/`AnthropicProvider`) → 500 → friendly-degrade; that timeout is now `LLM_REQUEST_TIMEOUT_SECONDS` (default 60, all providers); with it raised to 180 the test passes (well-formed proposals, ~75 s gen). PR4: **first synthesis-skill golden harness** — `researcher-agent` `flow/GoldenResearchSynthesisTest` (same opt-in gate, skipped in CI) feeds a fixed corpus (3 article hits w/ fetched text + 1 video, `WebSearchClient`/`PageFetchClient` mocked) through the real `research` synthesis hop (real AGENT.md + SKILL.md on the shared `Coordinator`) and asserts the model's free-text answer is grounded — non-trivial, cites ≥1 source, and **every** cited URL is a corpus url (the skill's "never invent URLs" rule, the failure mode that matters for a research agent). **Passed clean on `qwen2.5:7b`, no fixes** (~90 s gen at `LLM_REQUEST_TIMEOUT_SECONDS=180`). PR5: **grounded-synthesis golden harness** — `finance-agent` `advisor/GoldenAdvisorSynthesisTest` (same opt-in gate, skipped in CI) feeds two fixed spend-by-category windows (`SpendingClient` mocked) through the real `financial-advisor` synthesis hop and asserts the analysis is grounded in the data — non-trivial, names the actual top category, shows the currency, references ≥2 real categories (covers the issue's "synthesis contains its required content" leg; researcher PR4 covered link-provenance). **Passed on `qwen2.5:7b`** — model returns a full 3-section analysis (where money went / what changed vs prior window / hints); the one tweak was **test-side** (accept the `€` symbol as well as the `EUR` code — both satisfy "show the currency"; pinning the exact form would be a text assertion). Golden surface now spans all four shapes from the issue: agent routing + in-agent tool routing + JSON-skill output + free-text synthesis (provenance + grounding). PR6: **third-agent JSON-extract golden harness** — `nutritionist-agent` `foodlog/GoldenMealLogTest` (same opt-in gate, skipped in CI) runs the typed meal-log path through the real `meal-logger` skill (real AGENT.md + SKILL.md, `CaptionClient`/`MealClient` mocked) and captures the `LogMealInput` the production `FoodLogger` parsed from the model's reply — asserts a usable description + a structured signal (identified items or a positive kcal estimate). **Passed clean on `qwen2.5:7b`, no fixes** (~36 s gen). **Design note (owner Q on shared vs unique tests):** the golden tests share ~40 lines of scaffolding (the gate, the gateway `LlmClient`, the AGENT.md/SKILL.md loaders, a `NormalizedMessage` builder) but the **fixtures + assertions are unique per surface by design** — each validates that surface's own contract; and the routing mechanism itself differs per agent (LLM-classifier in finance/tasks/orchestrator vs **deterministic keyword heuristic** in nutritionist — which needs no golden test — vs plain LLM-passthrough in calendar, no structured contract yet). PR7 (done): **extracted the shared scaffolding** into a new `libs/golden-test-support` module — `@GoldenLlmTest` (the `@Tag("golden")` + `GOLDEN_LLM` gate as one meta-annotation) + `GoldenLlm` static helpers (`client()`, `message(...)`, `agentBody(cl)`, `skill(cl, path)`). Consumed test-scope by the 5 agent/orchestrator modules; all 6 golden tests migrated off their inline ~40-line copies (assertions + fixtures untouched, bespoke per surface). Separate lib (not `test-support`, which the mcp-* modules consume — they don't need agent-runtime in their test classpath). Verified: all 6 gate-skip without `GOLDEN_LLM`; one live re-run through the shared helper on `qwen2.5:7b` confirms the plumbing works. **Noted (RESOLVED in PR11):** the meal-log golden test was mildly flaky on a 7B (single-sample strict `verify(meals).log` — the model occasionally returned a non-parseable extract); fixed by the PR11 determinism pass. PR8: **chef recipe-card golden harness** — `chef-agent` `flow/GoldenRecipeCardTest` (on the shared `libs/golden-test-support` scaffolding) feeds fixed recipe hits (`WebSearchClient`/`DeliverablePublisher` mocked) through the real `recipe-finder` synthesis hop and asserts the synthesized card grounds in the supplied recipes (a distinctive title marker survives — robust to a 7B shortening the full title). **Passed clean on `qwen2.5:7b`** (~98 s). PR9: **stylist wardrobe-audit golden harness** — `stylist-agent` `flow/GoldenWardrobeAuditTest` (shared scaffolding) feeds a fixed 4-garment wardrobe (`WardrobeReadClient`/`DeliverablePublisher` mocked, profile optional) through the real `wardrobe-auditor` synthesis hop and asserts the production `WardrobeAuditor` parses a non-empty KEEP/QUESTION/REMOVE verdict set (the success tally "оставить N, под вопросом N, убрать N" + the stored link). **Passed clean on `qwen2.5:7b`** (~122 s). PR10: **creator-profiler golden harness** — `creator-agent` `profile/GoldenCreatorProfileTest` (shared scaffolding) runs a typed creator-track description through the real `creator-profiler` extract (`CreatorProfileClient` mocked) and captures the `SetCreatorProfileInput` the production `CreatorProfiler` parsed — asserts a niche + ≥1 other field. **Passed clean on `qwen2.5:7b`** (~35 s). **All 8 domain agents + orchestrator now have golden coverage.** PR11 (this — **closes #199**): **determinism pass** — `temperature=0` on the typed JSON-extract flows (`FoodLogger.logText` / `DietProfiler.setProfile` / `CreatorProfiler.setProfile`) via a new `LlmChatRequest.of(channel, messages, temperature)` factory (the providers already honour the field); extraction must be faithful/repeatable, not creative. Verified: meal-log golden passes **3/3 live** on `qwen2.5:7b` (was ~1-in-3). **✅ #199 DONE — Stage 5 unblocked.** The whole behavioural surface (intent/agent routing + in-agent tool routing + JSON-skill output + free-text synthesis with link-provenance + data-grounding) is now validated on a real model (`qwen2.5:7b` via local Ollama), opt-in (`@GoldenLlmTest` / `GOLDEN_LLM`) + skipped in CI, sharing `libs/golden-test-support`. Real findings fixed along the way: IntentRouter format-drift (flatten + enum-pin), configurable `LLM_REQUEST_TIMEOUT_SECONDS`, extract-determinism. **Dev-box note:** run the gateway as a single module (`mvn -pl platform/llm-gateway spring-boot:run`), NOT with `-am` (which runs the goal on the parent reactor → "Unable to find a suitable main class"); build deps first with a separate `-am … install`. **Last closed: [#196](https://github.com/fedoroff-vlad/ai-life/issues/196)** (CLOSED 2026-06-28, PRs #215 Grafana dashboards + #216 the Telegram monthly-report skill `MonthlyReporter`). Historical detail of #196 PR2 follows: it was on branch `feat/196-monthly-report-skill`: owner chose Grafana-first + text-first + monthly-summary on-demand. **PR1 (Grafana dashboards) is MERGED** (#215, zero-code: read-only PG datasource + `Finance overview` dashboard — monthly trend / spending-by-category / account balances / budget burn-down over the `finance.*` matviews, in `infra/grafana/`, wired into both compose files). **PR2 (this)** adds the **text-first monthly** HTML report: new `monthly-report` SKILL → `report/MonthlyReporter` on the shared `Coordinator` + `DeliverablePublisher` render→store→link seam (finance-agent gains the `MediaStoreClient`/`DeliverablePublisher` beans + a `FINANCE_PUBLIC_MEDIA_BASE_URL` split). It gathers the current month's `spending-by-category`, LLM-synthesizes a narrative, renders an HTML report (narrative + **deterministic** per-category breakdown — real amounts/counts, not LLM-generated) → media-service → TG link; routed via a new `report` classifier action in `IntentRouter`. Empty month → friendly invite (no LLM/store). Inline charts still deferred to a later `chart-render` capability-MCP. Unit + slice tests green (`MonthlyReporterTest`, `IntentRouterTest`). Last closed was **[#202](https://github.com/fedoroff-vlad/ai-life/issues/202)** (CLOSED 2026-06-27, PRs #212 + #213: singleton/reuse Testcontainers PG via new `libs/test-support` `AbstractPostgresIntegrationTest` across all integration tests — one `pgvector/pgvector:pg16` container shared across module JVMs via `withReuse(true)` + identical config hash, schema applied through `ScriptUtils` in `@BeforeAll` instead of init scripts. PR A #212 created the lib + piloted `mcp-finance`; PR B #213 migrated the remaining 20 modules. **Shared-outbox lesson:** the singleton DB removed the per-class outbox isolation old per-container tests relied on, so a module bus-consumer integration test must gate the listener off in non-consuming sibling contexts (`event-bus.enabled=false`); where a sibling *publishes the consumed topic* (nutrition capture tools emit `basket.captured`) that sibling must wipe `bus.outbox` in `@AfterEach` and the consumer must clean it `@BeforeEach`, else a leftover PENDING row chokes the poller. Before that, **[#201](https://github.com/fedoroff-vlad/ai-life/issues/201)** (CLOSED 2026-06-26, PR #210: MCP-SSE vs /internal REST doctrine — `@Tool`/MCP-SSE = LLM tool-selection + external clients; `POST /internal/tools/{name}` = official inter-service deterministic surface; updated `architecture.md` + `PATTERNS.md`). Next is owner's pick from the open issues (e.g. future-agents #186–#192, Stage-5 unblock, UI/reporting #195/#196). Historical detail of the closed #200 epic follows: Done sub-slices: OrchestratorInvokeClient (#203), MediaStoreClient (#204), **render→store→link deliverable helper (#205)** — new `libs/agent-runtime` `deliver/DeliverablePublisher` (`publish(household, owner, Doc)` render→upload→link + `mediaUrl(id)` builder + static `splitParagraphs`/`summary`), opt-in `@Bean` per deliverable agent (chef/nutritionist/stylist/creator), with all 9 gather→synthesize flows migrated off the copy-pasted `link()`/`base()`/`trimBase()`/`splitParagraphs()`/`summary()`; agent-runtime gained a `doc-render` dep; **DocRenderer/RenderConfig bean (#206)** — `DeliverablePublisher` got a default-theme convenience ctor (`new DeliverablePublisher(mediaStoreClient, baseUrl)` builds the default `HtmlDocRenderer`), so the 3 identical `RenderConfig` classes (chef/nutritionist/creator) were deleted; stylist keeps its themed `DocRenderer` bean; **base ActionController (#207)** — new `libs/agent-runtime` `web/AgentActionController` (abstract: `register(action, handler)` + `dispatch` doing the unknown-action error + uniform `"<action> failed"` onErrorResume with `requestedBy` log); the 4 agent `ActionController`s (calendar/finance/chef/creator) now extend it, keeping only their per-action business logic + the per-agent `@PostMapping` path; **OutboundHttpConfig boilerplate (this PR)** — new `config/SharedClientProperties` interface (each agent's `*AgentProperties` implements it) lets `AgentRuntimeConfig` build the three shared `profile/notifier/memory` `WebClient` beans once (24 identical bean methods → 3); all 8 agents drop them and keep only their agent-specific clients + URL values. **Issue #200 is now COMPLETE** — all six sub-slices shipped; the next agents (briefing/health/docs) inherit the shared scaffolding instead of copying it.
- **Stage snapshot (2026-06-25): foundation (Stage 0–4) closed; Stage 5 (real LLM) BLOCKED on model access (default provider = `mock`); Stage 6 domain agents COMPLETE.** All current domains are MVP-done — calendar, finance, tasks, researcher, stylist, nutrition (nutritionist + chef), creator. **No in-flight domain work.** Next is owner's pick: (a) a future agent — each is its own [`future-agent`](https://github.com/fedoroff-vlad/ai-life/labels/future-agent) issue (briefing #186, health #187, docs #188, family-memory #189, travel #190, email #191, smart-home #192); (b) unblock Stage 5 (connect a real model — Ollama/Anthropic — then golden tests); or (c) a UI/reporting follow-up — calendar web view (#195), finance reports (#196). CI is now incremental on PRs (GIB) — see CLAUDE.md. The bullets below are the historical per-PR log.
- **Stage:** Stage 1 fully closed (PR20). Stage 2 (finance) + Stage 3 (tasks) closed. **Stage 4 (memory + inter-agent) CLOSED** — all four tracks done: A (conversation-state), B (event-bus), C (inter-agent chains + first bus consumer), D (coordinator substrate: D1 scaffold + D2 gift-recommender + D3 tiers/birthday), plus the memory-from-chat fuel (MFC core). **Consciously NOT done (each an explicit "not now"):** Apache AGE graph (deferred — SQL `memory.relations` suffices; promotion criteria in memory-service README), C2b durable notifier (deprioritized — orthogonal reliability), and the roadmap's literal `…→ creator.draft_greeting →…` half (the `creator` agent is Stage 6). **Next active line — `mcp-media-processing`** (owner-prioritised after D3, 2026-06-19): the shared media-understanding capability-MCP, PR-sliced in [media.md](media.md) (MP-a scaffold+ocr-stub → MP-b real Tesseract → MP-c bind finance-agent + migrate receipt-parser off in-agent vision → MP-d caption/STT). **MP-a DONE (PR110): new capability-MCP `shared/mcp/mcp-media-processing` (port 8097, NO schema/JPA/datasource — first `shared/mcp/` module).** `ocr(mediaId)` `@Tool` fetches blob bytes from media-service (`http/MediaClient` → `GET /v1/media/{id}`, blocking `.block()` per the MCP `@Tool` convention) and runs a pluggable `engine/OcrEngine`; the MP-a default `StubOcrEngine` returns a deterministic marker (`[stub-ocr] <N> bytes`) so the fetch→engine→tool wiring is provable with **no native dependency** (MP-b swaps real Tesseract behind the same interface). New contract `contracts/media/OcrResult(text, lang?, confidence?)`. webflux-only (no servlet/JPA); Spring AI `MethodToolCallbackProvider` exposes the tool. Tested (`MediaProcessingOcrTest`: full MCP context boots, `ocr` hits `/v1/media/{id}` + returns the byte-count marker; empty body → empty text). Wired into root pom `<modules>`, compose (`depends_on: media-service`, no DB), `.env.example`, infra/README (8097). Full reactor compiles clean. **MP-b DONE (PR111: real OCR via Tess4J + native tesseract; engine decision LOCKED = "Tess4J in-image", owner 2026-06-19).** New `engine/TesseractOcrEngine` is the deployed default (`@ConditionalOnProperty mediaprocessing.ocr-engine=tesseract`, matchIfMissing) behind the same `OcrEngine` interface; `StubOcrEngine` now `=stub`-only (the hermetic wiring test sets it, and degraded/dev boxes). Engine decodes bytes via `ImageIO` → `Tesseract.doOCR` (lang `rus+eng`); `tessdata` resolved from `TESSDATA_PREFIX` → property → a probe of common distro paths (robust across tesseract 4/5 layouts). Unreadable bytes → empty text (not an error); genuine native failure → `IllegalStateException`. Tess4J dep version-managed in root pom (`tess4j.version=5.13.0`). Docker image installs `tesseract-ocr` + eng/rus + sets `TESSDATA_PREFIX`; **`ci.yml` installs tesseract so the real-OCR test runs in CI** (renders "HELLO WORLD" → asserts OCR reads it; self-skips via JUnit `Assumptions` where native is absent, e.g. a Windows dev box). MP-a wiring test pinned to `ocr-engine=stub` so it stays native-free (2 green); real test skips locally, runs in CI. **MP-d1 DONE (PR112: `caption` vision tool — centralised vision call; owner reorder 2026-06-19 = vision tool BEFORE the receipt-parser migration, since vision-on-image beats parsing noisy OCR text).** New `caption(mediaId, instruction)` `@Tool` fetches bytes from media-service → calls llm-gateway's `vision` channel via `libs/llm-client` (`LlmMessage.userWithImages`, blocking `.block()`) → returns `CaptionResult(text, model?)` (new contract). Generic: the caller supplies the instruction (free description or structured extraction) and parses the result — reasoning stays in the caller's skill, but the vision call lives here so no agent re-embeds it. Module gains the `llm-client` dep + `ailife.llm-client.base-url` (`LLM_GATEWAY_URL`); compose adds `depends_on: llm-gateway` + the env. Tested (`MediaProcessingCaptionTest`: media fetch + llm-gateway `/v1/chat` on `channel:vision` with the instruction + image part → returns the model text). Module suite green (caption + ocr-stub pass, real-OCR self-skips locally / runs in CI). **MP-c (bind + migrate receipt-parser) split into c1/c2 — mirrors money-pro PR44→PR45 (the deterministic agent→capability call goes over an HTTP `/internal/*` passthrough, NOT the MCP/SSE transport, which can't be MockWebServer'd per CLAUDE.md). MP-c1 DONE (PR113): mcp-media-processing `POST /internal/caption` passthrough → `caption` tool.** New `web/InternalCaptionController` delegates straight to `MediaProcessingMcpTools.caption(mediaId, instruction)` on `Schedulers.boundedElastic()` (the tool blocks per the `@Tool` convention), so the same fetch→vision-channel logic/invariants apply; the MCP `@Tool` stays the entry point for future LLM-driven selection. New contract `contracts/media/CaptionInput(mediaId, instruction)`. Tested (`InternalCaptionControllerTest`: `WebTestClient` POST → media-service `/v1/media/{id}` fetch + llm-gateway `/v1/chat` on `channel:vision` with the instruction + image part → `CaptionResult{text, model}`; MockWebServers, no native dep). Module suite green (3 pass + real-OCR self-skips locally). **MP-c2 DONE (PR114): finance-agent binds mcp-media-processing + `receipt-parser` migrated off in-agent vision onto the `caption` capability — MP-c COMPLETE, clears the STATUS Deferred anti-pattern.** New `http/CaptionClient` (`POST /internal/caption` → `CaptionResult`, 30s timeout, mirrors `MoneyProImportClient`) + `mcpMediaProcessingWebClient` bean + `finance-agent.mcp-media-processing-url` (`MCP_MEDIA_PROCESSING_URL`) + the `spring.ai.mcp.client.sse.connections.mcp-media-processing` binding block (for future LLM-driven selection; the deterministic receipt call uses the HTTP passthrough — MCP/SSE isn't MockWebServer-testable). `ReceiptParser.parse` no longer fetches bytes (dropped `MediaClient`/`LlmClient` deps) nor builds a `vision` `LlmChatRequest` inline — it calls `caption.caption(mediaId, instruction)` where the instruction = the `receipt-parser` SKILL.md (the self-contained strict-JSON extract prompt) + the user's caption folded in as a trailing hint. The rest of the flow (draft parse → account resolve → confirm-before-write pendingAction → resume writes on "да" → MFC-c receipt-caption observation) unchanged. `MediaClient` stays for `MoneyProCsvImporter`'s CSV fetch. `ReceiptFlowTest` reworked: the receipt intent now mocks mcp-media-processing's `/internal/caption` returning a `CaptionResult` (asserts the POST carries the media id + SKILL instruction + user note) instead of media+llm-gateway mocks; full finance-agent suite green (40). compose finance-agent gains `depends_on: mcp-media-processing` + `MCP_MEDIA_PROCESSING_URL`; `.env.example` + READMEs (finance-agent, AGENT.md) updated. **The canonical "vision stuffed into one agent" anti-pattern is retired — the vision call lives once in the capability-MCP, reusable by any future agent (docs/stylist/health).** **MP-d2a DONE (PR123: `transcribe` (STT) tool on a native-free stub engine — slice mirrors MP-a).** New `transcribe(mediaId)` `@Tool` on `MediaProcessingMcpTools` fetches audio/video bytes from media-service → runs a pluggable `engine/SttEngine` (audio-path mirror of `OcrEngine`) → new contract `contracts/media/TranscriptResult(text, lang?, durationSeconds?)`. Default `engine/StubSttEngine` returns a deterministic byte-count marker (`[stub-stt] <N> bytes`) so the fetch→engine→tool wiring is provable with NO native/model dependency. Tool surface 2→3 (`ocr`/`caption`/`transcribe`); MCP `instructions` + README (tools table, key classes, status) + `media.md` slice updated. Tested (`MediaProcessingTranscribeTest`: full MCP context boots with 3 registered tools, `transcribe` hits `/v1/media/{id}` + returns the byte-count marker; empty body → empty text). Module suite green (7 tests, 1 self-skip = the native-OCR test on a bare box); full reactor compiles clean. **No `/internal/*` passthrough yet — no consumer; the deterministic HTTP transport lands when a Stage-6 agent binds it (mirror MP-c).** **MP-d2b DONE (PR124: real STT via a whisper ASR sidecar over HTTP — engine decision LOCKED = sidecar service, owner 2026-06-21).** `WhisperSttEngine` (`@ConditionalOnProperty mediaprocessing.stt-engine=whisper`, matchIfMissing) is the deployed default behind the same `SttEngine`; `StubSttEngine` now `=stub`-only. It POSTs the audio bytes as multipart `audio_file` to the whisper webservice (`POST /asr?output=json`) → reads `text`/`language` (+ duration from the last segment). No-speech → empty text; sidecar 5xx/timeout/unparseable → `IllegalStateException` (mirrors `OcrEngine`). **Locked sidecar (NOT in-image JNI like OCR) on purpose:** polyglot-by-design (architecture.md) + the roadmap "run as a service" verdict + immature Java whisper bindings + a ~150 MB model that would bloat the JVM image; trade-off = +1 container, but the engine is a pure HTTP client → **fully MockWebServer-testable in CI, no native dep** (`WhisperSttEngineTest`, 4 cases — strictly better than MP-b's self-skipping native test). compose + dev.yml gain a `whisper` service (`onerahmet/openai-whisper-asr-webservice`, faster_whisper, host 9100); mcp-media-processing depends on it `service_started` (NOT healthy — ocr/caption must not block on whisper's first-boot model pull; transcribe is lazy + best-effort) + env `MCP_MEDIA_PROCESSING_STT_ENGINE`/`WHISPER_ASR_URL`. application.yml now also maps the `ocr-engine`/`tess-lang` placeholders (those README env vars were previously documented but unwired). `.env.example` + infra/README (port 9100 row) + module README updated. Module suite green (11 tests, 1 self-skip = native OCR); full reactor compiles clean. **`mcp-media-processing` is feature-complete (ocr + caption + transcribe) — the shared media-understanding capability any Stage-6 agent (docs/stylist/health) binds.** The memory half history below is retained for provenance, planned in [stage4.md](stage4.md). **Track A (conversation-state): PR65 (conversation-service — A1) + PR66 (route-lock read — A2) + PR67 (lock lifecycle + resume plumbing — A3) DONE** (see Done Stage 4). The orchestrator writes a lock when an agent returns a `pendingAction`, resumes the locked agent on the reply, and clears on resolve. **A4 DONE — both producers shipped: PR68 (receipt-parser confirm-before-write, finance) + PR69 (inbox-clarify apply-on-confirm, tasks).** **Track A (conversation-state) is COMPLETE** — multi-turn confirmations work end-to-end (lock → resume → apply/cancel → clear), and both owner-flagged debts (receipt confirm, inbox apply) are cleared. **Track B (event-bus) — B1 DONE (PR70) + B2 DONE (PR71: Spring-bean API `EventBusConfig`/`EventBusProperties`/`EventBusListenerContainer` + scheduler-service emits `schedule.fired` as the first reference producer).** **Track B complete on the producer side; the consumer-in-a-service half lands with C2.** **Track C — C1 (task-to-event, sync via orchestrator) is multi-slice; C1a DONE (PR72: orchestrator `/v1/agents/invoke` inter-agent sync primitive + `Agent.invoke`/`RemoteAgent` + `AgentActionRequest`/`AgentActionResult` contracts).** **C1b DONE (PR73: mcp-caldav `POST /internal/event` passthrough → `createEvent`).** **C1c DONE (PR74: calendar-agent `POST /agents/calendar/actions/create_event` — first agent `/actions/*` endpoint; maps invoke `args` → `CreateEventInput`, calls mcp-caldav `/internal/event`, returns `{eventUid}`).** **C1d DONE (PR75: mcp-tasks `POST /internal/link-event` passthrough → `link_task_to_event` + `LinkTaskToEventInput` contract).** **C1e DONE (PR76: tasks-agent task-to-event flow — `POST /agents/tasks/internal/task-to-event` → orchestrator `/v1/agents/invoke` (`create_event`) → mcp-tasks `/internal/link-event`). C1 COMPLETE — first full inter-agent chain works (tasks → orchestrator → calendar → back to tasks); clears the Deferred `task-to-event` item.** **Track C2 — C2a DONE (PR77: notifier-service is the first bus consumer — `EventBusListenerContainer` `@Bean` consuming `notify.requested` → existing `/internal/send` delivery path).** The async-notification pattern (`NotifyRequestedEvent`) is live and notifier stays domain-agnostic (text only). **Direction set with owner (2026-06-16):** the "Jarvis" north-star is a reusable **coordinator substrate** (event/intent → a domain agent coordinates: gathers from memory + specialist agents + capability-MCPs → LLM synthesis → propose → confirm → act). All owner scenarios (birthday-gift, picnic-menu, reunion-outfit, 3D-print) are the same shape — build the substrate once on the cheapest vertical, then "new domain = add a specialist". **Architecture doctrine fixed in [architecture.md](architecture.md):** capability-MCP vs domain-MCP, agent-led coordination via the hub, the orchestrator routing doctrine, brain inputs, propose-vs-act-on-confirm. **Repo restructured to group-by-domain** (`domains/<domain>/{<x>-agent, mcp-<x>(s), skills}` + `shared/` for capability-MCP & cross-cutting skills + `platform/` cross-cutting services + `libs/` compile-time code); each module is still its own app/container (co-location ≠ merge); paths/recipes updated across architecture.md/PATTERNS.md/CLAUDE.md. **Track D (coordinator substrate) STARTED — Coordinator scaffold DONE (PR91):** `libs/agent-runtime` `Coordinator` — the reusable **gather → synthesize** pattern (named async gather steps run in parallel → folded into a `context` object → LLM synthesis from `[systemPrompts] + {payload, context}`), per-step soft-fail, registered as a bean every agent gets on `@Import(AgentRuntimeConfig)`. This is the spine of agent-led coordination; latent until a flow uses it. **Next active line — D2 (owner-chosen 2026-06-16): budget-aware `gift-recommender`** — the first real Coordinator flow, validating the scaffold end-to-end on the cheapest vertical. PR-sliced in [stage4.md](stage4.md) §Track D: **D2a DONE (PR93: mcp-finance `GET /internal/gift-budget` read passthrough — the household's "Gifts" expense-category monthly budget envelope, composed from the existing `get_budget_status` read; mirror C1b).** **D2b DONE (PR94: finance-agent `POST /agents/finance/actions/get_gift_budget` — first finance `/actions/*` endpoint; forces `householdId` from the envelope, reads D2a via `GiftBudgetClient`, returns a structured `AgentActionResult` `{hasGiftBudget, amount?, currency?, remaining?}` — 404→`hasGiftBudget:false`, mcp-finance down→`ok=false`, never an HTTP error; mirror C1c).** **D2c DONE (PR95: calendar-agent `gift.recommend` rebuilt on the `Coordinator` — the FIRST real Coordinator flow). New `flow/GiftRecommender` gathers in parallel `{budget: finance `get_gift_budget` via the orchestrator `/v1/agents/invoke` hub (new `http/OrchestratorInvokeClient`), memories: recall, relations}`, the `Coordinator` folds the successful sources into `context` and synthesizes budget-aware gift ideas from `[AGENT.md, SKILL.md] + {payload(person), context}`, then fans out to the household. Per-step soft-fail (finance outage just drops the budget constraint). `TriggerController` branches `gift.recommend` → `GiftRecommender`; every other kind stays on the generic recall→LLM→notify path. `gift-recommender` SKILL.md upgraded to consume `context.budget` (respect `remaining`/`amount`, name a price range when present; ideas-only when absent) — clears its "finance integration ships later" note. D2 COMPLETE — the coordinator substrate is validated end-to-end on the cheapest vertical (calendar→finance→memory).** **memory-from-chat STARTED — MFC-a DONE (PR96): memory-service `POST /v1/capture` — the extraction+write core.** An LLM `FactExtractor` (DEFAULT channel, lenient JSON parse, best-effort → empty on any failure) pulls durable facts out of a message; `CaptureService` writes each via the existing `MemoryService.write` tagged `source=chat-capture`. Testable in isolation (Mockito-unit-tested parser + a Testcontainers capture→recall round-trip). **Design (owner-aligned 2026-06-19):** capture is a memory-service responsibility (it already owns the schema + recall + the llm-gateway hop for embeddings); facts arrive from many surfaces (a typed message, later a parsed receipt), so the trigger is the **event bus** — orchestrator/agents emit a message/observation event and memory-service consumes it off the request path. **MFC-b STARTED — consumer DONE (PR97): memory-service consumes `message.received` off the bus → `CaptureService`** (new `bus/MessageCaptureHandler` + `config/EventBusListenerConfig`; `@Import(EventBusConfig)`; new `contracts/message/MessageReceivedEvent`). Retry policy mirrors notifier's consumer (transient write failure throws → row stays PENDING/retried; parse/validation permanent → accept; extraction itself never throws). Tested via direct `OutboxPublisher` (capture round-trip + foreign-topic-ignored); existing memory tests stay green with the listener now active. **MFC-b drop-point DONE (PR98): memory-service `POST /v1/observations` — durable async HTTP front-door to the bus** for DB-less producers (publishes `message.received` to `bus.outbox`, returns 202; consumer does the LLM work later with retry). **Design call (owner-delegated to principles 2026-06-19, free/lean/maintainable):** reuse the existing Postgres event bus, NOT a new Redis — Postgres already runs (zero new infra), the outbox is a status-tracked queue (not a cache that becomes a "свалка"), and it IS the owner's preferred "service supplying from a relational DB" shape; the DB-less-producer ergonomics gap is closed by this one HTTP drop-point (Redis-push simplicity, durable storage). `/v1/capture` stays as the synchronous direct/debug path. **Capture is distributed by design** — each component emits what it learns (orchestrator: typed text; finance-agent: parsed-receipt facts; …), since no single point sees everything. **MFC-b producer DONE (PR99): orchestrator fire-and-forgets every inbound `/v1/intent` text to memory-service `/v1/observations` (`MemoryClient.observe`, off the response path, soft-fail — same posture as recall). memory-from-chat now runs AUTOMATICALLY end-to-end: typed message → orchestrator → bus → consumer → extract → stored memory (recallable).** Skipped when memory disabled / no household / blank text (attachment-only messages are captured by the agent that processes the attachment — capture is distributed by design). **MFC-c STARTED — relation/graph extraction (the owner's "простроить граф" half). MFC-c1 DONE (PR100): memory-service `capture/RelationExtractor` — the LLM extraction core for structured edges (`subject —edge→ object`), the relation counterpart of `FactExtractor`.** DEFAULT channel, lenient JSON parse (strips fences/leading prose), best-effort → empty on any failure; returns `capture/ExtractedRelation` triples; subject `"self"` marks a statement about the speaker. Mockito-unit-tested in isolation (7 cases, no Docker), mirroring `FactExtractorTest`. **MFC-c2 DONE (PR101): person resolution + `RelationExtractor` wired into `CaptureService` — relation capture now runs automatically.** New `http/ProfileClient` (memory-service → profile-service `GET /v1/people/by-household/{id}`, case-insensitive `displayName` match → `core.people` UUID; best-effort `null` on no-match/failure, never auto-creates a person) + `config/HttpConfig` `profileWebClient` bean (`memory.profile-base-url` ← `PROFILE_SERVICE_URL`, default `profile-service:8082`; `WebClient` is on the classpath transitively via llm-client). `CaptureService` now also extracts relations and writes resolved edges as a **best-effort side effect** (never throws, not reflected in the `MemoryDto[]` capture response): a `"self"` edge anchors on the message's `userId` (`subjectType="user"`); a named subject resolves to a person (`subjectType="person"`); an edge whose subject can't be anchored (no `userId` / unresolved name) is **dropped** — no auto-create of chat-derived people. Objects stored free-text (`objectType="label"`). Unit-tested (`CaptureServiceTest`, 5 Mockito cases: self→user, name→person, self-without-speaker skipped, unresolved skipped, write-failure-never-breaks-capture); full memory-service suite green (30 tests incl. the consumer integration — relation extraction adds one `/v1/chat` call the dispatcher serves, `{"facts":…}` has no `relations` key → no edges, counts unaffected). compose memory-service gains `PROFILE_SERVICE_URL` + `depends_on: profile-service`. **memory-from-chat now builds the graph automatically: a message about a known person → resolved edge in `memory.relations` (recallable by gift-recommender's relations gather).** **MFC-c3 DONE (PR102): finance-agent is the second capture producer — receipt captions → `/v1/observations`.** New `observe(householdId, userId, text, source)` on the shared `libs/agent-runtime` `MemoryClient` (mirrors orchestrator's: fire-and-forget, off the response path, 500 ms timeout, soft-fail, no-op on blank text) — so any agent can feed durable facts it saw into memory-from-chat. `ReceiptParser` now emits the photo **caption** (grounded with the parsed merchant, e.g. `"<caption> (чек: Starbucks)"`) to memory-service when the receipt carries one. **This closes the design gap PR99 left explicit:** the orchestrator's text capture skips attachment-only messages by design, so the agent that processes the attachment must emit the durable fact (capture is distributed). A captionless receipt is a bare transaction (no durable personal fact, already in the finance DB) → nothing emitted. **No new skill needed** — the central `FactExtractor`/`RelationExtractor` do all the reasoning; finance-agent is a thin producer (owner-aligned 2026-06-19: reasoning stays centralized in memory-service). Tested via `ReceiptFlowTest` (memory MockWebServer: caption+merchant POSTed on a captioned receipt; captionless → no observe), full finance-agent (38) + agent-runtime (11) suites green. **memory-from-chat MFC core complete** — both producers live (orchestrator typed text + finance receipt captions), graph builds automatically. **(Future, needs a skill — deferred, not now):** item-level receipt parsing → preference mining ("buys oat milk" → dietary) would extend the receipt-parser SKILL.md to extract line items; today receipts yield only amount/merchant/date. **D3 STARTED (relationship-tiered gift-budget rules as editable data + birthday "reminder + gift"). D3a DONE (PR104): mcp-finance `finance.fin_gift_budget_rule` store + `set_gift_budget_rule` / `list_gift_budget_rules` tools.** New migration [025-fin-gift-budget-rule.yml](../infra/liquibase/features/025-fin-gift-budget-rule.yml) — one editable rule per `(household, relationship tier)` (e.g. parent→20000 RUB), functional unique index on `(household_id, lower(relationship))` so tiers are case-insensitive; upserted in place (no time-versioning — a preference has no audit need, unlike `fin_budget`). `set_gift_budget_rule` upserts (keeps the originally-stored label casing on update, changes amount/currency — deterministic, collation-independent); `list_gift_budget_rules` lists ordered by relationship. New contracts `SetGiftBudgetRuleInput` + `GiftBudgetRuleDto`; new `domain/FinGiftBudgetRule(+Repository)` (`findByHouseholdIdAndRelationshipIgnoreCase`). Tool surface 16→18. Tested (`McpFinanceIntegrationTest` upsert/case-insensitive/ordering/one-row-per-tier); full mcp-finance suite green (33). **editable rules = DATA** (routing doctrine), not skill prose. **D3b DONE (PR105): mcp-finance `GET /internal/gift-budget-rule?householdId=&relationship=` read passthrough** (mirror D2a's `/internal/gift-budget`). New non-`@Tool` `getGiftBudgetRule(householdId, relationship)` (case-insensitive, throws when no rule) + `InternalGiftBudgetRuleController` → `GiftBudgetRuleDto` (200) | 404 (agent falls back to the `Gifts` envelope). Tested (tier rule returned case-insensitively / 404 when unset); mcp-finance suite green (34). **D3c DONE (PR106: finance-agent `get_gift_budget` action takes an optional `args.relationship`).** When supplied, `ActionController` reads the tier rule first via new `GiftBudgetClient.fetchRule` (`GET /internal/gift-budget-rule`, D3b) — a match wins (`source:"rule"`, no `remaining` since a preference has no spend window), a 404 falls back to the household "Gifts" envelope (`source:"envelope"`); no `relationship` reads the envelope directly (back-compat, D2b path unchanged). Response gains `source?`/`relationship?` (additive — the D2c `GiftRecommender` folds the whole node into `context` verbatim, so unaffected). Always an `AgentActionResult`, never an HTTP error (mcp-finance down → `ok=false`). Tested (`ActionControllerTest`: tier-rule-wins-over-envelope, 404→envelope-fallback) — finance-agent suite green (40, was 38). **D3d DONE (PR107: calendar-agent `GiftRecommender` forwards the person's `relationship` into the budget gather).** `fetchGiftBudget(household, person)` now sets `args.relationship` from `PersonDto.relationship` (when non-blank) on the `get_gift_budget` invoke, so finance returns the relationship-tiered rule (D3c) and falls back to the "Gifts" envelope when no tier rule is set. No relationship → unchanged D2c behaviour. Tested (`TriggerControllerTest.giftRecommendWake…` now asserts `"relationship":"sister"` in the invoke body) — calendar-agent suite green (13). **The gift-recommend chain is now relationship-tier-aware end-to-end (calendar → orchestrator → finance tier rule/envelope → memory).** **D3e DONE (PR108: birthday "reminder + gift" — one `gift.recommend` wake → two outputs; owner-chosen shape 2026-06-19).** `GiftRecommender.notifyHousehold` now fans **two** messages to each household member: (1) a short deterministic birthday **reminder** (`"🎂 Напоминание: скоро день рождения у {name}."`, with "через N дн." when `payload.daysUntil` is present), then (2) the existing LLM-synthesized **gift ideas**. Reminder skipped when no person resolved (nobody to name); gift message skipped when synthesis is empty — a degraded run still delivers whatever it has. Each notify is independently soft-failed. Tested (`TriggerControllerTest.giftRecommendWake…` now asserts both notifications in order — reminder names "Maria" + "день рождения", then gift ideas) — calendar-agent suite green (13). **D3 COMPLETE** — relationship-tiered editable gift-budget rules (data, not prose) + birthday reminder+gift two-output wiring, all stacked on the D2 coordinator substrate. **Deprioritized:** C2b "durable notifier" (publish `notify.requested` over the bus) — orthogonal reliability, do later. Stage 5 (real LLM) ongoing/blocked on model access. Stage 5 (real LLM) ongoing/blocked on model access. **Stage 2 (finance) ongoing — PR21..PR34 + PR35 (LLM-driven tool routing in `IntentController`) shipped.** User text like "импортируй CSV из Money Pro" now goes through an LLM classifier that picks either a tool (dispatched via PR34's `ToolDispatcher`) or a chat reply — closes the user-facing consumer side of the MCP-client stack.
- **Finance MVP CLOSED (owner boundary 2026-06-20).** The owner drew an explicit MVP line for the finance agent — receipt→capture + confirm + on-request **analysis** — and recorded the full beyond-MVP vision so nothing is lost. **PR116 (docs): finance.md MVP boundary + recorded-vision table + roadmap.md candidate shared capabilities (`chart-render`, `web/market-data`, STT).** The vision's deferred items (year analysis with chart → `chart-render`; report template → needs design; chat-driven category grouping; big-purchase deliberation → Coordinator + conversation-state; voice capture → STT; investment advisory → advisory-only, `web/market-data`) each map to an existing architectural home, no new layer. **financial-advisor (the missing MVP leg) DONE — PR115 + PR117.** **PR115: mcp-finance `GET /internal/spending-by-category` read passthrough** (`InternalSpendingController` → the `spending_by_category` tool; `[from,to)` + `kind` default expense; 400 on bad window; mirrors `InternalBudgetController`). **PR117: finance-agent reactive spending analysis.** New `advisor/FinancialAdvisor` on the shared `Coordinator`: gathers two spend-by-category windows (recent 90d + prior 90d, via new `http/SpendingClient`) in parallel → one LLM synthesis from `[AGENT.md, financial-advisor SKILL.md] + {payload(userText), context}` → top categories, what changed vs the prior window + a hypothesis why, optimisation hints; text-first (chart deferred). Routed via a new `advice` action in `IntentRouter` (now `route(NormalizedMessage)` so the advisor has `householdId`; classifier prompt gains the analysis option). New `skills/finance/financial-advisor/SKILL.md`. Tested (`IntentRouterTest` advice case + new `FinancialAdvisorTest`: gather two windows → synth, MockWebServers); full finance-agent suite green (42). Doctrine held: orchestrator routes, finance-agent reasons, data via mcp-finance. **Next active line — `researcher` agent + shared `mcp-web` capability** (owner-chosen after finance MVP, 2026-06-20): a web research specialist with cheap-first token economy (HTTP search+fetch → one LLM synthesis), PR-sliced in [research.md](research.md). **Engine LOCKED = SearXNG** (self-hosted, free, no key/quota), behind a swappable `SearchEngine` interface. Slices ALL DONE (R-0..R-d, PR118–PR122). **R-0 (PR118, docs): [research.md](research.md) + INDEX + roadmap + this STATUS.** **R-a (PR119): `shared/mcp/mcp-web` capability-MCP (port 8098, no schema) + `web_search` over SearXNG** — `engine/SearchEngine` + `SearxngSearchEngine` (GET `/search?format=json` → `WebSearchHit`s, drops URL-less, trims to limit), `web_search` `@Tool`, `POST /internal/search` passthrough, contracts `WebSearchHit/Result/Input`; SearXNG service in compose + dev.yml + `infra/searxng/settings.yml` (JSON on, limiter off); MockWebServer test. **R-b (PR120): `fetch_url` + `/internal/fetch`** — `engine/PageFetcher` + `JsoupPageFetcher` (jsoup LOCKED, owner 2026-06-20; strip boilerplate, prefer `<article>`/`<main>`, cap+truncate, best-effort empty-on-failure), contracts `FetchUrlInput/PageContent`; jsoup version-managed in root pom. **R-c (PR121): `domains/researcher/researcher-agent` (port 8099) scaffold + orchestrator registration** — `@Import(AgentRuntimeConfig)`, binds `mcp-web` over SSE, registered in orchestrator `application.yml` agents + `RESEARCHER_AGENT_URL` (manifest-driven, no orchestrator Java change); `IntentController` shipped as a chat fallback. **R-d (PR122): the cheap-first `research` flow + skill.** `flow/Researcher` on the shared `Coordinator`: search (`http/WebSearchClient` → `/internal/search`) → fetch top N (`http/PageFetchClient` → `/internal/fetch`, parallel, soft-fail per page) → fold the corpus into `context` → ONE LLM synthesis from `[AGENT.md, research SKILL.md] + {payload(userText), context}` → summary + grouped article/video links. `IntentController` now runs the flow (a specialist treats every routed message as a research request); `skills/researcher/research/SKILL.md`. **Token economy is STRUCTURAL** — search + fetch are plain HTTP (no model cost); only the synthesis hits the LLM. Tested (`ResearcherFlowTest`: search → fetch → one `/v1/chat` carrying the corpus → summary; asserts exactly one LLM call). **researcher + mcp-web COMPLETE.** **`mcp-web` is the shared web capability (roadmap.md) — later bound by chef recipe-search / briefing news / finance investment-advisory (which also needs a sibling `market-data` capability).** **Next active line — `mcp-market-data` capability + finance `investment-advisor` skill** (owner-chosen 2026-06-21: "давай 3-е" = pull market-data + investment-advisory forward). A sibling capability-MCP to `mcp-web` (read-only `quote` over **Stooq** — source LOCKED, free/no-key, behind a swappable `MarketDataSource`) + a finance `investment-advisor` skill on the `Coordinator`, **advisory-only (never trades/moves money)**. PR-sliced in [market-data.md](market-data.md): **MD-0 (docs opener) DONE (PR125): market-data.md + INDEX + roadmap + finance.md + this STATUS.** **MD-a DONE (PR126): `shared/mcp/mcp-market-data` (port 8100, no DB, no backing container — Stooq is public HTTPS) + `quote` `@Tool` over Stooq.** `engine/MarketDataSource` + `StooqMarketDataSource` (`@ConditionalOnProperty marketdata.source=stooq`, matchIfMissing; GET Stooq `/q/l/?s={symbol}&f=sd2t2ohlcv&e=csv` → parse the CSV row; `N/D`→null fields = "no data" not an error) behind the swappable interface; `quote(symbol)` `@Tool` → new contracts `market/Quote(symbol, price?, asOf?, open?, high?, low?, volume?)` + `market/QuoteInput(symbol)`; `POST /internal/quote` passthrough (`InternalQuoteController`, blocking on `boundedElastic`, mirror mcp-web's `/internal/search`). Read-only — **no order/trade tool exists, by design (advisory data only)**. webflux-only, Spring AI `MethodToolCallbackProvider` exposes the one tool. Registered in root pom `<modules>`, compose (app service, no `depends_on`), `.env.example`, infra/README (8100). Tested (`InternalQuoteControllerTest`: full MCP context boots with 1 tool, parses a real row + unknown-symbol→null-price; MockWebServer stands in for Stooq, no network). Full reactor compiles clean; compose validates. **No agent binding yet** — the capability stands alone. **MD-c DONE (PR127): finance-agent binds `mcp-market-data` + `investment-advisor` flow.** `advisor/InvestmentAdvisor` on the shared `Coordinator` (copy `FinancialAdvisor`): gathers a `quote` per named symbol (`http/MarketDataClient` → `POST /internal/quote`, parallel, soft-fail per symbol, fan-out capped at 12) → one LLM synthesis from `[AGENT.md, investment-advisor SKILL.md] + {payload(userText, symbols), context(quotes)}` → considerations, **advisory-only (never trades/moves money; empty symbols → friendly invite, no call)**. Routed via a new `invest` action in `IntentRouter` — the classifier maps tickers → source-native symbols (`aapl.us`/`^spx`/`xauusd`/`btcusd`) and passes them in `symbols`. New `mcpMarketDataWebClient` bean + `finance-agent.mcp-market-data-url` (`MARKET_DATA_URL`) + the `spring.ai.mcp.client.sse.connections.mcp-market-data` binding (future LLM-driven selection; the deterministic call uses the HTTP passthrough). New `skills/finance/investment-advisor/SKILL.md`; AGENT.md (skill + advisory-only rule) + finance-agent README + compose (`depends_on: mcp-market-data` + `MARKET_DATA_URL`) updated. Tested (`InvestmentAdvisorTest`: gather a quote per symbol → synth + empty-symbols invite, MockWebServers; `IntentRouterTest` invest case maps symbols); full finance-agent suite green (45, was 42). **The market-data line (MD-0/a/c) is COMPLETE** — investment advisory works end-to-end (intent → classify+map symbols → quote per symbol → advisory synthesis), advisory-only; `mcp-market-data` is the shared read-only quotes capability any future agent (briefing) binds. (Optional later: **MD-b** `history` for trend.)
- **Next active line — `stylist` domain** (owner-chosen 2026-06-21: "давай стилист"). New `mcp-wardrobe` domain-MCP (owns a `wardrobe` schema) + `stylist-agent`, reusing the shared `mcp-media-processing` (vision **analysis** — catalogue garments, "analyse me": colour type/цветотип, fabrics, body shape) + `mcp-web` (trends). **MVP = cheap vertical, no GPU** (owner-refined 2026-06-21): upload wardrobe + self photos + body params → "analyse me" `style_profile` + a beautifully-formatted **capsule** + a **fabric/colour/texture analysis**, both rendered as **HTML** deliverables. **Output format LOCKED = HTML now, with a render-format seam for a future PDF-conversion integration** (owner 2026-06-21); Notion/image deferred. **Deferred (recorded vision):** GPU visualise-me / virtual try-on (CatVTON/Fooocus — engine lock later), marketplace search + buy links (Ozon/Yandex/Lamoda — source lock later), the "saw it on the street" composite, PDF, final template designs (owner to send examples). PR-sliced in [stylist.md](stylist.md): **ST-0 (docs opener) DONE (PR131): stylist.md + INDEX + roadmap + this STATUS.** **ST-a DONE (PR132): `domains/stylist/mcp-wardrobe` domain-MCP (port 8101, owns the new `wardrobe` schema — first stylist module).** New migration [040-wardrobe.yml](../infra/liquibase/features/040-wardrobe.yml) — two tables: `wardrobe.wardrobe_item` (one row per catalogued garment: name + category/colour/material/pattern/season/formality free-text varchars + `image_media_id` → media-service object, no cross-schema FK, mirrors `task_item.calendar_event_uid`) + `wardrobe.style_profile` (one per person via the unique `(household_id, owner_id)` index: person/colour type, body shape, `suitable_fabrics` jsonb, height/weight, `measurements` jsonb, `image_media_id`). New `wardrobe` schema added to [postgres/init.sql](../infra/postgres/init.sql) (schemas are pre-created there, NOT in the changelogs). Six `@Tool` methods on `WardrobeMcpTools` — garment CRUD (`add_item`, `list_items`, `update_item`, `delete_item`) + style-profile upsert/read (`set_style_profile` keyed on (household,owner) with null-owner = household-default via native-SQL `CAST` finder, `get_style_profile`). New contracts `wardrobe/{WardrobeItemDto, AddItemInput, UpdateItemInput, StyleProfileDto, SetStyleProfileInput}` (jsonb fields = `JsonNode`, mirrors `ConversationStateDto`). Scaffold per PATTERNS.md (template `mcp-tasks`, minus the scheduler — no outbound deps); webflux MCP server, JPA over Postgres. Tested (`McpWardrobeIntegrationTest`, 7 cases: add+descriptive fields, required-field guards, category filter + household scoping, partial update, delete+returns-row, style-profile upsert-in-place with jsonb roundtrip + one-row-per-(household,owner), get returns null-then-profile). Wired into root pom `<modules>`, compose (`depends_on: postgres+liquibase`, no scheduler), `.env.example`, infra/README (port 8101 + `wardrobe` schema row), PATTERNS numbering table (040-049 = stylist). Full reactor compiles; module suite green. **ST-b DONE (PR133): `domains/stylist/stylist-agent` scaffold (port 8102) + orchestrator registration + binds all three capabilities.** `@Import(AgentRuntimeConfig)`; AGENT.md (`name: stylist`, mcp: `mcp-wardrobe` + `mcp-media-processing` + `mcp-web`, skills: [] until ST-d) served at `GET /agents/stylist/manifest`. Binds the three MCPs over SSE (`spring.ai.mcp.client.sse.connections.*` — future LLM-driven tool selection; the deterministic ST-c..e flows call them over `/internal/*` HTTP passthroughs). `IntentController` ships a **chat fallback** (`chat/StylistChat` — one LLM turn, AGENT.md as system prompt; replaced branch-by-branch as flows land). `config/StylistAgentProperties` (the three MCP URLs + profile/notifier/memory) + `config/OutboundHttpConfig` (`mcpWardrobe/mcpMediaProcessing/mcpWeb` WebClients for the future flows + the `profile/notifier/memory` qualified beans the shared runtime clients pick up). Registered in orchestrator `application.yml` (`{name: stylist}` + `STYLIST_AGENT_URL`, manifest-driven — no orchestrator Java change; its classifier test builds its own registry so unaffected). Scaffold per PATTERNS.md "new agent" (template `researcher-agent`, leaner than finance-agent). Tested (`ManifestControllerTest`: full context boots with the MCP client disabled, AGENT.md parsed, runtime beans + LlmClient + StylistChat resolve, manifest lists all three capabilities). Wired into root pom `<modules>`, compose (stylist-agent service `depends_on` llm-gateway + profile/notifier/memory + the three MCPs; `STYLIST_AGENT_URL` in orchestrator env), `.env.example`, infra/README (port 8102). Full reactor compiles; module suite green. **ST-c (catalogue flow) split into c1/c2 — mirrors MP-c (the deterministic agent→MCP write goes over an HTTP `/internal/*` passthrough, NOT the MCP/SSE transport, which can't be MockWebServer'd per CLAUDE.md). ST-c1 DONE (PR134): `mcp-wardrobe` `POST /internal/item` add-passthrough → `add_item`.** New `web/InternalItemController` delegates straight to `WardrobeMcpTools.addItem` (servlet-style `ResponseEntity`, 400 on the required-field guard), so the tool's invariants apply identically; the MCP `@Tool` stays the entry point for future LLM-driven selection. Reuses the existing `AddItemInput` contract. Tested (`McpWardrobeIntegrationTest` now 8: WebTestClient POST adds a garment + missing-name → 400). README updated. Mirrors mcp-finance's `/internal/transaction` (PR39). **ST-c2 DONE (PR135): stylist-agent wardrobe-catalogue flow — ST-c COMPLETE.** `IntentController` now routes a **photo attachment** → new `catalogue/WardrobeCataloguer` (mirror of finance's `receipt-parser`): asks `mcp-media-processing` `caption` (new `http/CaptionClient` → `/internal/caption`) for a structured garment extract — instruction = the new `wardrobe-cataloguer` SKILL.md (strict-JSON `{name,category,colour,material,pattern,season,formality}` / `{"error":"not a garment"}`) → lenient-parses the draft → writes via `mcp-wardrobe` (new `http/WardrobeClient` → `/internal/item`), storing the photo's media id as `imageMediaId` (the attachment `storageUri`, a UUID). **Write-immediately** (owner bulk-loads the wardrobe; edits via `update_item` later); non-photo messages stay on the `chat/StylistChat` fallback. Any stage failing → friendly message. New skill at `domains/stylist/skills/wardrobe-cataloguer/SKILL.md`; AGENT.md `skills:` updated; README updated. The vision call lives once in the capability-MCP (reused, not re-embedded — same doctrine as MP-c). Tested (`WardrobeCataloguerTest`, 2 cases: photo → caption(+user note hint)+write asserts `/internal/caption` & `/internal/item` bodies incl. `imageMediaId`; "not a garment" → reply, no write; MockWebServers). Module suite green (3 tests). **ST-d DONE (PR136): "analyse me" flow + responsive HTML analysis deliverable + the render-format seam (owner chose one PR; body params from BOTH photo + typed text; HTML stored in media-service, reply = summary + link).** `mcp-wardrobe` gains `POST /internal/profile` → `set_style_profile` (`web/InternalProfileController`, mirror ST-c1). stylist-agent `IntentController` now routes a photo by a deterministic caption keyword heuristic: analyse-cues/body-params → new `analyse/AnalyseMe`, else the catalogue flow (default). `AnalyseMe`: `caption` (instruction = new `style-analyst` SKILL.md, user note folded in so the model also reads typed height/weight/measurements — satisfies "и то, и то") → parse profile JSON → `set_style_profile` via new `http/StyleProfileClient` (owner = the user) → render a **responsive HTML** analysis through the new **render seam** (`render/StylistRenderer` interface + `HtmlStylistRenderer` mobile-first inline-CSS, + `StylistDoc`/`RenderedDoc` model) → store it in media-service via new `http/MediaStoreClient` (multipart `POST /v1/media`, `text/html`) → reply with a summary + a link (`stylist-agent.public-media-base-url` + `/v1/media/{id}`, configurable so it opens on any device). Per-stage soft-fail (profile saved even if the page store fails → user still gets the summary). New skill `domains/stylist/skills/style-analyst/SKILL.md`; AGENT.md `skills:` += style-analyst. **Render seam LOCKED (owner 2026-06-21): HTML now, a PDF renderer drops in behind the same interface; lift to a shared `doc-render` capability-MCP on the second consumer.** New props `stylist-agent.{media-service-url, public-media-base-url}` + `mediaServiceWebClient` bean; compose stylist-agent gains `depends_on: media-service` + `MEDIA_SERVICE_URL`/`STYLIST_PUBLIC_MEDIA_BASE_URL`; `.env.example` updated. Tested: `McpWardrobeIntegrationTest` now 9 (profile upsert via `/internal/profile`); stylist-agent `AnalyseMeTest` 2 (self-photo+params → caption(+note)→profile(owner=user)→HTML upload→link; "not a person photo" → reply, no write) — module suite green (5). **Concrete-examples richness (a second LLM prose pass) deferred — the MVP page is grounded in the structured profile fields.** **ST-e DONE (PR137): capsule advisor on the `Coordinator` + capsule HTML — STYLIST MVP COMPLETE (ST-a..e).** `mcp-wardrobe` gains read passthroughs `GET /internal/items` (`web/InternalItemsController` → `list_items`) + `GET /internal/profile` (added to `InternalProfileController`, 404 when unset). New stylist-agent `flow/StylistAdvisor` (copy of `InvestmentAdvisor`/`FinancialAdvisor`): on a capsule-cue text it gathers in parallel `{wardrobe: GET /internal/items, profile: GET /internal/profile (soft 404→omitted), trends: mcp-web POST /internal/search}` via new `http/WardrobeReadClient` + `http/WebSearchClient` (season computed locally, added to payload), the `Coordinator` folds the successful sources into `context` and synthesizes a capsule from `[AGENT.md, capsule-advisor SKILL.md] + {payload(request, season), context}` → renders a **capsule HTML page with a garment-photo gallery** (render seam extended: `StylistDoc` gained an optional `imageUrls` gallery, `HtmlStylistRenderer` a responsive image grid) → stores in media-service → reply summary + link. Empty wardrobe → invite to catalogue first (no LLM). `IntentController` routes a capsule-cue non-photo message → advisor, else chat. New skill `capsule-advisor`; AGENT.md `skills:` += capsule-advisor. Tested: `McpWardrobeIntegrationTest` now 10 (`/internal/items` filter + `/internal/profile` 404→200); stylist-agent `StylistAdvisorTest` 2 (gather→synth→HTML store with the garment-photo gallery + the deliverable link; empty wardrobe → invite, no synthesis) — module suite green (7). **Stylist MVP done: catalogue (ST-c) + analyse-me/HTML (ST-d) + capsule/HTML (ST-e), all on the shared `mcp-media-processing`/`mcp-web` capabilities + the `mcp-wardrobe` domain-MCP + the render seam.** **Deferred (recorded, each a later line with its own owner-locked engine/source):** GPU visualise-me / virtual try-on, marketplace search + buy links, the "saw it on the street" composite, PDF output (drops into the render seam), final template designs (owner to send examples), and a second LLM "concrete examples" prose pass for the analysis page. **Phase 2 — luxury-editorial deliverable boards (owner refs 2026-06-21):** owner sent reference boards (The Row/Toteme/Dior aesthetic — ivory ground, serif display caps, grid, KEEP/QUESTION/REMOVE verdict tags, gold HERO row, palette swatches, Kibbe/colour-season/archetype, gap analysis with priority+price tier, coverage before/after). **Aesthetic LOCKED = luxury editorial; build now = editorial HTML on the owner's REAL photos + rich LLM analysis, photorealistic renders (garment-on-body / "you in different looks") stay on the deferred GPU image-gen line** (owner: editorial first, renders later). Menswear skills frame their prompts on Kibbe / Найденская-Трубецкова / Flusser methodology (apply, never reproduce copyrighted text). Boards in order — Body & Style Analysis → Wardrobe Audit → Capsule → Gap Analysis (gap's marketplace buy-links stay deferred). Sliced in [stylist.md](stylist.md) §Phase 2. **ST-f DONE (PR138): editorial render foundation.** `render/StylistDoc` extended to a board model (header kicker/title/subtitle + optional keyed `sections`, colour `palette` swatches, `verdicts` grid KEEP/QUESTION/REMOVE per garment, `hero` row, image `gallery`) with a fluent `builder` + back-compat constructors (the ST-d analysis / ST-e capsule pages compile unchanged). `HtmlStylistRenderer` rewritten to the locked luxury-editorial template (ivory/serif-caps/grid/gold-hero, responsive, dark-mode aware, Google-Fonts Cormorant+Jost — loads when served to a browser); renders each block only when present, HTML-escaped. Pure-unit-tested (`HtmlStylistRendererTest`, 3: all blocks + editorial chrome, text-only skips empties, escaping). stylist-agent suite green (10). Existing analyse/capsule deliverables now render in the new style automatically. **Theme refined (PR139, owner): noble warm-beige LIGHT-ONLY (dropped the dark-mode flip — owner wanted the beige always, like the refs) + display font Oranienbaum + body Manrope (both Cyrillic; Cormorant/Jost lacked it and fell back to Georgia).** **Follow-up (owner-suggested polish): lift theme (colours + fonts) into env vars so a redeploy re-skins without code — DONE in ST-n (PR148).** **ST-g DONE (PR139): Body & Style Analysis board — deep analysis modelled on the owner's favourite reference.** `style-analyst` SKILL upgraded to emit a full board JSON (Kibbe + subtype, archetype, body type/proportions/bone/posture, colour season + undertone/contrast + hex palette, silhouette strategies with harmony, neckline/waist, fabric & texture logic, what-not-to-wear, styling principles, style-codes, final direction + philosophy; still copies typed height/weight/measurements). `AnalyseMe.buildDoc` rewritten to populate the rich editorial board from the draft via the `StylistDoc` builder — each section/palette added only when the analysis filled it; `set_style_profile` still persists the schema fields (the board renders the fuller draft directly). Tested (`AnalyseMeTest` asserts the rendered board carries the subtitle/silhouettes/palette/avoid/style-codes/final-direction; `HtmlStylistRendererTest` updated for the Oranienbaum/light theme). stylist-agent suite green (10). **ST-h DONE (PR140): Wardrobe Audit board.** New `flow/WardrobeAuditor` on the `Coordinator` + `wardrobe-auditor` skill: gather wardrobe items + profile → one LLM synthesis → a verdict JSON (`{verdicts:[{name,verdict,reason}], hero, systemicPattern, palette}`) → render the audit board (KEEP/QUESTION/REMOVE verdict grid with **garment photos matched back by name**, gold hero row, palette swatches, "Системная ошибка" diagnosis) → store → reply summary (keep/question/remove counts) + link. Empty wardrobe → invite. `IntentController` routes an audit-cue text ("ревизия / разбор гардероба") → auditor (checked before capsule), else capsule/chat. New skill `wardrobe-auditor`; AGENT.md updated. Tested (`WardrobeAuditorTest`, 2: board renders verdict tags + photo-by-name + hero + palette + diagnosis + counts; empty → invite). stylist-agent suite green (12). **ST-i DONE (PR141): Capsule board editorial chrome.** The capsule already rendered on the editorial template (since ST-f); `StylistAdvisor.store` now builds the doc via the `StylistDoc` builder — kicker "Curated · Strategic · Aligned" + a season/occasion subtitle + an "Образы" section + the garment-photo gallery — consistent with the analysis/audit boards. suite green (12). **ST-j DONE (PR142): Wardrobe Gap Analysis board — PHASE 2 COMPLETE (all 4 boards live).** New `flow/GapAnalyst` on the `Coordinator` + `gap-analyst` skill: gather wardrobe + profile → one LLM synthesis → a gap JSON (`{gaps:[{name,fills,priority,priceTier}], doNotBuy, coverageBefore/After, focusAreas, palette}`) → render the gap board ("Что докупить" with priority/price tier, "Не покупать", "Фокус", a coverage `before → after` subtitle, palette) → store → reply summary + link. Runs even on an empty wardrobe (essentials become the gaps). `IntentController` routes a gap-cue text ("что докупить / список покупок") → gapAnalyst (after audit, before capsule). **Marketplace buy-links stay deferred.** New skill `gap-analyst`; AGENT.md updated. Tested (`GapAnalystTest`: gap board renders what-to-buy + priorities + do-not-buy + coverage 52%→88% + palette). stylist-agent suite green (13). **Phase 2 done: 4 editorial boards on the locked beige/Oranienbaum template — Body & Style Analysis (ST-g), Wardrobe Audit (ST-h), Capsule (ST-i), Gap Analysis (ST-j) — all on the shared `Coordinator` + `mcp-wardrobe`/`mcp-media-processing`/`mcp-web`, real photos + LLM analysis; photorealistic renders + marketplace links remain the deferred GPU/marketplace lines.** Open follow-up (owner-suggested polish): env-var theming (colours/fonts) — **DONE in ST-n (PR148)**. **ST-k DONE (PR143): poster layout redesign (owner-driven 2026-06-22).** Owner reacted that the clean stacked page didn't read like the reference *board* (too minimal, no centering, over-boxed). Reworked `HtmlStylistRenderer` to an **airy editorial poster**: hairline dividers instead of boxed cards (single beige ground), a **centered photo anchor** (new `StylistDoc.featuredImageUrl`), text sections in a **hairline-separated multi-column flow** (`column-count:2`), with verdict grid / hero / palette strip / gallery as full-width rows. `AnalyseMe` sets the featured photo = the analysed self-photo (`/v1/media/{mediaId}`). All four boards re-render on it (analysis reads dense + photo-anchored; audit/capsule/gap unchanged content). **Honest boundary (owner-agreed): the reference's drawn mini-figures / "you in different looks" model shots are image-generation — the deferred line; ST-k ships structure, figures + web-ref look images + generated imagery are later layers.** Tested (`HtmlStylistRendererTest` +featured-photo assertion; all flow tests green). stylist-agent suite green. (Theme env-var follow-up DONE in ST-n/PR148.) **ST-l DONE (PR144): image-gen capability-MCP scaffold (stub-first — owner-chosen).** Lay the whole image-generation infra now with a **stub** engine; flip to a self-hosted model later (owner's Mac Studio GPU) by config, no caller change. New `shared/mcp/mcp-image-gen` capability-MCP (port 8103, no DB, mirrors mcp-market-data): `generate_image(prompt, refMediaIds?)` → render via the configured `ImageEngine` (`StubImageEngine` placeholder PNG default / `LocalImageEngine` POSTs to `IMAGE_GEN_LOCAL_URL` when `image-gen.engine=local`, selected by `@ConditionalOnProperty`) → upload to media-service (`MediaUploader`) → return media id + engine. `/internal/generate` passthrough; new `imagegen/{ImageGenInput, ImageGenResult}` contracts. Deploy surface wired (root pom module, compose `depends_on: media-service`, `.env.example`, infra/README port 8103). **Not yet bound in stylist-agent / not yet called by the flows — that + the real engine are the next steps** (use-case order: board illustrations first via text-to-image, then virtual try-on with refMediaIds once a real model is up). Tested (`InternalGenerateControllerTest`: stub render → media-service multipart upload → media id + model `stub`). Starts the deferred GPU image-gen line; the boards' drawn figures / "you in different looks" become real once the engine is flipped + flows wired. **ST-m DONE (PR146): stylist-agent binds `mcp-image-gen` + the capsule board gets a generated lookbook illustration (mirrors MD-c — agent binds a capability + one flow uses it).** New `http/ImageGenClient` (`POST /internal/generate` → `ImageGenResult`, 60s timeout) + `mcpImageGenWebClient` bean + `stylist-agent.mcp-image-gen-url` (`MCP_IMAGE_GEN_URL`) + the `spring.ai.mcp.client.sse.connections.mcp-image-gen` SSE binding (future LLM-driven selection; the deterministic call uses the HTTP passthrough). `flow/StylistAdvisor.store` now first generates a text-to-image **lookbook illustration** (prompt grounded in the synthesized capsule + season) → sets it as the board's `featured` image, **soft-failed** (image-gen outage just drops the visual, the textual capsule still ships). With the **stub** engine it embeds a placeholder PNG (proves the wiring); flip `IMAGE_GEN_ENGINE=local` → a real illustration appears with no code change. AGENT.md `mcp:` += mcp-image-gen; compose stylist-agent gains `depends_on: mcp-image-gen` + `MCP_IMAGE_GEN_URL`; README updated. Tested (`StylistAdvisorTest` now asserts the `/internal/generate` call + the generated illustration embedded as the board's featured image; module suite green, 13). **Image-gen is now bound and used (board illustrations) — remaining image-gen steps: the real local engine + the deferred virtual try-on (refMediaIds).** **PARKED — owner-decided 2026-06-22: wait for GPU-model access before building `LocalImageEngine` / try-on (same posture as Stage 5 golden tests — BLOCKED on model access).** Rationale: a real `LocalImageEngine` must target the chosen model server's actual API (ComfyUI / A1111 / Fooocus / OpenAI-compatible — each differs), so writing it blind risks a mismatch; the stub infra is ready, the flip is one config line (`IMAGE_GEN_ENGINE=local`). **When unblocked:** lock the image-server API → implement `LocalImageEngine` + a MockWebServer CI test (the whisper precedent) → then design the try-on flow. **Do NOT pick "real image-gen engine" as an active line until the owner has model access.** **ST-n DONE (PR148): env-var theming — clears the long-standing owner-suggested polish.** New `config/StylistThemeProperties` (`stylist-agent.theme.*`) holds the board palette (paper/ink/soft/muted/line/gold/keep/question/remove) + the serif/sans CSS font stacks + the Google-Fonts `css2?` query; defaults = the locked beige/Oranienbaum aesthetic. `HtmlStylistRenderer` now builds the `:root` CSS variables + the fonts `<link>` from the theme (layout rules stay constant), so any value is overridable via `STYLIST_THEME_*` env (e.g. `STYLIST_THEME_PAPER=#f3ece0`) — a redeploy re-skins all four boards **without a code change**. Registered in `@EnableConfigurationProperties`; application.yml + .env.example + README updated. Tested (`HtmlStylistRendererTest` +re-skin case: custom palette/font propagates, locked default disappears); module suite green (14). **Note on "no new infra":** it's a lean-by-default principle (tooling-simplicity memory + the MFC-b Postgres-over-Redis call), NOT a ban — infra is added when it earns its keep (SearXNG, whisper, MinIO, Radicale all did); Notion was deferred on cost/payoff, not forbidden.
- **Next active line — `nutrition` (food) domain** (owner-chosen after stylist; scope synced with owner 2026-06-22 around two concrete cases). One `mcp-nutrition` domain-MCP (`nutrition` schema, port 8104) bound by two agents — **`nutritionist-agent`** (log + basket breakdown + ration + shopping list) + **`chef-agent`** (recipes). **Driving cases:** (1) grocery receipt → finance logs the expense **and** the nutritionist breaks the basket down (КБЖУ, good/bad) → nutritionist + chef propose a ration + recipes; (2) family shopping plan ("закупиться в Ленте") → check the store via web → multi-person (me + wife + 8-mo infant on прикорм) → ration + shopping list (nutritionist) + recipes (chef, food.ru links + HTML card). **Owner LOCKs (2026-06-22):** (1) **MVP = both tracks** — food log **and** the basket→breakdown→ration+recipes chain (the headline). (2) **Grocery receipt fans out to BOTH agents via the bus** — finance parses the receipt once, writes the expense, and (if grocery) extracts **line-items** + publishes a `basket.captured` event; `nutritionist-agent` **consumes** it and runs the breakdown (same consumer pattern as notifier/memory-service; no duplicate vision). Direct path (user sends a basket straight to the nutritionist, it captions itself) lands first; the auto fan-out is the IA slice on top. (3) **Multi-person from the start** (per-`owner_id` profiles + ad-hoc people in the request); ⚠️ **infant/medical safety — general guidance only, explicit "не замена педиатру" caveat**. (4) **Food/macro data = Open Food Facts** (free, no key) via a shared `mcp-food-data` capability (sibling market-data); MVP may LLM-estimate and bind it later. (5) **Chef recipes (MVP) = web-search links (food.ru) + HTML card**; generated step photos deferred to GPU image-gen. (6) **Shape = 1 domain-MCP + 2 agents** (both MVP — the cases need recipes). (7) **render lifted to a shared `doc-render`** — recommended a compile-time **`libs/doc-render`** lib (pure render, no new container), upgrading to `mcp-doc-render` only when PDF/native rendering needs isolation; `StylistDoc`→generic `Doc`, theme moves into the lib, stylist migrated with no visual change. Full plan + schema + inter-agent design + PR slices in [nutrition.md](nutrition.md); INDEX + roadmap + PATTERNS (`050-059` = nutrition) updated. **NU-0 DONE (PR149 docs opener; revised PR150 to the synced basket/multi-agent vision).** **DR-a DONE (PR151): render seam lifted to a shared `libs/doc-render` lib + stylist migrated (no visual change).** New `libs/doc-render` (pure Java, no Spring): `Doc` (was `StylistDoc` — generic board model, builder + back-compat ctors), `DocRenderer` (seam), `HtmlDocRenderer` (the luxury-editorial poster, now constructed with a `DocTheme`), `DocTheme` (palette/fonts POJO — canonical home of the locked beige/Oranienbaum defaults), `RenderedDoc`. stylist-agent depends on it: deleted its `render/*`; `StylistThemeProperties` (env `STYLIST_THEME_*`) keeps binding + gains `toDocTheme()`; new `config/RenderConfig` exposes the `DocRenderer` bean (`new HtmlDocRenderer(theme.toDocTheme())`); the four flows build `Doc` + render via the injected `DocRenderer`. Renderer unit test moved to the lib (`HtmlDocRendererTest`, 4 incl. the re-skin case); stylist suite green (10), doc-render green (4), full reactor compiles. **`libs/doc-render` is the shared deliverable renderer nutrition/chef now consume; PDF later behind the same seam (lib→`mcp-doc-render` only if native isolation needed).** **NU-a DONE (PR152): `domains/nutrition/mcp-nutrition` (port 8104, owns the new `nutrition` schema — first nutrition module).** New migration [050-nutrition.yml](../infra/liquibase/features/050-nutrition.yml) — three tables: `nutrition.meal_log` (one row per logged meal: description + source(photo|text) + `items`/macros jsonb + `image_media_id`), `nutrition.diet_profile` (one per person via the unique `(household_id, owner_id)` index, null-owner = household-default: macro goals + `restrictions`/`tastes` jsonb — holds the wife/infant profiles), `nutrition.basket` (one analysed grocery basket: merchant + source(receipt|manual) + `items`/totals + `analysis` jsonb + `receipt_media_id`). New `nutrition` schema added to [postgres/init.sql](../infra/postgres/init.sql). Eight `@Tool` methods on `NutritionMcpTools` — meal log (`log_meal`/`list_meals`/`delete_meal`), diet profile (`set_diet_profile`/`get_diet_profile`, keyed `(household,owner)` with null-owner = household-default via native-SQL `CAST` finder, mirrors mcp-wardrobe), basket (`save_basket`/`list_baskets`/`get_basket`). New contracts `nutrition/{MealLogDto,LogMealInput,DietProfileDto,SetDietProfileInput,BasketDto,BasketItem,SaveBasketInput}` (jsonb fields = `JsonNode`; basket `items` converts to/from `List<BasketItem>` via the shared `ObjectMapper`). Macros are nullable best-effort estimates (LLM now; FD-a/`mcp-food-data` later); `image_media_id`/`receipt_media_id` carry no cross-schema FK (mirrors `wardrobe_item.image_media_id`). Scaffold per PATTERNS.md (template `mcp-wardrobe`); webflux MCP server, JPA over Postgres, **no `/internal/*` passthroughs yet** (they land with the agent flows NU-c/d/f, mirror ST-c1). Tested (`McpNutritionIntegrationTest`, 7 cases: log+defaults, required-field guards, list scoping by household/owner + newest-eaten + limit, delete+returns-row, diet-profile upsert-in-place with jsonb roundtrip + one-row-per-(household,owner), get-null-then-profile, basket items/analysis roundtrip + list + get-by-id). Wired into root pom `<modules>`, compose (`depends_on: postgres+liquibase`, no agent deps), `.env.example`, infra/README (port 8104 + `nutrition` schema→module row), PATTERNS numbering table (050-059 = nutrition already reserved). Full reactor compiles; module suite green (8 tools registered). **NU-b DONE (PR153): `domains/nutrition/nutritionist-agent` scaffold (port 8105) + orchestrator registration + binds all three capabilities.** `@Import(AgentRuntimeConfig)`; AGENT.md (`name: nutritionist`, mcp: `mcp-nutrition` + `mcp-media-processing` + `mcp-web`, `skills: []` until NU-c, **+ the infant/medical-safety rule — general guidance + pediatrician caveat**) served at `GET /agents/nutritionist/manifest`. Binds the three MCPs over SSE (future LLM-driven tool selection; the deterministic NU-c.. flows call them over `/internal/*` HTTP passthroughs). `IntentController` ships the `chat/NutritionistChat` fallback (one LLM turn, AGENT.md as system prompt — replaced branch-by-branch as flows land). `config/NutritionistAgentProperties` (three MCP URLs + profile/notifier/memory) + `config/OutboundHttpConfig` (`mcpNutrition/mcpMediaProcessing/mcpWeb` WebClients + the qualified profile/notifier/memory beans the shared runtime clients pick up). Registered in orchestrator `application.yml` (`{name: nutritionist}` + `NUTRITIONIST_AGENT_URL`, manifest-driven — no orchestrator Java change). Scaffold per PATTERNS.md "new agent" (template `stylist-agent`, leaner — no flows/render yet). Tested (`ManifestControllerTest`: full context boots MCP-client-disabled, AGENT.md parsed, runtime beans + LlmClient + NutritionistChat resolve, manifest lists all three capabilities). Wired into root pom `<modules>`, compose (nutritionist-agent service `depends_on` llm-gateway + profile/notifier/memory + the three MCPs; `NUTRITIONIST_AGENT_URL` in orchestrator env), `.env.example`, infra/README (port 8105). Full reactor compiles; module suite green. **NU-c (food-log flow) split into c1/c2 — mirrors ST-c (the deterministic agent→MCP write goes over an HTTP `/internal/*` passthrough). NU-c1 DONE (PR154): mcp-nutrition `POST /internal/meal` add-passthrough → `log_meal`.** New `web/InternalMealController` delegates straight to `NutritionMcpTools.logMeal` (400 on the required-field guard), reusing the `LogMealInput` contract; the MCP `@Tool` stays the entry point for future LLM-driven selection. Mirrors mcp-wardrobe's `/internal/item` (ST-c1). Tested (`McpNutritionIntegrationTest` now 8: WebTestClient POST logs a meal + missing-description → 400). README updated. **NU-c2 DONE (PR155): nutritionist-agent food-log flow — NU-c COMPLETE.** New `foodlog/FoodLogger` handles BOTH a meal **photo** (→ `mcp-media-processing` `caption` via new `http/CaptionClient` → `/internal/caption`) and a **typed** meal (→ one llm-gateway `DEFAULT` turn), both using the new `meal-logger` SKILL as the strict-JSON extract prompt → lenient-parse → **write-immediately** via new `http/MealClient` → mcp-nutrition `/internal/meal`, attributed to the sender (`ownerId = userId`). `IntentController` routes a photo → `logPhoto` (the default photo route until NU-f's basket-cue split, mirrors stylist's catalogue default), a typed food-log cue ("съел…/на обед…/запиши…") → `logText`, else the `NutritionistChat` fallback. New skill `domains/nutrition/skills/meal-logger/SKILL.md`; AGENT.md `skills:` += meal-logger. The vision call lives once in the capability-MCP (reused, not re-embedded — same doctrine as MP-c/ST-c). Tested (`FoodLoggerTest`, 3: photo→caption(+note)+write asserts source=photo/ownerId/imageMediaId; typed→llm `/v1/chat`+write source=text; "not a meal" → reply, no write; MockWebServers). Module suite green (4). **NU-d (multi-person diet profile) split into d1/d2 — mirror NU-c. NU-d1 DONE (PR156): mcp-nutrition `POST` + `GET /internal/diet-profile` passthroughs** → `set_diet_profile` / `get_diet_profile` (`web/InternalDietProfileController`, 400 on bad set / 404 on unset read; (household,owner) keying; mirror mcp-wardrobe's `/internal/profile`). Tested (`McpNutritionIntegrationTest` now 9: upsert-in-place + 404-then-200 read). README updated. **NU-d2 DONE (PR157): nutritionist-agent diet-profiler flow — NU-d COMPLETE.** New `profile/DietProfiler`: a typed diet-profile cue → one llm-gateway turn with the new `diet-profiler` SKILL → extract `{scope, macro goals, restrictions, tastes, notes}` → upsert via new `http/DietProfileClient` → `/internal/diet-profile`. Scope `self` → `ownerId = userId`, `household` → `ownerId = null` (default). Named ad-hoc people (wife/infant) are carried inline in the NU-g ration request, not stored as owner rows (`diet_profile.owner_id` → `core.users`). `IntentController` routes a profile cue ("моя цель…/у меня аллергия…") → diet-profiler before the food-log cue. New skill `diet-profiler`; AGENT.md `skills:` += diet-profiler. Tested (`DietProfilerTest`, 3: self-scope upsert with restrictions; household-scope → ownerId omitted; "not a profile" → reply, no write). Module suite green (7). **NU-e (nutrition-analysis board) split e1/e2 — DONE. NU-e1 (PR158): mcp-nutrition `GET /internal/meals` read passthrough** → `list_meals` (ownerId scope + limit; `web/InternalMealsController`, mirror mcp-wardrobe's `/internal/items`). Tested (`McpNutritionIntegrationTest` now 10). **NU-e2 (PR159): nutritionist-agent nutrition-analysis flow — NU-e COMPLETE.** New `analysis/NutritionAnalyst` on the shared `Coordinator`: an analysis-cue text ("разбор питания", "как я питаюсь") gathers in parallel `{meals: GET /internal/meals (new `http/MealReadClient`), profile: GET /internal/diet-profile (new `DietProfileClient.get`, 404→empty)}` → one LLM synthesis from `[AGENT.md, nutrition-analyst SKILL.md] + {payload(request, mealCount), context}` (intake vs goals, deficits/excesses, recommendations) → renders an HTML board via the shared **`libs/doc-render`** (`config/RenderConfig` exposes the `DocRenderer` bean with the lib-default `DocTheme`) → stores in media-service (new `http/MediaStoreClient`, `source=nutritionist`) → reply summary + link. Empty food log → an invite to log first (no LLM call); per-step soft-fail (no profile just drops the goal comparison). `IntentController` routes analysis cues before the food-log cue. New skill `domains/nutrition/skills/nutrition-analyst/SKILL.md`; AGENT.md `skills:` += nutrition-analyst. New props/beans `media-service-url` + `public-media-base-url` (env `MEDIA_SERVICE_URL`/`NUTRITIONIST_PUBLIC_MEDIA_BASE_URL`) + `mediaServiceWebClient`; compose nutritionist-agent gains `depends_on: media-service` + the envs; `.env.example` + README + pom (`doc-render` dep) updated. Tested (`NutritionAnalystTest`, 2: gather meals+profile → synth → HTML store + link; empty log → invite, no LLM/store; MockWebServers). Module suite green (9). **The nutritionist now delivers its first HTML board (the third doc-render consumer after stylist's analysis/capsule).** **NU-f (basket breakdown, direct) split f1/f2 — DONE. NU-f1 (PR160): mcp-nutrition `POST /internal/basket` add passthrough** → `save_basket` (`web/InternalBasketController`, 400 on the required-field guard; mirror `InternalMealController`). Tested (`McpNutritionIntegrationTest` now 11). **NU-f2 (PR161): nutritionist-agent basket-breakdown flow — NU-f COMPLETE.** New `basket/BasketBreakdown` (the domain headline, direct path): a grocery basket sent straight to the nutritionist as a **photo** with a basket cue ("продукты"/"корзина"/"чек") → `mcp-media-processing` `caption` (new `http/BasketClient` writes; reuses `CaptionClient`), or a **typed list** ("разбери продукты"/"список покупок") → one llm-gateway `DEFAULT` turn — both via the new `basket-analyst` SKILL as the extraction+breakdown prompt with the diet profile folded in (so good/watch/cut respect goals/restrictions) → lenient-parse `{merchant, items[], totals, analysis{good/watch/cut}, summary}` → save via `/internal/basket` (source=receipt for a photo / manual for a list, `receiptMediaId` = the photo) → render a **verdict board** through the shared `libs/doc-render` (good→KEEP, watch→QUESTION, cut→REMOVE tiles + КБЖУ totals + summary) → store in media-service → reply summary + link. Per-stage soft-fail; no profile just drops the goal comparison; "not a basket"/no items → friendly reply, no write. `IntentController` routes a basket-cue **photo** → basket (else photo → food-log), and a basket-cue **text** → basket (after the analysis cue, before the food-log cue). New skill `domains/nutrition/skills/basket-analyst/SKILL.md`; AGENT.md `skills:` += basket-analyst. Tested (`BasketBreakdownTest`, 3: photo→caption(+profile+note)→save(source=receipt,mediaId)→board upload; typed list (no profile)→llm→save(source=manual); "not a basket"→reply, no save; MockWebServers with an `@AfterEach` drain for cross-test isolation). Module suite green (12). **The headline direct basket flow works (photo/list → КБЖУ + good/watch/cut → HTML verdict board); the automatic grocery-receipt fan-out (finance → nutrition off the bus) is the IA slice on top.** **IA-a (grocery-receipt fan-out producer) split a1/a2 — DONE.** **IA-a1 (PR162): `contracts/basket/BasketCapturedEvent` (topic `basket.captured`) + mcp-finance bus drop-point.** finance-agent is DB-less, so mcp-finance (which owns a datasource on the shared Postgres) gains the `event-bus` dep + `@Import(EventBusConfig)` (the `OutboxPublisher`) + `web/InternalBasketCapturedController` (`POST /internal/basket-captured` → `outbox.publish`, 202; 400 on missing householdId) — mirrors memory-service's `/v1/observations` drop-point, scoped to finance's own event. Event carries household, owner, merchant, items (reuses nutrition `BasketItem`, macros null — consumer re-estimates), receiptMediaId, capturedAt. test-schema += bus/outbox. Tested (`McpFinanceIntegrationTest` now 36). **IA-a2 (PR163): finance receipt-parser extracts line-items + grocery flag + publishes — IA-a COMPLETE.** The `receipt-parser` SKILL now also returns `is_grocery` + line `items`; `ReceiptParser` parses them and, when the receipt is a grocery basket with items, fire-and-forgets a `BasketCapturedEvent` (merchant + name/qty items + receiptMediaId) to the IA-a1 drop-point via new `http/BasketCapturedClient` (off the response path, soft-fail — same posture as `MemoryClient.observe`). Emitted at parse time (like the MFC-c observation), independent of the expense confirm; non-grocery/itemless receipts emit nothing. Items map to `BasketItem` name+qty only (nutrition re-estimates КБЖУ); fan-out capped at 100 items. Tested (`ReceiptFlowTest` now 5: grocery receipt → caption(is_grocery+items) → `/internal/basket-captured` POST carries merchant/items/receiptMediaId, asserted via a path-routing dispatcher since the fire-and-forget POST races the accounts GET; existing non-grocery tests unaffected). finance-agent suite green. **The producer half of case-1 is live: a grocery receipt → finance logs the expense AND publishes `basket.captured` to the bus.** **IA-b (grocery-receipt fan-out consumer) split b1/b2 — DONE; case-1 fan-out now works end-to-end.** **Design fork (owner-chosen 2026-06-23):** agents stay DB-less — the bus listener lives in mcp-nutrition (which owns a datasource), not in the agent, even though nutrition.md said "agent consumes"; mcp-nutrition forwards the event to the agent over HTTP where the breakdown reasoning lives. **IA-b1 (PR164): nutritionist-agent `POST /internal/basket-event` + `BasketBreakdown.breakdownFromEvent`** — one LLM breakdown over the line items finance already extracted (no re-vision) via the `basket-analyst` SKILL (diet profile folded in) → save basket → render the verdict board → **notify every household member** (no user reply channel on a bus consume, so it fans out like the gift-recommender); best-effort, empty-items ignored. `BasketBreakdown` refactored to shared `saveAndRender`/`render` helpers + injected `ProfileClient`/`NotifierClient`. Tested (`BasketEventFlowTest`, 2). **IA-b2 (PR165): mcp-nutrition bus consumer.** mcp-nutrition gains the `event-bus` dep + `@Import(EventBusConfig)` + a `config/EventBusListenerConfig` registering an `EventBusListenerContainer` over `bus.outbox` with `bus/BasketCapturedHandler` — drains `basket.captured`, parses it, and forwards to nutritionist-agent's `/internal/basket-event` via `nutritionistAgentWebClient` (new `config/HttpConfig` + `McpNutritionProperties` `mcp-nutrition.nutritionist-agent-url` ← `NUTRITIONIST_AGENT_URL`). Retry policy mirrors notifier/memory (transient forward failure throws → row stays PENDING/retried; foreign topic / bad payload accepted, not retried). test-schema += bus/outbox; compose mcp-nutrition gains `NUTRITIONIST_AGENT_URL` (no `depends_on` — would cycle with nutritionist-agent→mcp-nutrition; the forward is retried). Tested (`BasketCapturedConsumerIntegrationTest`, 2: published `basket.captured` row → forwarded to the agent MockWebServer + row PUBLISHED; foreign topic ignored but drained; Testcontainers). **The full case-1 chain is live: a grocery receipt → finance logs the expense + publishes `basket.captured` → mcp-nutrition consumer → nutritionist-agent breakdown → household notified — one receipt, vision done once, reaches both domains.** **NU-g DONE (PR166): nutritionist-agent ration + shopping-list flow (multi-person `Coordinator`) — the second driving case ("закупиться в Ленте").** New `flow/MealPlanner` on the shared `Coordinator`: on a ration-cue text it gathers in parallel `{profile: the sender's diet profile, householdProfile: the household-default profile (covers wife/infant without their own row), meals: recent food log, store: that store's availability via `mcp-web` — only when a store is named in the request}` → the `Coordinator` folds the successful sources into `context` and runs one LLM synthesis from `[AGENT.md, meal-planner SKILL.md] + {payload(request, season), context}` → a multi-person ration + a grouped shopping list → renders an HTML board via the shared `libs/doc-render` → stores in media-service → reply summary + link. Ad-hoc people (wife, the 8-mo infant on прикорм) are read by the LLM from the request text (not stored as owner rows); the `meal-planner` SKILL carries the infant/medical-safety caveat. Per-step soft-fail (no profile/store just drops that constraint); render/store failure still hands back the textual plan; synthesis failure → a friendly message. New `http/WebSearchClient` (`POST /internal/search` on mcp-web — store lookup, mirrors stylist's). `IntentController` routes a ration cue ("составь рацион"/"план питания"/"закупиться в Ленте") → `MealPlanner` **before** the basket cue (so a planning "закупиться" wins over basket breakdown). New skill `domains/nutrition/skills/meal-planner/SKILL.md`; AGENT.md `skills:` += meal-planner. Tested (`MealPlannerTest`, 2: named store → gather self+household profiles + meals + web search → synth carries the SKILL/profiles/meals/store-hit → HTML store + link, parallel gather routed by a path Dispatcher; no store named → no web search; MockWebServers). Module suite green (16). **The chef invocation (ration → recipes via the hub) lands with `chef-agent` (CH-b).** **CH-a DONE (PR167): `domains/nutrition/chef-agent` scaffold (port 8106) + orchestrator registration + binds mcp-nutrition + mcp-web + doc-render.** `@Import(AgentRuntimeConfig)`; AGENT.md (`name: chef`, mcp: `mcp-nutrition` + `mcp-web`, `skills: []` until CH-b) served at `GET /agents/chef/manifest`. Binds the two MCPs over SSE (future LLM-driven tool selection; the deterministic CH-b flow calls them over `/internal/*` HTTP passthroughs). `IntentController` ships the `chat/ChefChat` fallback (one LLM turn, AGENT.md as system prompt). `config/ChefAgentProperties` (mcp-nutrition/mcp-web + media-service + profile/notifier/memory) + `config/OutboundHttpConfig` (`mcpNutrition/mcpWeb/mediaService` WebClients + the qualified profile/notifier/memory beans the shared runtime clients pick up) + `config/RenderConfig` (the shared `DocRenderer` bean for the CH-b recipe cards). Registered in orchestrator `application.yml` (`{name: chef}` + `CHEF_AGENT_URL`, manifest-driven — no orchestrator Java change). Scaffold per PATTERNS.md "new agent" (template `nutritionist-agent`, leaner — no vision/flows yet). **Note: chef shares the `domains/nutrition/skills/` dir with the nutritionist, so its `SkillRegistry` also sees the nutritionist's skills — benign (the manifest declares chef's own skills, flows select by name).** Tested (`ManifestControllerTest`: full context boots MCP-client-disabled, AGENT.md parsed, runtime beans + LlmClient + ChefChat + DocRenderer resolve, manifest lists mcp-nutrition + mcp-web). Wired into root pom `<modules>`, compose (chef-agent `depends_on` llm-gateway + profile/notifier/memory + media-service + mcp-nutrition + mcp-web; `CHEF_AGENT_URL` in orchestrator env), `.env.example`, infra/README (port 8106). Full reactor compiles; module suite green. **CH-b1 DONE (PR168): chef-agent recipe flow (direct) + the doc-render link-list generalisation.** New `flow/RecipeFinder`: a recipe-cue message → search recipes via `mcp-web` (`http/WebSearchClient` → `/internal/search`, query biased with "рецепт") → fold the hits into a `context` → one LLM synthesis on the shared `Coordinator` from `[AGENT.md, recipe-finder SKILL.md] + {payload(request), context}` → render an HTML **recipe card** (the synthesized text as sections + the **real recipe links from the search hits**, never LLM-invented URLs) via the shared `libs/doc-render` → store in media-service (`http/MediaStoreClient`, `source=chef`) → reply summary + link. Empty search → the skill falls back to a couple of simple dishes (no links). Token economy is structural (search = HTTP, only the synthesis hits the LLM). **doc-render extended (the planned generalisation):** `Doc` gained an optional `links` list (`LinkItem{label,url,note}` + builder `link(...)`, back-compat ctors null it); `HtmlDocRenderer` renders it as a styled `<a target=_blank rel=noopener>` list — the generic recipe-link list nutrition anticipated, landed with its first consumer. `IntentController` routes a recipe cue ("рецепт"/"что приготовить") → `RecipeFinder`, else the `ChefChat` fallback. New skill `domains/nutrition/skills/recipe-finder/SKILL.md`; AGENT.md `skills:` += recipe-finder. Tested (`RecipeFinderTest`, 2: recipe request → search → synth carries the SKILL + hits → HTML card with clickable food.ru links; empty search → synth + card, no link list; MockWebServers) + `HtmlDocRendererTest` +link-list case (doc-render 5, stylist 14 unaffected by the back-compat `Doc` change). chef-agent suite green (3). Full reactor compiles. **CH-b2 DONE (PR169): ration → recipes over the hub — the chef line is COMPLETE.** Chef side: new `web/ActionController` exposes `POST /agents/chef/actions/{action}`; `recommend_recipes` reads `args.request` (the ration text) and runs the shared `RecipeFinder.recommend` core (web recipe search → synthesis → HTML recipe card) → returns `{link, summary}` as an `AgentActionResult` (never an HTTP error). `RecipeFinder` refactored to expose `recommend(household,user,request)` used by both the intent path and the action; the search query is capped to the request's headline (a long ration makes a poor query). Nutritionist side: new `http/OrchestratorInvokeClient` + `orchestratorWebClient` bean + `orchestrator-url` prop; `MealPlanner`, once the ration is rendered, invokes chef `recommend_recipes` over `/v1/agents/invoke` and folds the returned recipe-card link into the reply ("Рецепты от шефа: …") — **soft-failed** (a chef outage / `ok=false` just drops the recipes line, the ration still ships). compose: nutritionist-agent gains `ORCHESTRATOR_URL` (no `depends_on` — the orchestrator depends on the agents, so the reverse edge would cycle; the invoke is best-effort). Tested (`RecipeFinderTest` +2: hub action → card link, unknown action → ok=false; `MealPlannerTest` now asserts the chef invoke `{targetAgent:chef, action:recommend_recipes, requestingAgent:nutritionist}` + the folded recipe link, and the soft-fail path; chef suite 5, nutritionist 16). Full reactor compiles. **The nutrition MVP is COMPLETE** — food log (NU-c) + multi-person diet profiles (NU-d) + nutrition analysis (NU-e) + basket breakdown (NU-f) + the grocery-receipt fan-out (IA-a/b, case 1) + ration/shopping list (NU-g) + chef recipes (CH-a/b) — one `mcp-nutrition` domain-MCP + two agents on the shared `Coordinator`/`mcp-media-processing`/`mcp-web`/`libs/doc-render`. **FD-a DONE (PR171): `shared/mcp/mcp-food-data` (port 8107, no schema, no backing container — Open Food Facts is public HTTPS, like Stooq) + `food_lookup(query)` `@Tool`.** `engine/FoodDataSource` + `OpenFoodFactsDataSource` (`@ConditionalOnProperty fooddata.source=openfoodfacts`, matchIfMissing) behind the swappable interface (sibling of mcp-market-data/Stooq): a numeric query → `GET /api/v2/product/{barcode}.json` (precise), a text query → `GET /cgi/search.pl?...&json=1&page_size=1` (best-effort first hit); parses `nutriments` per-100g macros → new contracts `food/FoodFacts(query, name?, brand?, barcode?, quantity?, nutriScore?, kcal100g?, protein100g?, fat100g?, carbs100g?)` + `food/FoodLookupInput(query)`. Missing product/fields → null (no data, not an error; mirrors `Quote`'s null price). `POST /internal/food-lookup` passthrough (`InternalFoodLookupController`, blocking on `boundedElastic`, mirror `/internal/quote`). Read-only reference data — no write tool, by design. webflux-only; Spring AI `MethodToolCallbackProvider` exposes the one tool. Registered in root pom `<modules>`, compose (app service, no `depends_on`), `.env.example`, infra/README (8107). Tested (`InternalFoodLookupControllerTest`, 3: barcode → product API + parsed macros, name → search API first hit, unknown → null name; MockWebServer stands in for Open Food Facts, no network). Full reactor compiles; compose validates. **Not yet bound** — the nutrition agents use LLM macro estimates today; binding `food_lookup` into the basket/log analysis for precise macros is the follow-up (**FD-c**, mirror MD-c). **The nutrition domain capability surface is complete** — `mcp-nutrition` domain-MCP + `mcp-food-data` reference capability. **FD-c DONE (PR172): nutritionist-agent binds `mcp-food-data` + the basket breakdown grounds its КБЖУ in precise per-100g facts (mirror MD-c).** New `http/FoodDataClient` (`POST /internal/food-lookup` → `FoodFacts`, 8s timeout, mirror `MarketDataClient`/`CaptionClient`) + `mcpFoodDataWebClient` bean + `nutritionist-agent.mcp-food-data-url` (`MCP_FOOD_DATA_URL`) + the `spring.ai.mcp.client.sse.connections.mcp-food-data` binding (future LLM-driven selection; the deterministic enrichment uses the HTTP passthrough). `BasketBreakdown.render` (shared by the direct NU-f path **and** the IA-b bus-fan-out path) now first runs `enrichFacts`: a parallel `food_lookup` per item (soft-failed per item, capped at 40, concurrency 6), keeping only matched products, folded into the board as a "Точные КБЖУ (Open Food Facts, на 100 г)" section (name/brand + per-100g macros + Nutri-Score). Read-only, no extra LLM call — a no-match just omits the section (the LLM's own estimate still ships); quantity→portion math stays out of scope. AGENT.md `mcp:` += mcp-food-data; compose nutritionist-agent gains `depends_on: mcp-food-data` + `MCP_FOOD_DATA_URL`; `.env.example` + READMEs (nutritionist-agent, AGENT.md, mcp-food-data → "bound") updated. Tested (`BasketBreakdownTest` asserts the board carries the Open Food Facts facts section + Nutri-Score + the per-item lookup; `BasketEventFlowTest` wires the enrichment on the bus path too; MockWebServer `Dispatcher` stands in for mcp-food-data). Full nutritionist suite green (16). **The basket breakdown is now grounded in real reference macros where Open Food Facts matches — nutrition is the first precise-data consumer of the shared capability.** Next active line — the **creator / content-factory agent** (Stage 6), STARTED below.
- **Stage 6 — `creator` (content factory) STARTED.** Owner-chosen after nutrition (2026-06-23): a per-user creator agent that monitors trends (YouTube/Reddit/Telegram/web now; Threads/Instagram/Pinterest later) → in 10–15 min delivers 3–5 fresh trends (with links) + 10 post ideas + 2–3 ready drafts (title/text/CTA/hashtags) + per-platform format recs. **Shape = the researcher gather→synthesize on the shared `Coordinator`, fanned out to multiple sources** (token economy structural — gather = HTTP/API, one LLM synthesis). **Owner LOCKs (2026-06-23):** (1) MVP = **API/RSS sources only** (YouTube Data API, Reddit API, Telegram public channels, web/SearXNG via the existing `mcp-web`); Threads/Instagram/Pinterest (no API) **deferred** to a later `mcp-browser`. (2) **Free-OSS-first** — each source hides its upstream behind a swappable `*Source` (Stooq/SearXNG/Open Food Facts precedent). (3) **Per-user content tracks** via `creator_profile` keyed `(household, owner)` (diet/style-profile pattern). (4) **browser-use is an engine, not a rebuild** — reused behind `mcp-browser` when JS/login-gated platforms land. (5) **Deliverable = HTML via the shared `libs/doc-render`**. Also closes the long-deferred Stage-4 chain `calendar.birthday_upcoming → creator.draft_greeting → notifier.send` (CR-g). Full plan + schema + capability list + PR slices in [creator.md](creator.md). **CR-0 DONE (PR173, docs opener):** creator.md + INDEX + PATTERNS (060-069 = creator) + roadmap + this STATUS. Shape: `mcp-creator` domain-MCP (port 8108, owns `creator` schema = `creator_profile` + `trend` cache + `content_piece` idea/draft history) + `creator-agent` (8109) + source capability-MCPs `mcp-youtube` (8110) / `mcp-reddit` (8111) / `mcp-feeds` (8112); `mcp-browser` (8113) deferred. **CR-a DONE (PR174): `domains/creator/mcp-creator` (port 8108, owns the new `creator` schema — first creator module).** Migration [060-creator.yml](../infra/liquibase/features/060-creator.yml) — three tables: `creator_profile` (one per person via the unique `(household_id, owner_id)` index, null-owner = household-default: niche/audience/tone + `platforms`/`guardrails` jsonb), `trend` (cached trend: source/platform/title/url/summary + `metrics` jsonb), `content_piece` (idea/draft: kind + body/cta + `hashtags` jsonb + status + soft `trend_id` provenance, no FK — the cache is evictable). `creator` schema added to init.sql. Eight `@Tool` methods on `CreatorMcpTools` — profile (`set_creator_profile`/`get_creator_profile`, keyed (household,owner) via the native-SQL `CAST` finder), trend cache (`save_trend`/`list_trends`), content pieces (`save_content_piece`/`list_content_pieces`/`get_content_piece`/`delete_content_piece`). New contracts `creator/{CreatorProfileDto, SetCreatorProfileInput, TrendDto, SaveTrendInput, ContentPieceDto, SaveContentPieceInput}` (jsonb = `JsonNode`). Scaffold per PATTERNS.md (template `mcp-nutrition`, minus the bus consumer — no inbound events yet); **no `/internal/*` passthroughs yet** (they land with the agent flows CR-c/d/e). Tested (`McpCreatorIntegrationTest`, 7 cases via Testcontainers). Wired into root pom, compose (`depends_on: postgres+liquibase`), `.env.example`, infra/README (8108 + `creator` schema row). Full reactor compiles; module suite green (8 tools). **CR-b DONE (PR175): `domains/creator/creator-agent` (port 8109) scaffold + orchestrator registration + binds mcp-creator + mcp-web.** `@Import(AgentRuntimeConfig)`; AGENT.md (`name: creator`, mcp: `mcp-creator` + `mcp-web`, `skills: []` until CR-c, + no-clickbait/platform-rules guardrails) at `GET /agents/creator/manifest`. Binds the two MCPs over SSE (the CR-c.. flows call them over `/internal/*` passthroughs). `IntentController` ships the `chat/CreatorChat` fallback; `config/` holds `CreatorAgentProperties` + `OutboundHttpConfig` (`mcpCreator`/`mcpWeb` + the qualified profile/notifier/memory beans the runtime clients pick up). Registered in orchestrator `application.yml` (`{name: creator}` + `CREATOR_AGENT_URL`). Template `researcher-agent`/`nutritionist-agent`. Tested (`ManifestControllerTest`); wired into root pom, compose, `.env.example`, infra/README (8109). Full reactor compiles; module suite green. **CR-c (creator-profile flow) split c1/c2 (mirror NU-d). CR-c1 DONE (PR176): mcp-creator `POST` + `GET /internal/creator-profile` passthroughs** → `set_creator_profile` / `get_creator_profile` (`web/InternalCreatorProfileController`, 400 on bad set / 404 on unset read; (household,owner) keying; mirror mcp-nutrition's `InternalDietProfileController`). Tested (`McpCreatorIntegrationTest` now 8). **CR-c2 DONE (PR177): creator-agent creator-profiler flow — CR-c COMPLETE.** New `profile/CreatorProfiler`: a typed profile cue → one llm-gateway turn with the new `creator-profiler` SKILL → extract `{scope, niche, audience, tone, platforms, goals, guardrails, notes}` → upsert via new `http/CreatorProfileClient` → `/internal/creator-profile` (self → `ownerId=userId`, household → null default). `IntentController` routes a profile cue → creator-profiler, else `CreatorChat`. New skill `creator-profiler`; AGENT.md `skills:` += creator-profiler. The `diet-profiler` shape. Tested (`CreatorProfilerTest`, 3). Module suite green (4). **YT-a DONE (PR178): `shared/mcp/mcp-youtube` (port 8110, no schema — first creator source).** `youtube_trends` reads trending/most-relevant videos from the YouTube Data API v3 (one `GET /search?part=snippet&type=video&order=relevance` call) → each item → a uniform `trends/TrendHit{source, platform, title, url, summary?, metrics?}` (the `watch?v=` link, channel + publishedAt in `metrics`; the researcher's `WebSearchHit` generalised across sources). Behind a swappable `engine/VideoTrendsSource` (`youtube.source=youtubedata` default, sibling of `FoodDataSource`/`MarketDataSource`); **free quota needs `YOUTUBE_API_KEY`, blank key → empty list** (graceful soft-fail for the multi-source gather; no key in CI). New contracts `trends/{TrendHit, YoutubeTrendsInput}`; `POST /internal/youtube-trends` passthrough (mirror `InternalFoodLookupController`). Scaffold per PATTERNS "capability-MCP" (template `mcp-food-data` — webflux MCP, no JPA/datasource/backing container). Tested (`InternalYoutubeTrendsControllerTest`, 3 via MockWebServer: search → 2 hits w/ metrics + query params, no matches → empty, blank query → empty no upstream call). Wired into root pom, compose (no `depends_on`), `.env.example`, infra/README (8110). Not bound yet (creator-agent binds it in CR-d). **RD-a DONE (PR179): `shared/mcp/mcp-reddit` (port 8111, no schema — second creator source, sibling of YT-a).** `reddit_trends` reads hot/most-relevant posts from the Reddit API: app-only OAuth (one `POST /api/v1/access_token`, grant `client_credentials`, HTTP Basic with the app id:secret) mints a bearer token → one listing call (`/r/{sub}/hot` | `/r/{sub}/search` | global `/search`) → each post → a uniform `trends/TrendHit` (the permalink, subreddit + score + comment count in `metrics`). Behind a swappable `engine/SocialTrendsSource` (`reddit.source=redditapi` default); **free API needs `REDDIT_CLIENT_ID` + `REDDIT_CLIENT_SECRET`, blank → empty list** (graceful soft-fail; no creds in CI). New contract `trends/RedditTrendsInput`; `POST /internal/reddit-trends` passthrough. Scaffold per PATTERNS "capability-MCP" (template `mcp-youtube`). Tested (`InternalRedditTrendsControllerTest`, 3 via one MockWebServer for both auth + API hosts: subreddit hot → token then listing → 1 hit w/ metrics + Bearer header; query-only → global `/search`; no target → empty no upstream call). Wired into root pom, compose (no `depends_on`), `.env.example`, infra/README (8111). Not bound yet (creator-agent binds it in CR-d). **FE-a DONE (PR180): `shared/mcp/mcp-feeds` (port 8112, no schema — third creator source, sibling of YT-a/RD-a).** `feed_items(source)` takes an RSS/Atom feed URL (→ fetch + Rome `SyndFeedInput`) or a public Telegram channel handle `durov`/`@durov` (→ fetch `t.me/s/{channel}` + jsoup parse `.tgme_widget_message`, newest-first) → each item → a uniform `trends/TrendHit` (platform `rss` w/ `{publishedAt, author}` metrics, or `telegram` w/ `{channel}`). Behind a swappable `engine/FeedSource` (`feeds.source=romejsoup` default); **no key** (both surfaces public). Added `com.rometools:rome` to the parent dependencyManagement (jsoup already managed). New contract `trends/FeedItemsInput`; `POST /internal/feed-items` passthrough. Scaffold per PATTERNS "capability-MCP" (template `mcp-youtube`). Tested (`InternalFeedItemsControllerTest`, 3 via one MockWebServer: RSS XML → Rome → 2 hits w/ author + publishedAt; Telegram HTML → jsoup → 2 hits newest-first w/ channel; blank source → empty no fetch). Wired into root pom, compose (no `depends_on`), `.env.example`, infra/README (8112). Not bound yet (creator-agent binds it in CR-d). **All three source capabilities (YT-a/RD-a/FE-a) done.** **CR-d DONE (PR181): the headline trend → ideas → drafts synthesis.** `flow/ContentStrategist` on the shared `Coordinator`: a trend/ideas/draft cue (new `IntentController` branch after the profile cue) → resolve the creator track (self → household-default) → niche = profile.niche or the request text → gather `web` + `youtube` + `reddit` for the niche (and a named RSS/`@channel` `feed` if the request mentions one) in parallel via the new single `http/TrendGatherClient` (binds all four `/internal/*` passthroughs; maps web `WebSearchHit`→`TrendHit`) → ONE `DEFAULT` synthesis with the new `content-strategist` SKILL → render an HTML content-plan board via `libs/doc-render` (`config/RenderConfig` `DocRenderer`; the synthesis as the «План» section + a **provenance links section built deterministically from the gathered corpus** so links are real even on a mock LLM) → store via the new `http/MediaStoreClient` (`POST /v1/media`) → reply with a first-line summary + the link. Per-source soft-fail (Coordinator omits a failed/empty source); render/store failure still returns the textual plan. `creator-agent` now binds `mcp-youtube`/`mcp-reddit`/`mcp-feeds` (AGENT.md `mcp:` + SSE connections) + `media-service`; `CreatorAgentProperties`/`OutboundHttpConfig` gain the source + media URLs + `publicMediaBaseUrl`; doc-render dep added. Tested (`ContentStrategistTest`, 2 via 6 MockWebServers: full gather→synth→render→store→link + one source 500 → still a plan); creator-agent module suite green (6). Wired into compose (`depends_on` the sources + media) + `.env.example`. **CR-e DONE (PR182): the content-strategist persist.** After the synthesis, `creator-agent` writes the run into the `mcp-creator` cache via the new `http/CreatorCacheClient`: the gathered `TrendHit` corpus → `POST /internal/trends` in one batch (new `web/InternalTrendController`, loops `save_trend`), and the synthesized plan → `POST /internal/content-piece` as a `draft` (new `web/InternalContentPieceController`), both attributed to the speaker. **`save_trend` is now idempotent on the source link** — a trend with the same `(householdId, ownerId, url)` already cached is returned as-is rather than duplicated (new `TrendRepository.findForUrl`, native-SQL `CAST` for the null-owner default, mirroring `findForOwner`; a null `url` never dedups), so re-running a gather is safe. Persist runs in the chain before render/store but **soft-fails** — the reply already carries the deliverable link, so a cache miss only logs. Tested: `ContentStrategistTest` now asserts the batched trend POST (web/youtube/reddit urls) + the draft POST; `McpCreatorIntegrationTest` (now 10) adds the per-`(owner, url)` dedup case + the two `/internal/*` write endpoints. **CR-g (the `draft_greeting` action) split g1 (callee) / g2 (caller), mirroring CH-b2/NU-g. CR-g1 DONE (PR183): the `draft_greeting` action endpoint.** `creator-agent` `POST /agents/creator/actions/draft_greeting` (new `web/ActionController`): args `{person, occasion?}` (occasion defaults to a birthday) → one LLM draft via the new `greeting-drafter` skill (new `flow/GreetingDrafter`) → returns `{greeting, model}`. Always an `AgentActionResult` (structured `ok=false` on a bad request / LLM failure), mirroring the chef's `ActionController` (minus web/render — the greeting is plain text). AGENT.md `skills:` += greeting-drafter (+ a responsibilities bullet). Tested (`ActionControllerTest`, 3: ok draft + skill/payload reached the LLM, missing person → structured error, unknown action → rejected); module suite green (9). **CR-g2 DONE (PR184): the chain closed (calendar caller).** On a `birthday.greet` wake the calendar agent's new `flow/BirthdayGreeter` invokes `creator.draft_greeting` over the orchestrator hub (new longer-timeout `OrchestratorInvokeClient.invoke(req, timeout)` — the draft sits behind an LLM on the creator side, the 5s passthrough default is too tight) → fans the returned greeting out to the household via notifier — closing the long-deferred Stage-4 chain `calendar.birthday_upcoming → creator.draft_greeting → notifier.send`. **Best-effort with a fallback:** when the creator can't help (no person to name, hub/LLM error, `ok=false`, or empty draft) `BirthdayGreeter.greet` resolves `false` and `TriggerController` falls back to the calendar's own `birthday-greeter` skill, so the wake always greets. Tested (`TriggerControllerTest`: the creator-hub path — invoke is `creator.draft_greeting`, greeting fans out, no local LLM — + an `ok=false` fallback to the local skill; the orchestrator MockWebServer is now an action-routing `Dispatcher` so the gift/birthday paths don't share an enqueue order, and per-test request-queue draining keeps `takeRequest` clean; suite 14 green). **Stage 6 creator MVP COMPLETE (CR-0…g)** — content factory shipped: per-person creator track, trend → ideas → drafts synthesis (multi-source, HTML deliverable), trend/draft cache, and the inter-agent greeting chain. Feed gather is request-token-driven for now (the profile has no monitored-feed list). **Deferred (recorded in [creator.md](creator.md) §Deferred):** Threads/Instagram/Pinterest via `mcp-browser`, generated post imagery, post scheduling/auto-posting, paid trend providers.
- **Next goal:** (a) `receipt-parser` skill (LLM-vision draft → confirm → add). **Storage decision (owner, MinIO-now):** photo bytes live in MinIO via a central **media-service** built into the foundation (future nutrition / stylist / researcher agents will all catalogue media there); the media path is on `storageUri`, NOT inline base64. Split: **PR36 (vision message content in llm-gateway) DONE**; **PR37 (media-service: MinIO + `media` schema + store/fetch REST) DONE**; **PR38 (gateway-telegram captures the Telegram photo → media-service → `NormalizedMessage.attachment(storageUri)`) DONE**; **PR39 (mcp-finance `POST /internal/transaction` add-passthrough so an agent can persist a parsed draft) DONE**; **PR40 (mcp-finance `GET /internal/accounts` list-passthrough so the receipt flow can resolve a target account) DONE**; **PR41 (`receipt-parser` flow in finance-agent: photo → vision draft → resolve account → write-immediately `add_transaction`) DONE — `receipt-parser` feature complete**. **Confirm flow = write-immediately MVP** (owner); confirm-before-write deferred — see Deferred work. **Next:** (b) Money Pro CSV import. **Mapping decision (owner) = auto-create accounts (MVP)** over interactive multi-turn (which would need the deferred conversation-state layer); a one-time history import is better served by auto-creating accounts by name (user renames/merges later). Split: **PR42 (gateway-telegram captures document/CSV uploads → media-service, `kind=file`) DONE**; **PR43 (`import_moneypro_csv` gains `autoCreateAccounts`) DONE**; **PR44 (mcp-money-pro-import `POST /internal/import` passthrough) DONE**; **PR45 (finance-agent CSV-attachment flow → auto-create import) DONE — goal (b) complete**. Interactive mapping deferred with the conversation-state layer. **(c) matviews (`fin_mv_monthly_by_category`, `fin_mv_account_balance`) + `refresh_matviews` tool — PR46 DONE.** The matviews ARE the dashboard read surface (Metabase/Grafana query them directly), so building them unblocks the dashboard rather than waiting on it; `refresh_matviews` keeps them current. **Stage-2 finance goals (a)+(b)+(c) all complete.** **Deferred:** scheduler-driven periodic `refresh_matviews` registration (no cron caller yet — manual/agent-triggered for now); **`update_transaction` + `delete_transaction` — PR47 DONE**; **`export_csv` — PR48 DONE.** The full mcp-finance tool surface finance.md listed is now complete (16 tools).
- **Then:** Stage 5 close-out. **PR50 (Langfuse chat tracing) + PR51 (Langfuse stream + embed tracing) DONE** — the full llm-gateway trace surface now exports to Langfuse: `/v1/chat` (`llm-gateway.chat`, full usage), `/v1/chat/stream` (`llm-gateway.chat.stream`, accumulated delta output, no usage — stream emits text only, model resolved from the channel), `/v1/embed` (`llm-gateway.embed`, input texts + usage + vectorCount/dimensions metadata). All off by default (`LANGFUSE_ENABLED=false`), fire-and-forget + soft-fail. **Remaining Stage 5 — BLOCKED on model access (owner: real models not available yet, 2026-06-12):** (i) golden tests on real models (roadmap §Risks: assert output *structure*, not text) — needs either paid API keys or a local Ollama; the `mock` provider can't exercise real model behaviour. When a model is available, the free path is `LLM_PROVIDER=openai-compatible` → local Ollama (provider ready since PR19). The real-Ollama opt-in E2E variant (PR20's flow against a Testcontainers Ollama on `@Tag("slow")`) folds into the same model-access unblock. **Next active work: Stage 3 (tasks/GTD) — owner-chosen, runs on `mock`, no model needed.**
- **Stage 3 (tasks/GTD) — CLOSED (PR52–PR64).** All scope from roadmap §Stage 3 + tasks.md §Skills shipped: schema + mcp-tasks (11 tools + 2 internal passthroughs) + tasks-agent (intent tool-routing + 2 intent skills + 1 trigger skill + weekly cron) + orchestrator catch-all routing. **`task-to-event` consciously deferred to Stage 4** (cross-agent = inter-agent agenda; `link_task_to_event` MCP tool already exists). **Next: Stage 4 — memory + inter-agent** (conversation-state layer, multi-agent orchestration, first cross-agent chain via event bus). Domain spec [tasks.md](tasks.md) owner-blessed 2026-06-12: **full GTD** (projects + contexts + waiting/scheduled/someday + weekly-review); **inbox routing = explicit-only** (tasks-agent routed via AGENT.md few-shots like calendar/finance; catch-all fallback deferred — see Deferred). **PR52 (Stage-3 spec opener: tasks.md + INDEX row) DONE. PR53 (030-tasks.yml schema + mcp-tasks CRUD starter) DONE** (see Done Stage 3). **PR54 (mcp-tasks GTD transitions: `clarify_task`/`update_task`/`complete_task`/`delete_task`/`link_task_to_event`) DONE** (see Done Stage 3; mcp-tasks tool surface now 9). **PR55 (tasks-agent skeleton: manifest + intent real, triggers stub) DONE** (registered in orchestrator as `tasks`). **PR56 (MCP-client wiring tasks-agent ↔ mcp-tasks) DONE** (mirrors finance PR33). **PR57 (`ToolDispatcher` + `InternalToolsController`) DONE** (mirrors finance PR34). **PR58 (LLM-driven tool routing in `IntentController` via `IntentRouter`) DONE** (see Done Stage 3 — mirrors finance PR35; user text now drives real mcp-tasks tool calls). **The tasks consumer-side stack is complete — a user message → orchestrator → tasks-agent → LLM router → mcp-tasks tool.** **PR59 (mcp-tasks `GET /internal/review` passthrough) DONE. PR60 (`weekly-review` SKILL.md + real `TriggerController`) DONE** (see Done Stage 3 — first tasks skill + proactive trigger flow, mirrors finance PR24a/PR10/PR27a). **PR61 (`enable_weekly_review`/`disable_weekly_review` mcp-tasks tools — per-household `weekly.review` cron in scheduler-service) DONE** (see Done Stage 3 — mirrors finance PR27b; no local schedule-id column, reconciles by `(household, kind=weekly.review)` against scheduler's list endpoint). **PR62 (`inbox-clarify` skill — reactive intent-driven GTD clarification) DONE** (see Done Stage 3; owner-chosen reactive intent-flow over proactive trigger — first intent-invoked skill in the system). **PR63 (`next-action-suggester` skill — reactive intent-driven next-action ranking) DONE** (see Done Stage 3; second intent skill, mirrors inbox-clarify; new mcp-tasks `GET /internal/tasks` filtered-list passthrough). **PR64 (orchestrator catch-all inbox routing — `tasks` as classifier fallback) DONE** (see Done Stage 3 — closes Stage 3). **`task-to-event` deferred to Stage 4** — it's cross-agent (task→event via orchestrator→calendar-agent), tasks.md marks it "(later)", and inter-agent interaction is the Stage-4 agenda; the `link_task_to_event` MCP tool already exists.

## Deferred work
- ~~**`receipt-parser` confirm-before-write**~~ — **DONE in PR68** (Stage 4 / A4): the conversation-state layer (A1–A3) landed and receipt-parser now shows the draft → writes only on "да". Owner-flagged debt cleared.
- **Real-Ollama opt-in E2E variant** — drive PR20's `E2EStage1ClosingFlowTest` flow against a Testcontainers Ollama instance behind a JUnit `@Tag("slow")` so CI doesn't pay the model-pull cost. Worth doing once we see a regression class the mock-LLM closer misses (timeouts / retries / JSON-shape drift). Until then the existing per-seam tests + the wire-contract closer keep the build honest.
- ~~**Tasks inbox catch-all routing**~~ — **DONE in PR64** (`orchestrator.catch-all-agent=tasks`; actionable-unmatched → tasks-inbox, echo reserved for small talk, deterministic fallbacks stay echo).
- ~~**`task-to-event` (task → calendar event)**~~ — **DONE across C1a–C1e (PR72–PR76)**: the agent-to-agent call path now exists end-to-end (tasks-agent → orchestrator `/v1/agents/invoke` → calendar-agent `create_event` → mcp-tasks `link_task_to_event`). One-direction MVP (task→event); event-done → task-done is still later. **Follow-up (new, smaller):** wire the user-facing trigger — auto-offer "поставить в календарь?" when an inbox-clarify proposal sets a hard `due_at` — currently the flow is reachable only via the internal `POST /agents/tasks/internal/task-to-event` endpoint.
- ~~**`receipt-parser` runs the `vision` channel inside finance-agent (the canonical "stuffed into one place" anti-pattern)**~~ — **DONE in MP-c (PR113 + PR114)**: the vision call now lives once in the shared `mcp-media-processing` capability (`caption` tool); `receipt-parser` calls it over the `/internal/caption` passthrough. Any future media-ingesting agent (docs/stylist/health) reuses the same capability.
- **Shared media-understanding capability (`mcp-media-processing`) — owner-prioritised AFTER D3 (2026-06-19), PR-sliced in [media.md](media.md). MP-a/b/d1/c DONE (PR110–114).** Media (photo/audio/video) can belong to ANY domain (receipt→finance, sick-note→health/docs, outfit→stylist), so *understanding* it (OCR / STT / vision-caption / video-frames) is a **shared capability-MCP** any agent binds. Lives at `shared/mcp/mcp-media-processing` (port 8097, no schema), reads bytes from media-service. **Reuse free OSS engines** (Tesseract for OCR, whisper for STT). Shipped: MP-a (scaffold + `ocr` stub) → MP-b (real Tesseract behind `OcrEngine`, Tess4J in-image) → MP-d1 (`caption` vision tool) → MP-c (bind finance-agent + migrate receipt-parser) → MP-d2a (`transcribe` STT tool on a native-free `StubSttEngine`, PR123) → **MP-d2b (real whisper via an ASR sidecar over HTTP behind `SttEngine`, PR124 — engine LOCKED = sidecar service).** **`mcp-media-processing` is now feature-complete (ocr + caption + transcribe); no remaining MP slices.** Next media consumer = a Stage-6 agent binding `transcribe` (voice notes) — that's where the `/internal/transcribe` passthrough lands (mirror MP-c). See memory `feedback-oss-reuse-shared-capabilities`.

## Done (Stage 0, merged)
End-to-end echo path: gateway-telegram → orchestrator → llm-gateway (mock) → reply. PRs 1–5 merged.

## Done (Stage 2)
- **PR49 — `/actuator/info` skill inventory (`SkillInfoContributor` in agent-runtime):** Optional follow-up to PR32 (option (b) from the loader-hardening discussion) — the **quiet observability lane** complementing PR32's loud-at-startup fail-fast. New `actuate/SkillInfoContributor implements InfoContributor` in `libs/agent-runtime` adds `skills.{count, names, triggers}` to `/actuator/info` (names + triggers sorted + de-duplicated; null-trigger tolerant) so a deploy smoke check can verify the live registry from outside the JVM without reading startup logs. Registered as a `@Bean` in `AgentRuntimeConfig`, so **both agents get it for free** (they `@Import(AgentRuntimeConfig.class)` and already expose `info` over web — `management.endpoints.web.exposure.include` already lists `info`, so no application.yml change). `libs/agent-runtime/pom.xml` gained `spring-boot-starter-actuator` (a dep both agents already pull transitively for `/actuator/health` — this only formalises it). 3 new `SkillInfoContributorTest` cases (sorted count/names/triggers; empty registry → 0 + empty lists; dedup + null-trigger tolerance across skills) — agent-runtime test count 5→8. finance-agent context still boots clean with the new bean (`ManifestControllerTest` green). Full reactor builds clean.
- **PR48 — `export_csv` tool in mcp-finance:** Last of the tools finance.md listed (`export_csv`); the mcp-finance tool surface is now complete (16 tools). `export_csv(householdId, accountId?, categoryId?, from?, to?)` reuses the `filter()` query (same optional filters as `list_transactions`) but with a higher `EXPORT_HARD_CAP` (10000) instead of the 200 interactive cap, ordered **oldest-first** (ledger style — `filter()` returns newest-first, so the rows are re-sorted in-memory by `ts`). Columns: `date,amount,currency,category,account,note,source` — account/category resolved to **names** (one `findByHouseholdId*` lookup each → `Map<UUID,String>`, not per-row queries). Hand-rolled **RFC-4180** field quoting (`csvField`: wrap in quotes + double internal quotes when a value holds a comma/quote/CR/LF) — consistent with mcp-money-pro-import keeping opencsv off the classpath. Truncation detected by reading `cap+1` rows and trimming, so a `truncated` flag is set without a second count query. New contracts `ExportCsvInput` + `CsvExportResult(csv, rowCount, truncated)`. Returns the CSV text for the agent to hand to Telegram as a file. Test: `exportCsvRendersOldestFirstWithResolvedNamesAndEscaping` — inserts two rows newest-first, asserts the export re-sorts oldest-first, an account name with a comma is quoted, a note with embedded quotes is doubled-and-wrapped, an uncategorised row has an empty category cell, and a category filter narrows the export. 31 mcp-finance tests green (was 30). Full reactor builds clean.
- **PR47 — `update_transaction` + `delete_transaction` tools in mcp-finance:** Completes the transaction-CRUD surface finance.md listed (only `add`/`list` existed before). **`update_transaction`** is a partial update: `id` required, every other field applied only when non-null (null = leave unchanged) — re-categorise or fix an amount, but can't clear an already-set field (documented limitation in `UpdateTransactionInput`). Immutable: `householdId` / `source` / `externalRef` / `createdAt` (moving a row across households or rewriting provenance isn't an "edit" and would break the idempotent-import unique). Moving to a different `accountId` is allowed only within the same household (same cross-household guard as `add_transaction`). **`delete_transaction(id)`** fetches → returns the deleted `FinTransactionDto` (so the agent can confirm / offer undo) → deletes; throws on unknown id. **Confirm-before-delete is the agent layer's job** (finance.md principle "confirm before delete/bulk") — the MCP tool itself is unconditional, matching the low-level contract. `FinTransaction` entity gained setters for the mutable fields only. New contract `UpdateTransactionInput`. Tool count 13→15. Tests: `updateTransactionAppliesNonNullFieldsAndGuardsCrossHousehold` (partial update changes amount/note/category, leaves currency/ts/source; cross-household account → reject; unknown id → "Transaction not found") + `deleteTransactionReturnsDeletedRowAndThrowsOnUnknown` (returns the row, list no longer shows it, second delete throws). 30 mcp-finance tests green (was 28). Full reactor builds clean.
- **PR46 — finance reporting matviews + `refresh_matviews` tool:** Closes Stage-2 finance goal (c). New migration [024-finance-matviews.yml](../infra/liquibase/features/024-finance-matviews.yml) creates two materialized views over `finance.*`: **`fin_mv_monthly_by_category`** — `(household_id, month, category_id, category_name, category_kind, currency, net_amount, spent, tx_count)` grouped by household/month(UTC)/category/currency; `spent` is the magnitude of outflow (`sum(CASE WHEN amount<0 THEN -amount ELSE 0 END)`) so an expense category reads positive while `net_amount` keeps the sign; category is **LEFT JOINed** so uncategorised transactions still roll up (category_* null). **`fin_mv_account_balance`** — `(account_id, household_id, account_name, currency, archived, balance, tx_count)` where `balance = opening_balance + COALESCE(Σ amount, 0)` (sign-aware, mirrors `get_balance` but precomputed for every account at once); LEFT JOIN so a zero-transaction account still shows its opening balance. Plain (non-unique) `household_id` indices for dashboard filtering. **Decision captured in the YAML:** NOT `REFRESH ... CONCURRENTLY` — that needs a unique index and a `coalesce` over the nullable `category_id`; a personal-finance dataset is small and a brief refresh lock is acceptable (revisit only if a household's volume makes the lock noticeable). **These matviews ARE the BI read surface** — Metabase/Grafana query them directly; they are deliberately NOT exposed as MCP read tools (finance.md: "Dashboards: Metabase/Grafana over finance.* + matviews"). New `refresh_matviews()` `@Tool` in `FinanceMcpTools` runs the two `REFRESH MATERIALIZED VIEW` statements via an injected `JdbcTemplate` (matview names are a fixed in-class `MATVIEWS` constant — never caller input, so the string-concatenated SQL is safe), returns the new `MatviewRefreshResult(List<String> refreshed, Instant refreshedAt)` contract. Tool count 12→13. `test-schema.sql` mirrors both matviews (`CREATE MATERIALIZED VIEW IF NOT EXISTS`). Test: `refreshMatviewsRecomputesBalanceAndMonthlyRollup` (own household for determinism — the shared test DB accumulates rows) seeds opening 100 + two expenses (-30, -20), refreshes, asserts `fin_mv_account_balance.balance=50` and `fin_mv_monthly_by_category` (March/coffee/EUR) `spent=50, tx_count=2`; then writes a third row and asserts the matview stays stale (50) until the next refresh flips it to 40 — proving snapshot semantics. 28 mcp-finance tests green (was 27). Full reactor builds clean. **Deferred:** scheduler-driven periodic refresh registration (no cron caller exists yet — `refresh_matviews` is manual/agent-triggered for now; a future PR can register a `matviews.refresh` cron the same way `set_budget` registers `budget.alert`).
- **PR45 — finance-agent CSV-attachment flow (Money Pro import, auto-create accounts):** Closes goal (b) end-to-end (Telegram CSV → media-service → import with auto-created accounts). `IntentController` gained a third branch: an `image` attachment → `ReceiptParser` (PR41), a `file` attachment → new `moneypro/MoneyProCsvImporter`, else the PR35 `IntentRouter`. `MoneyProCsvImporter` fetches the CSV bytes from media-service (reuses `MediaClient`), base64s them, and calls mcp-money-pro-import's `POST /internal/import` (PR44) via new `http/MoneyProImportClient` with `autoCreateAccounts=true` + empty `accountMap`, then reports counts: "Импортировал N транзакций (пропущено M дублей). Создал счета: …". **No interactive account mapping** — deferred with the conversation-state layer (owner decision); a one-time history import auto-creates accounts by name. Failures degrade to a friendly Russian message (no LLM on this path → `IntentResponse.llmModel` is null). New `finance-agent.mcp-money-pro-import-url` (reuses the existing `MCP_MONEY_PRO_IMPORT_URL` env that already feeds the MCP-SSE connection) + `moneyProImportWebClient` bean; added to both main + test application.yml. AGENT.md description refreshed (receipt photos + Money Pro import now real, not "later PR"). Test: `CsvImportFlowTest` (media + mcp-money-pro-import MockWebServers) drives `POST /agents/finance/intent` with a `file` attachment → asserts media fetched by storageUri, import POST body carries `householdId` + `autoCreateAccounts=true` + base64 CSV, reply reports "Импортировал 3 транзакций" + "Создал счета: Wallet, Card". finance-agent suite 31 tests green (was 30). Full 21-module reactor compiles + installs clean. **Goal (b) done; remaining Stage-2: (c) matviews once a dashboard caller exists.**
- **PR44 — mcp-money-pro-import `POST /internal/import` passthrough:** Third slice of goal (b) (split out so PR45's finance-agent flow stays tight). mcp-money-pro-import was MCP-only; the attachment-driven CSV import is a **deterministic** call (finance-agent already has the bytes + wants an auto-create import), so a plain HTTP passthrough is the right shape — no LLM-driven MCP tool selection, and it's MockWebServer-testable from finance-agent (the MCP/SSE path isn't). New `web/InternalImportController` — `POST /internal/import` (body `ImportMoneyProCsvInput`) → `ImportMoneyProCsvResult` (200) | 400, delegating to `MoneyProImporter.run`. Same reasoning + shape as mcp-finance's `POST /internal/transaction` (PR39); the MCP `@Tool` stays the entry point for any future LLM-chosen import. The module gained its first web layer (the `@SpringBootApplication` component-scan already covers `web/`). Test class switched to `WebEnvironment.RANDOM_PORT` (existing 5 tests drive `tools.*` directly, unaffected) + new `internalImportPassthroughRunsImport` (POST a one-row auto-create CSV → 200, `created==1`, `createdAccounts==["Petty"]`). 6 tests green. README updated (also corrected the now-stale "read-only `FinAccount`" note from PR43). Full 21-module reactor compiles + installs clean. **Next:** PR45 — finance-agent fetches the CSV bytes and calls this.
- **PR43 — `import_moneypro_csv` auto-creates accounts (`autoCreateAccounts`):** Second slice of goal (b). Until now the import required a complete `accountMap` (Money Pro account name → existing `fin_account.id`); unmapped names were row errors, an empty map fatal. New `autoCreateAccounts` flag: when set, a Money Pro account name not in `accountMap` **and** not matching an existing household account by name (case-insensitive) auto-creates a `fin_account` instead of erroring; `accountMap` may be empty in this mode. New accounts take the row's currency (else `USD` fallback) + default type `cash` (a CSV can't tell us the type — user re-types later). Existing accounts are reused by name so a re-import doesn't duplicate; an in-import name cache prevents dup-creation across rows. Created names come back in the result's new `createdAccounts` list (in dry-run, what *would* be created). Contracts: `ImportMoneyProCsvInput` gained `autoCreateAccounts` (8th component) + a **back-compat 7-arg constructor** so existing callers/tests compile unchanged; `ImportMoneyProCsvResult` gained `createdAccounts`. The mcp-money-pro-import `FinAccount` entity (was a read-only 3-column view for the cross-household guard) became **writable** — mapped `name` + `type` + a creating constructor; unmapped columns (`owner_id`, `opening_balance`, `archived`, `metadata`, `created_at`) keep their DB defaults on insert. `FinAccountRepository.findByHouseholdId` preloads existing names. Tool description + README updated. Test: `autoCreateAccountsCreatesMissingAndReusesExistingByName` (own household for isolation — absolute `moneypro_import` counters are shared across tests) asserts a new "Savings" account is created with the row's currency + `cash` type, an existing "Wallet" is reused (transaction points at the pre-seeded id, no duplicate), and `createdAccounts == ["Savings"]`. 5 mcp-money-pro-import tests green. Full 21-module reactor compiles + installs clean. **Next:** PR44 — finance-agent CSV-attachment flow calls this with `autoCreateAccounts=true`.
- **PR42 — gateway-telegram captures document/CSV uploads → media-service:** First slice of goal (b) (Money Pro CSV import). Generalises PR38's photo path: the `IncomingPhoto` record became `IncomingMedia(bytes, mimeType, filename, kind)` since photo and document follow the identical upload path — only the attachment `kind` differs (`image` for a Telegram photo → receipt flow, `file` for a document → CSV import). `AiLifeBot.consume` now also handles `msg.hasDocument()`: trusts the `Document`'s declared filename + MIME (octet-stream fallback), downloads the bytes via the shared `download(File)` helper (factored out of the photo path), and sets `kind = mime.startsWith("image/") ? "image" : "file"` (an image sent uncompressed as a document still routes to the receipt flow). `MessageProcessor.attachmentsFor` uploads with the media's `kind` and stamps the matching `Attachment` kind. **No orchestrator change** — attachments already pass through verbatim. Test: `MessageProcessorTest` gained `documentMessageUploadsToMediaAsFileKind` (CSV `text/csv` → media upload + `NormalizedMessage` carries a `file` attachment with the media id); the existing photo test switched to `IncomingMedia(...,"image")`. 4 gateway-telegram tests green. **Gotcha:** telegrambots 7.11 `Document` is `…meta.api.objects.Document`. Full 21-module reactor compiles + installs clean. **Next:** PR43 adds `autoCreateAccounts` to `import_moneypro_csv`.
- **PR41 — `receipt-parser` flow in finance-agent (receipt photo → transaction, write-immediately MVP):** Final slice — closes the `receipt-parser` feature end-to-end (Telegram photo → media-service → vision → transaction). `IntentController` now branches: a message carrying an `image` attachment takes the new `receipt/ReceiptParser` path; everything else stays on the PR35 `IntentRouter`. **Pipeline:** `MediaClient.fetch(storageUri)` pulls the photo bytes from media-service (PR37) → base64 → `LlmChatRequest` on the **`vision` channel** (PR36's `LlmMessage.userWithImages`) layering `[AGENT.md.body, receipt-parser SKILL.md, image]` → LLM returns a strict-JSON draft `{amount, currency, merchant, date}` → `AccountClient.list` (PR40) picks the **first non-archived** account → `TransactionClient.add` POSTs to mcp-finance's `/internal/transaction` (PR39). **Sign:** the stored amount is forced negative (expense) regardless of the LLM's sign. **categoryId left null** so mcp-finance's one-shot `transaction.uncategorised` hook fires the categorizer. `ownerId` null (household-shared) for the MVP. Currency/ts null → mcp-finance defaults (account currency / now). Reply: "Добавил: Starbucks -4.50 EUR (счёт «Main card»). Категорию предложу отдельно — поправьте, если неверно." **Lenient draft parsing** (strips markdown fences / leading prose; `{"error":...}` or no amount → "не распознал сумму"); every failure stage degrades to a friendly Russian message, never an exception. **No confirm-before-write** (owner decision — deferred, needs a conversation-state layer; recorded in Deferred work + AGENT.md). New `receipt-parser` SKILL.md (intent-invoked, empty `triggers`, declared in AGENT.md `skills:` so PR32's loader cross-check covers it). New `http/MediaClient` + `http/AccountClient`; `TransactionClient` gained `add()`; new `finance-agent.media-service-url` (`MEDIA_SERVICE_URL`) + `mediaServiceWebClient` bean (test + main application.yml both updated — the test yml REPLACES main per PR33). compose finance-agent gains `MEDIA_SERVICE_URL` + `depends_on: media-service`. Test: `ReceiptFlowTest` (3 MockWebServers — llm/media/mcp-finance) drives `POST /agents/finance/intent` with an image attachment and asserts: media fetched by storageUri, llm hit on `vision` with an image part, mcp-finance GET accounts → POST transaction with negative amount + source=telegram + note=merchant; reply contains "Добавил"/merchant/account. finance-agent suite 30 tests green (was 29). Full 21-module reactor compiles + installs clean.
- **PR40 — mcp-finance `GET /internal/accounts` list-passthrough:** Fifth slice of `receipt-parser` (tiny, mirrors PR39). The receipt flow needs a target `accountId` for `add_transaction` (the photo carries amount/merchant/date but not which account), so finance-agent must list the household's accounts and pick one — but there was no non-MCP read path for accounts. New `InternalAccountController` exposes `GET /internal/accounts?householdId=<uuid>` → `List<FinAccountDto>` (ordered by name), delegating to the `list_accounts` tool. Mirrors `InternalBudgetController`'s query-param shape. Test: `internalAccountsListReturnsHouseholdAccountsOnly` (lists own household's account, never another household's). Full reactor green. README updated. PR41 (receipt flow) will GET this, pick the first non-archived account as the MVP default.
- **PR39 — mcp-finance `POST /internal/transaction` add-passthrough:** Fourth slice of `receipt-parser` (split out of the old PR39 so the finance-agent receipt flow in PR40 stays a tight slice). Until now finance-agent could only *read* mcp-finance over its `/internal/*` passthroughs (Budget/Recurring/Transaction GETs) — there was **no write path** that doesn't go through an LLM-driven MCP tool call. `InternalTransactionController` gained `POST /internal/transaction` (body `AddTransactionInput`) that **delegates straight to `FinanceMcpTools.addTransaction`** (injected `@Component`), so every tool invariant applies identically: cross-household account guard, currency-defaults-to-account, and the uncategorised one-shot `transaction.uncategorised` trigger. `IllegalArgumentException` (e.g. cross-household account) → `{"error": "..."}` 400; success → `FinTransactionDto` 200. This is what PR40's `receipt-parser` calls once it has parsed a concrete draft from a photo — it already has the values, so an LLM round-trip to pick the tool would be pure overhead. Test: `McpFinanceIntegrationTest.internalAddTransactionPostPersistsRowAnd400OnBadInput` (POST → 200 + DTO + GET round-trip confirms the row; cross-household account → 400). Full reactor green. README updated. **No change to the MCP tool surface** — this is an HTTP passthrough only, consistent with the PR33 "mcp-finance stays on HTTP /internal" decision.
- **PR38 — gateway-telegram captures Telegram photos → media-service → `NormalizedMessage` image attachment:** Third slice of `receipt-parser` — the inbound entry path. `AiLifeBot.consume` now accepts **photo** messages (was text-only): picks the largest `PhotoSize`, resolves it via `GetFile`, streams the bytes (`downloadFileAsStream`), and hands them to `MessageProcessor` as a new `IncomingPhoto(bytes, mimeType, filename)` (mime defaults to `image/jpeg` — Telegram re-encodes to JPEG; `.png`/`.webp` honoured). For a photo message the caption becomes `text` (may be null). New `media/MediaServiceClient` does a multipart `POST /v1/media` to PR37's media-service and returns the `MediaObjectDto`; `MessageProcessor` then stamps an `Attachment("image", mimeType, storageUri=<media object id>, caption=null)` onto the `NormalizedMessage` (caption lives in `text`, so no duplication). **Not soft-failed** — for a photo the upload IS the payload, so a media-service failure surfaces as the bot's generic error reply rather than a silent drop. Orchestrator already forwards `attachments` verbatim to the agent (`RemoteAgent` ships the whole `NormalizedMessage`), so no orchestrator change. `IncomingMessage` gained a 7th `photo` component + a back-compat 6-arg constructor (photo=null) so the two existing text-flow tests compile unchanged and never touch media-service. New `gateway.services.media-base-url` (`MEDIA_SERVICE_URL`, default `http://media-service:8088`) + `mediaWebClient` bean (`.clone()`d); compose gateway-telegram gains the env + `depends_on: media-service`. Tests: `MessageProcessorTest` gained a third MockWebServer (media) + `photoMessageUploadsToMediaAndAttachesStorageUri` — asserts the multipart upload (householdId + source form fields) AND that the orchestrator body carries `text` = caption, `kind=image`, `storageUri` = the returned media id, `mimeType`; the two existing tests now also assert `media.getRequestCount()==0` for text-only. 3 gateway-telegram tests green; full 21-module reactor compiles + installs clean. **Gotcha captured:** in telegrambots 7.11 `PhotoSize` is `…meta.api.objects.PhotoSize`, NOT `…objects.photo.PhotoSize`. **Next:** PR39 — finance-agent's `receipt-parser` skill fetches the bytes back by id, runs the vision channel, drafts an `AddTransactionInput`, confirms, then `add_transaction`.
- **PR37 — media-service: central media catalogue (MinIO + `media` schema):** Second slice of `receipt-parser`, and the **foundational object-storage track** the owner chose to build now (MinIO-now over inline-base64) because every future media-ingesting agent (nutrition / stylist / researcher) will catalogue here. New platform service `platform/media-service` (port **8088**, servlet stack — the MinIO SDK is blocking, same reasoning as memory-service). Stores binary blobs (receipt photos, voice notes, docs, video) in **MinIO** (S3-compatible) and metadata in new `media.media_object` ([008-media.yml](../infra/liquibase/features/008-media.yml), uncommented from the reserved master-changelog slot): `(id, household_id, owner_id?, kind, mime_type, size_bytes, sha256, bucket, storage_key, source?, metadata, created_at)` with `ix_media_object_household` + `ix_media_object_sha256` (sha256 indexed for future dedup; **no `(household, sha256)` unique yet** — two identical uploads both succeed for now, captured in the YAML). REST surface: `POST /v1/media` (multipart `file` part + `householdId`/`ownerId?`/`kind?`/`source?` form fields → `MediaObjectDto`), `GET /v1/media/{id}` (raw bytes + stored content-type), `GET /v1/media/{id}/meta` (metadata JSON), `DELETE /v1/media/{id}` (204/404). Callers reference an object **only by `id`** — bucket/key layout never leaves the service (`MediaRow.toDto()` drops them); object key is `<householdId>/<objectId>` (clusters a household's blobs, UUID kills collisions). `kind` derives from the MIME top-type (`image`/`audio`/`video`/`file`) when the caller leaves it blank. `MediaService` flow: size-cap (`MEDIA_MAX_BYTES`, default 10 MiB) → sha256 → `ObjectStore.put` → row insert; `ObjectStore` is a single-bucket MinIO wrapper that creates the bucket on startup (idempotent) and collapses MinIO's broad checked-exception surface into one `ObjectStoreException`. **No auth** — internal-only by convention; fetch is household-agnostic (caller authorized upstream), documented in the README. New contract `media/MediaObjectDto`. New MinIO Java SDK dep (`io.minio:minio:8.5.14`, version-managed in root pom). `minio` added to the full `docker-compose.yml` (was dev-only) + a `media-service` service block; `.env.example` media-service block; infra/README port row (8088) + `media` schema owner row updated to "media-service (writes); gateway-telegram + agents (via media-service)". Tests: 4 `MediaServiceIntegrationTest` cases on Testcontainers **Postgres + MinIO** — upload → DB row + byte round-trip through MinIO → meta → delete → 404; non-image MIME → `kind=file`; oversized → 400 (cap dropped to 1 KiB in the test context); unknown id → 404. WebTestClient needs webflux, which a servlet app lacks → added `spring-boot-starter-webflux` (test scope only). Full 21-module reactor compiles + installs clean. **Next:** PR38 wires gateway-telegram to upload the Telegram photo here and stamp the `storageUri` onto the `NormalizedMessage` attachment.
- **PR36 — vision message content in llm-gateway (receipt-parser foundation):** First slice of the `receipt-parser` split — the bottom of the stack, fully testable in isolation, no open architectural questions. The `vision` channel + `LLM_VISION_MODEL` already existed since PR18/19 but no message could actually carry an image. New contract `llm/LlmImage(mediaType, dataBase64)` (base64 inline — gateway stays stateless, never fetches a URL). `LlmMessage` gained a 4th NON_NULL component `List<LlmImage> images` (normalised empty→null in the compact constructor so text-only messages serialise without an `images` key) + a **back-compat 3-arg constructor** so the existing `system()/user()/assistant()` factories and any direct callers compile unchanged + `userWithImages(text, images)` factory + `hasImages()`. Both real providers translate a multimodal turn into their native shape, keeping the plain-string form for text-only turns (back-compat with every existing provider test): **anthropic** → `content` array of blocks (optional leading `text` block, then one `{type:image, source:{type:base64, media_type, data}}` per attachment); **openai-compatible** → `content` array of parts (optional leading `text` part, then one `{type:image_url, image_url:{url:"data:<mt>;base64,<data>"}}` per attachment). **mock** echoes ` [images=N]` after the text so vision callers can assert deterministically. Tool/role handling unchanged (Anthropic still rejects TOOL, openai still rejects TOOL). Tests: one vision case added to each of `AnthropicProviderTest` / `OpenAiCompatibleProviderTest` / `MockProviderTest` (asserts the exact block/part shape + data-URI + image count); all existing provider + ChatController tests stay green (23 llm-gateway tests). Full 20-module reactor compiles + installs clean (contract change is back-compat across every consumer). README gained a **Vision channel** section with the wire shape + per-provider translation. **Next:** PR37 wires the multipart-image entry path (open Q: where the photo lives between draft & confirm).
- **PR35 — LLM-driven tool routing in `IntentController`:** Closes the user-facing consumer side of the MCP-client stack (PR33 wiring → PR34 dispatcher → PR35 LLM classifier). New `intent/IntentRouter` is the brain: when MCP tools are wired it builds a system prompt with `AGENT.md.body + Available tools (name + description + JSON Schema) + strict JSON output contract (action: tool|chat + name/args or text)`, ships it to llm-gateway alongside the user message, parses the response. **Tool path:** `dispatch(name, argsJson)` via `ToolDispatcher` on `Schedulers.boundedElastic` (same blocking-call pattern as `InternalToolsController`), tool's JSON result becomes the IntentResponse text. **Chat path:** structured `text` field — or, when the LLM returns plain prose instead of JSON, the raw body — propagates verbatim. **Lenient parsing**: non-JSON, missing `action`, dispatch failures all degrade to a user-facing string ("Не удалось вызвать инструмент «X»: <reason>") rather than bubbling exceptions. **Back-compat short-circuit:** when `availableToolDefinitions()` is empty (e.g. `spring.ai.mcp.client.enabled=false` in tests, or no MCP servers wired) the router falls through to the pre-PR35 single-system-prompt chat path — existing IntentControllerTest stays green without modification. New method `ToolDispatcher.availableToolDefinitions(): List<ToolDefinition>` exposes the full tool metadata (name + description + inputSchema) the router needs to brief the LLM; alphabetical sort keeps the prompt deterministic across restarts. `IntentController` is now a thin 3-line wrapper that delegates to the router and wraps the result in `IntentResponse(agent, text, llmModel)` — `llmModel` propagated from the routing-turn response so the orchestrator's intent test contract is preserved. New `RouterResult` record (text, invokedTool, llmModel) — `invokedTool` is informational metadata for future audit / multi-turn confirmation flows. Tests: 5 new `IntentRouterTest` cases (no-tools fallback, tool-dispatch happy path with exact args JSON, chat-action text propagates, non-JSON treated as chat reply, dispatch failure → friendly Russian-locale error). Existing `IntentControllerTest` stays green (back-compat path). finance-agent test count 29 (was 24). Full 20-module reactor green (3:46).
- **PR34 — `ToolDispatcher` + `InternalToolsController` (first consumer of PR33's MCP-client wiring):** Closes the consumer-side of PR33. New `tools/ToolDispatcher` is a pure dispatcher: take a tool name + JSON args, find the matching `ToolCallback` in the Spring AI `ToolCallbackProvider`, invoke it. The `ToolCallbackProvider` is wired via `ObjectProvider<>` so the dispatcher bean stays valid even when `spring.ai.mcp.client.enabled=false` (test-resources default for finance-agent) — in that mode `availableToolNames()` returns `[]` and `dispatch` throws a clear "MCP client is disabled — set spring.ai.mcp.client.enabled=true" error rather than NPE'ing on a missing bean. New `web/InternalToolsController` exposes the dispatcher as `GET /agents/finance/internal/tools` (list names) + `POST /agents/finance/internal/tools/{name}` (invoke with raw JSON body, response is the tool's JSON-stringified result verbatim — no envelope). Blocking `ToolCallback.call` is scheduled on `Schedulers.boundedElastic` so the WebFlux event loop stays free. Unknown tool name → 400 with `{"error":"..."}` and the list of known tools. **No auth** — admin-only by convention (not routed via orchestrator); intended for cron / deploy scripts / the future intent-flow PR that puts an LLM in front of the dispatcher. Tests: 5 new `ToolDispatcherTest` cases (dispatch happy-path with args round-trip + result passthrough; unknown tool → IllegalArgumentException with sorted known-names; alphabetical sort of availableToolNames; empty provider → empty list; MCP-disabled → clear error message). 3 new `InternalToolsControllerTest` cases (list returns names, POST passes args verbatim, unknown tool → 400 with error body). Existing Trigger / Manifest / Intent / McpClientWiring tests stay green. finance-agent tests now 12 + 2 + 1 + 1 + 5 + 3. Full reactor green.
- **PR33 — First real Spring AI MCP-client wiring (finance-agent ↔ mcp-money-pro-import via SSE):** Closes the PR23 dangler. Until now every agent talked to MCP servers only through their `/internal/*` HTTP passthroughs; this PR adds the actual Spring AI MCP-client transport (SSE / `spring-ai-starter-mcp-client-webflux`, BOM-managed via `spring-ai.version=1.0.8`) inside finance-agent. `application.yml` grew a `spring.ai.mcp.client.sse.connections.mcp-money-pro-import.url` block (default `http://mcp-money-pro-import:8094`, env-overridable via `MCP_MONEY_PRO_IMPORT_URL`); auto-config exposes a `ToolCallbackProvider` bean populated with the remote `@Tool` methods (`import_moneypro_csv`). **mcp-finance stays on its HTTP `/internal/*` passthroughs** — switching it to MCP would only pay off once a ChatClient is choosing tools, and that's a later PR; the explicit "don't migrate everything yet" decision is captured in the README. **Eager-connect caveat:** the Spring AI SDK dials each configured server at context refresh (per-server `request-timeout`, default 20s) — so a missing mcp-money-pro-import surfaces at agent boot, not on first import attempt (opinionated, consistent with PR32's fail-fast loader hardening). Toggle via `FINANCE_AGENT_MCP_CLIENT_ENABLED=false` in dev/degraded environments. Test scaffolding: `src/test/resources/application.yml` is now a **full mirror of `src/main/resources/application.yml`** (Spring Boot's test/resources `application.yml` REPLACES the main file rather than merging — captured in a comment so the next maintainer doesn't relearn it) with `spring.ai.mcp.client.enabled=false` so the 3 existing test classes don't pay the 20s connect cost. New `McpClientWiringTest` re-enables MCP with `initialized=false` (registers connections but doesn't dial) and asserts a `ToolCallbackProvider` bean is wired — enough to prove transport + config + auto-config without paying for a live SSE round-trip; that round-trip is the next PR's job. finance-agent tests now 12 + 2 + 1 + 1 (Trigger / Manifest / Intent / McpClientWiring). New env vars in infra/.env.example: `MCP_MONEY_PRO_IMPORT_URL` + `FINANCE_AGENT_MCP_CLIENT_ENABLED`. Full reactor green.
- **PR32 — Skill-loader hardening (fail-fast on declared-but-not-loaded SKILL.md):** Closes the silent-failure mode PR31 caught — a SnakeYAML parse error (the classic unquoted `<word>: <value>` mid-sentence in a description) made the loader log WARN and drop the whole SKILL.md from the registry; future trigger requests for that skill 404 forever until a test happens to exercise that exact kind. `libs/agent-runtime`'s `AgentRuntimeConfig.skillRegistry` now cross-checks the scanned set against `AgentManifest.skills()` and throws an `IllegalStateException` at bean creation if any declared name is missing — Spring Boot startup aborts loudly with a single line that names the missing skill(s), the loaded set, and any per-file parse failures captured during the scan. **Per-skill parse failures are accumulated** (was: only logged, lost to scroll-back) so the error message points the next maintainer at the exact `.../SKILL.md` that broke and what SnakeYAML complained about. Opt-out: an empty / missing `skills:` in AGENT.md skips the cross-check entirely (back-compat for any agent that doesn't want to declare its skills yet). Calendar-agent's AGENT.md backfilled (`skills: [birthday-greeter, gift-recommender]`) so it actually benefits from the hardening — calendar's `ManifestControllerTest` gained the matching assertion. `libs/agent-runtime` gains its first test module (5 cases): happy-path with one declared skill + one undeclared malformed sibling (must not throw); broken declared skill → throws with `[bad-skill]` + path + parse-failure detail + "Fix the SKILL.md" hint; empty declared list → opts out cleanly; ghost name (declared but not on classpath) → throws with the "name mismatch or skills-classpath pattern" branch of the hint; blank skills-classpath → empty registry. Test resources at `libs/agent-runtime/src/test/resources/test-skills/{good,bad}/SKILL.md`; `pom.xml` gained `spring-boot-starter-test` (test scope, JUnit5 + AssertJ transitively). calendar-agent + finance-agent tests stay green (12 + 12 + 2). Full reactor green.
- **PR31 — `transaction-categorizer` skill + one-shot `transaction.uncategorised` loop:** Closes the second finance skill. The SKILL.md, AGENT.md trigger/skill declarations, `TransactionClient`, `TriggerController.enrichTransactionUncategorised` branch, `InternalTransactionController`, `SchedulerClient.registerTransactionUncategorisedOneshot` + `McpFinanceProperties.Transaction` nested config, and the `add_transaction` hook itself had all been authored in the working tree before this PR; what this PR added is the wire-up validation (tests + READMEs + STATUS) and the YAML-frontmatter fix that made the skill actually loadable. Loop shape: `mcp-finance.add_transaction(... categoryId=null, source!='moneypro_import' ...)` → soft-fail POST `/v1/schedules` with `{kind: "transaction.uncategorised", runAt: now, payload: {transactionId}}` → scheduler tick → orchestrator → finance-agent's `transaction.uncategorised` trigger → enrichment via `GET /internal/transaction/{id}` → skill prompt sees `{amount, currency, note?, source?, ts?}` → notifier fan-out. 404 path stamps `status: no_active_transaction` (row vanished between schedule and tick) so the skill SKIPs. 5xx → 503 so scheduler retries. **moneypro_import is the only `source` that skips the hook** — bulk imports would otherwise flood the user with N suggestions; captured both in the tool description and the SKILL.md edge cases. **YAML gotcha worth keeping:** SKILL.md frontmatter values containing `<word>: <value>` mid-sentence (e.g. "Provenance: `manual`") are parsed by SnakeYAML as a nested key/value, fail with "mapping values are not allowed here", and the loader silently drops the whole skill — fixed by switching to "Provenance tag — `manual`". This is the second time we've shipped a SKILL.md that the loader rejected without anyone noticing until a test exercised the trigger; lifting the loader's WARN to an INFO + an `actuator/info` skill-count export would catch the next one faster, but that's deferred. Tests: finance-agent's `TriggerControllerTest` grew `transactionUncategorisedEnrichesPayloadFromMcpFinance` (enrichment + LLM body carries amount/currency/note/source + 2× notifier fan-out) and `transactionUncategorised404FromMcpFinanceMarksPayloadAndSkipsNotifier` (404 → no_active_transaction marker → SKIP → no notifier touch). 12 tests now (was 10). mcp-finance's `McpFinanceIntegrationTest` grew three tests: `addTransactionWithoutCategoryRegistersOneshotSchedule` asserts the POST body carries `kind=transaction.uncategorised`, `ownerAgent=finance`, the saved transactionId, AND `runAt` (one-shot, not cron); `addTransactionFromMoneyProImportDoesNotRegisterScheduler` asserts the hook is silent for `source=moneypro_import` even when `categoryId` is null; `internalTransactionReturnsRowAfterAddAnd404Otherwise` round-trips the new REST passthrough. 25 tests now (was 22). Full reactor green.
- **PR30 — `fin_recurring.next_due` post-tick advance:** Closes PR29b's known limitation. New `POST /internal/recurring/{id}/advance` on mcp-finance (`web/InternalRecurringController#advance`, `@Transactional`): looks up the row, recomputes `next_due` via the now-public `FinanceMcpTools.nextDueFromCron(cron, now)`, saves, returns the updated DTO. 404 when the row is gone — caller treats it as no-op. New `RecurringClient.advance(UUID)` in finance-agent (`POST … toBodilessEntity() … then()`). New `TriggerController.maybeAdvanceRecurring` hook chained **after** `runSkill` and before the 202 response: fires only for `kind=recurring.due`, **skipped when payload carries `status: no_active_recurring`** (the enrichment stamp from PR29a — advancing a deleted row is pointless), errors logged + swallowed because a stale column is cosmetic and the trigger already ran. Sequencing matters: advance runs after `dispatch` so a SKIP from the skill still advances the column (the tick happened, regardless of whether the user got a notification); errors don't poison the 202. Tests: mcp-finance gained one test asserting POST returns a DTO whose `next_due` is strictly after `Instant.now()` (we assert the invariant, not "post > pre" — daily crons land on the same boundary if you advance immediately, captured in test comment), plus the 404 path — now **22 tests**. finance-agent `TriggerControllerTest`'s recurring happy-path test now asserts a second mcp-finance hit lands on `/internal/recurring/{id}/advance` after the skill ran; the 404 test asserts **no** advance call happens because the payload was stamped. Existing 8 tests stayed green. Full reactor green.
- **PR29b — `upsert_recurring` auto-registers/deletes the `recurring.due` cron in scheduler-service:** Closes the Stage-2 recurring loop end-to-end. `McpFinanceProperties` gained a nested `Recurring` (`mcp-finance.recurring.{owner-agent, trigger-kind}`) — **no cron prop** because the cron is per-row (`fin_recurring.cron`), unlike budgets where every alert shares one. `SchedulerClient.registerRecurring(householdId, recurringId, cron)` is the new convenience: payload `{recurringId}` so finance-agent's PR29a enrichment can hydrate the rest at trigger time, cron passed explicitly by the caller. **Lifecycle reconciliation** in `FinanceMcpTools.upsertRecurring` (extracted helper `reconcileRecurringSchedule`): autoRemind=true + no schedule → register; autoRemind=false + had schedule → delete + null; autoRemind=true + had schedule + cron changed → register new + delete old in **register-first order** so a flaky scheduler can't leave us cron-less (same flake-safe ordering PR27b's `setBudget` uses; captured in code comments). same-state no-op when nothing changed. Soft-fail policy unchanged — a register failure returns null and the row is saved without a `schedule_id`, future reconciliation pass can wire the cron. **Known limitation called out in Next goal (c):** scheduler-service advances its own `next_run_ts` after each tick, but `fin_recurring.next_due` is recomputed only on `upsert_recurring` — so the column becomes a stale snapshot once the cron starts firing. A future `/internal/recurring/{id}/advance` REST or a "recompute next_due" job would close that loop; both are out of scope here. `application.yml` gained `mcp-finance.recurring.{owner-agent, trigger-kind}` reading the matching env vars. 4 new integration tests (21 total, was 17): autoRemind=true → POST with row's cron + payload + `scheduleId` propagated to DTO; autoRemind=false → no scheduler call at all; cron change while autoRemind=true → POST new + DELETE old in that order with different `scheduleId`s; flip autoRemind true→false → DELETE only + DTO's `scheduleId` is null. Existing 17 tests stayed green. Full reactor green.
- **PR29a — finance-agent `recurring.due` enrichment + `recurring-due` skill + mcp-finance `/internal/recurring/{id}` REST:** Direct mirror of PR27a, with the soft-fail enrichment wrapper generalised. New non-MCP REST passthrough `web/InternalRecurringController` in mcp-finance — `GET /internal/recurring/{id}` → `FinRecurringDto` (200) | 404; 5-line controller delegating to `FinRecurringRepository.findById`. New skill at [skills/finance/recurring-due/SKILL.md](../skills/finance/recurring-due/SKILL.md) — frontmatter triggers `recurring.due`, RU+EN bodies, edge cases for `no_active_recurring`, zero amount, missing currency. AGENT.md gained both the `recurring-due` skill entry and the `recurring.due` trigger declaration (mirrors PR24a's `budget.alert` shape). New `http/RecurringClient` in finance-agent (parallel to `BudgetStatusClient`; 200 → present, 404 → empty Optional, 5xx → propagated). `TriggerController` refactor: `enrichIfNeeded` now switches by kind, delegates to `enrichBudgetAlert` (existing logic) or new `enrichRecurringDue`. Both branches share `markStatus(payload, "no_active_*")` and `withPayload` helpers. **`BudgetEnrichmentFailedException` renamed to `EnrichmentFailedException`** with a `wrap(Throwable)` static so a third kind can plug in without re-deriving the wrapper (captured in code). The log line is now `"trigger enrichment failed (kind={}…)"` so the kind shows up alongside the schedule id. `buildRecallQuery` now also picks up the recurring row's `name` (anchors recall on "recurring.due for Apartment rent" instead of just the kind). Enriched payload shape: `{recurringId, name, amount, currency, nextDue?, note?}`; 404 → `markStatus(..., "no_active_recurring")` → skill SKIPs. Tests: mcp-finance grew by one round-trip test for `/internal/recurring/{id}` (404 → 200 after `upsert_recurring`) — now **17 tests**. finance-agent `TriggerControllerTest` gained two tests — enrichment happy path asserts mcp-finance was called, LLM body carries the enriched `name`/`amount`/`currency`/`nextDue`, and notifier fan-out reached both household members; 404 path asserts payload was stamped `no_active_recurring` and the LLM's `SKIP` short-circuited notifier — `TriggerControllerTest` now **10 tests** (was 8). `BudgetStatusDispatcher` extended to serve both `/internal/budget-status` and `/internal/recurring/*` endpoints behind per-endpoint flags. Existing 8 tests stayed green (back-compat — `EnrichmentFailedException` rename is internal). Full reactor green. **No scheduler-side cron registration yet** — that's PR29b; until then `recurring.due` runs only when a manual `POST /v1/schedules` references it.
- **PR28 — `fin_recurring` schema + `upsert_recurring` / `list_recurring` tools in mcp-finance:** New feature changelog [023-finance-recurring.yml](../infra/liquibase/features/023-finance-recurring.yml) creates `finance.fin_recurring` (id, household, owner?, account, category?, name, amount numeric(18,2), currency, cron varchar(64), next_due, note, auto_remind bool, schedule_id, metadata jsonb, created_at) with `ix_fin_recurring_household` and `ix_fin_recurring_next_due`. **cron over RRULE** captured in the YAML comment: scheduler-service already speaks Spring `CronExpression` (PR8), so an iCalendar parser just for the rare case where cron isn't expressive enough wasn't worth the dependency cost — the next maintainer should not relitigate. owner_id mirrors `fin_transaction` (null = shared household recurring, non-null = private); category_id nullable so "salary income" can live un-categorised until a user assigns one. Master changelog gains the include; test-schema.sql mirrors. Two new tools in `FinanceMcpTools`: **`upsert_recurring`** validates the cron via `CronExpression.isValidExpression`, recomputes `next_due` from `CronExpression.parse(cron).next(now)` on every upsert (so changing cadence takes effect immediately — captured in tool description), defaults `currency` to the account's, rejects cross-household account (mirrors `add_transaction`'s guard), stores `auto_remind` as a column-level flag (scheduler auto-registration follows in a later PR per next-goals). **`list_recurring`** filters by optional `accountId` / `categoryId` and orders by `next_due ASC NULLS LAST` so the agent sees what's due soonest. Uses the same `CAST(:p AS uuid)` pattern as the existing `filter()` queries — pgjdbc still can't infer a NULL bind type inside `(:p IS NULL OR col = :p)`. Period boundary helper (`nextDueFromCron`) anchors on `ZoneOffset.UTC` so stored `next_due` is comparable across household time zones; agent does any local-time presentation. New `FinRecurring` entity (with `scheduleId` field already in place for the future auto-registration PR), `FinRecurringRepository`. New contracts `FinRecurringDto` + `UpsertRecurringInput`. Tool count 10→12. Tests: 3 new integration tests — upsert creates with sane `next_due` then update preserves id and re-computes a later `next_due` for a daily cron; `list_recurring` filters by account and orders by `next_due ASC` (per-minute "soon" before daily "later"); cross-household account + malformed cron both rejected with clear messages. 16 mcp-finance tests pass (was 13). Full reactor green. **Matviews still deferred** — `fin_mv_monthly_by_category` / `fin_mv_account_balance` land when a dashboard caller exists. **autoRemind scheduler wiring** also deferred — PR-on-deck would call `SchedulerClient.register` from `upsert_recurring` when `autoRemind && schedule_id == null`, same shape as PR27b, payload `{recurringId}` so finance-agent's `TriggerController.enrichIfNeeded` can read the live row at trigger time.
- **PR27b — `set_budget` registers/deletes the recurring `budget.alert` schedule in scheduler-service:** Closes the Stage-2 budget loop end-to-end — cron → orchestrator → finance-agent → `/internal/budget-status` enrichment (PR27a) → skill → notifier — without any manual `POST /v1/schedules`. New feature changelog [022-fin-budget-schedule-id.yml](../infra/liquibase/features/022-fin-budget-schedule-id.yml) adds nullable `fin_budget.schedule_id uuid` (no FK on purpose — scheduler-service owns its own row lifecycle and a deleted schedule row should not cascade-corrupt a budget row; mirrors `calendar.ics_subscriptions.schedule_id` from PR13). Master changelog gains the include; test-schema.sql mirrors the column. New `config/McpFinanceProperties` (`mcp-finance.scheduler-url` + nested `mcp-finance.budget.{cron, owner-agent, trigger-kind}`) — mcp-finance had no `config/` package before, so `McpFinanceApplication` gained `@ConfigurationPropertiesScan`. New `config/HttpConfig` (`schedulerWebClient` bean, `.clone()`d from the shared builder — same leakage caveat as elsewhere). New `scheduler/SchedulerClient` — **synchronous** (block) because it's called from the `@Tool` execution path which is already non-reactive; shape duplicated from mcp-ics-import's `SchedulerClient`. Soft-fail policy captured in javadoc: register failure returns `null` so the budget row is still inserted (`schedule_id IS NULL`) and a future reconciliation pass can wire the cron; delete failure logged WARN and swallowed (404 = success, the row is gone either way). **Cron payload shape** — only `{categoryId, period}` (no pre-computed `spent`); finance-agent's `TriggerController.enrichIfNeeded` (PR27a) turns those two keys into the full skill payload at trigger time. Default cron `0 0 9 * * *` (UTC, daily 09:00) — short enough that month/week/year budgets all get a same-day signal; finance-agent recomputes the period window inside `get_budget_status`, so cron frequency and period are decoupled. **Replace flow ordering in `setBudget`** (captured in code comments): close prior active row → **register new schedule FIRST** → save new budget row with the returned `schedule_id` → **then delete old schedule**. That order matters — a scheduler outage mid-replace leaves the new active row cron-less but the old cron is still firing (less bad than both new + old being deleted). `FinBudget` gained `scheduleId` field + getter/setter; `FinBudgetDto` gained `scheduleId` (NON_NULL, omitted from JSON when null). `application.yml` gained the `mcp-finance.*` block reading `SCHEDULER_URL` / `MCP_FINANCE_BUDGET_*` env. Tests: `McpFinanceIntegrationTest` gains a `MockWebServer scheduler` started in a **static initializer** (not `@BeforeAll` — Spring resolves the `@DynamicPropertySource` supplier during context refresh, which runs BEFORE `@BeforeAll`, so the port must be bound at class-init time; captured in a comment). New `SchedulerDispatcher` (200 + ScheduleDto echo on POST, 204 on DELETE, 500 when `simulate5xx`) + `@BeforeEach` resetting it; **existing `setBudgetUpsert…` test extended** to assert (a) first set_budget POSTs `/v1/schedules` with cron + ownerAgent + kind + categoryId + period in the body, (b) replace flow POSTs new + DELETEs old in the documented order, (c) returned `scheduleId` is propagated through the DTO. **New test `setBudgetSurvivesSchedulerOutageAndSavesRowWithoutScheduleId`** flips `simulate5xx` and asserts the tool does NOT throw, the budget row is still saved, and its `scheduleId` is null. New okhttp `mockwebserver` dependency added to `mcp-finance/pom.xml` (was missing — required for the scheduler stub). 13 mcp-finance tests pass (was 12). Full reactor green.
- **PR27a — finance-agent enriches `budget.alert` payload from live mcp-finance numbers:** New non-MCP REST passthrough in mcp-finance — `GET /internal/budget-status?householdId=&categoryId=&period=` (`web/InternalBudgetController`) — wraps `getBudgetStatus` tool; mirrors mcp-ics-import's `/internal/pull/{subscriptionId}` pattern (PR13). 200 → `BudgetStatusResult`, 404 → no active budget (single `IllegalArgumentException` from the tool is collapsed there). New `http/BudgetStatusClient` in finance-agent (kept local, not yet in `libs/agent-runtime` — second consumer threshold not reached). **Differentiated error policy** captured in the client javadoc: 200 → `Optional.of(result)`; 404 → `Optional.empty()` ("no active budget — skill emits SKIP, not retry"); 5xx / network / 2s timeout → propagated error, controller maps to **503** so scheduler-service retries on the next tick. Why per-status: PR13's `IcsImportClient` could collapse everything because `ics.pull` is fire-and-forget; budget alerts must distinguish "no budget" from "mcp-finance down" since those have opposite downstream consequences. `FinanceAgentProperties` gained `mcp-finance-url` + matching `MCP_FINANCE_URL` env (default `http://mcp-finance:8092`); `OutboundHttpConfig` gained a fourth `.clone()`d `WebClient` bean. `TriggerController.enrichIfNeeded`: only fires for `kind=budget.alert` AND payload lacks `spent` AND has both `categoryId` (parseable UUID) + non-blank `period`. Already-enriched payloads (manual callers, PR24a's test fixtures with `{categoryName,limit,spent,...}`) skip the lookup entirely → **back-compat with all four PR24a-era tests**. Enriched payload shape: `{categoryId, categoryName, limit, spent, currency, period, ratio?}` (ratio omitted when null). When mcp-finance returns 404 the payload is patched with `status: no_active_budget` so the skill SKIPs cleanly (added an explicit Edge case for this in [skills/finance/budget-alerts/SKILL.md](../skills/finance/budget-alerts/SKILL.md)). **Implementation note (Reactor + Java type inference):** the obvious `Mono.just(ResponseEntity.<Void>build())` form fails to infer when composed in a single chain — promoting the pipeline `Mono<ResponseEntity<Void>>` to a local variable before `.onErrorResume` is the working shape (captured here so the next maintainer doesn't relearn it). Tests: mcp-finance `McpFinanceIntegrationTest` switched to `WebEnvironment.RANDOM_PORT` (existing 11 tests still drive `tools.*` directly, so the switch is transparent — and the new test asserts /internal/budget-status round-trip including 404 → 200 transition after `setBudget` + an in-window transaction). finance-agent `TriggerControllerTest` grew a fifth MockWebServer (`mcpFinance`) + `BudgetStatusDispatcher` with `snapshot` / `return404` / `simulate5xx` flags; three new tests: scheduler-shape payload `{categoryId,period}` triggers `/internal/budget-status` call + LLM body now carries the enriched numbers + downstream fan-out still happens; 404 from mcp-finance marks payload `no_active_budget` + skill SKIPs without notifier touch; 5xx returns **503** + LLM/profile/notifier untouched. Existing 5 tests still green (back-compat). `mcp-finance` test count 11→12; `finance-agent` test count 5→8. Full reactor green. **No scheduler-side registration yet** — that's PR27b; until then a manual `POST /v1/schedules` against scheduler-service drives the loop.
- **PR26 — `fin_budget` schema + `set_budget` / `get_budget_status` / `spending_by_category` tools in mcp-finance:** New feature changelog [021-fin-budget.yml](../infra/liquibase/features/021-fin-budget.yml) creates `finance.fin_budget` (id, household_id, category_id, period, limit_amount numeric(18,2), currency, valid_from, valid_to, metadata jsonb, created_at) with a **partial unique index** `uq_fin_budget_active(household_id, category_id, period) WHERE valid_to IS NULL` so at most one currently-active budget can exist per slot — historical rows (valid_to NOT NULL) are exempt so set_budget keeps a full audit trail. Master changelog reordered: 021 loads right after 020. Period is varchar(16) (enum-like) with the actual `month|week|year` set enforced at the tool layer to keep migrations cheap when a fourth period lands. Three new tools in `tools/FinanceMcpTools`: **`set_budget`** upserts the active row — looks up the active row via `FinBudgetRepository.findActive`, calls `saveAndFlush` to close it (`valid_to=now()`) **before** inserting the fresh row, otherwise Hibernate may reorder UPDATE/INSERT and the partial unique index fires (captured in a comment); cross-household category rejected (mirrors `add_transaction`'s account guard). **`get_budget_status`** computes the current UTC period window (`month` = first day of month at 00:00Z, `week` = ISO Monday 00:00Z, `year` = Jan 1 00:00Z; end = next period start, half-open), pulls signed sum via new `FinTransactionRepository.sumAmountByCategoryInWindow`, returns `spent = abs(signedSum)` so refunds (positive on expense) reduce it, and `ratio = spent / limit` (null when limit is 0 to avoid divide-by-zero on the wire); throws `IllegalArgumentException` when no active budget exists for the slot (no separate 404 type — fits the existing tool error contract). **`spending_by_category`** aggregates by `(category, currency)` over `[from, to)` via new native-SQL `FinTransactionRepository.spendingByCategory`; result is `List<Object[]>{categoryId, name, currency, signedSum, txCount}` mapped to `SpendingByCategoryRow` with `spent = abs(signedSum)`; `kind` filter null → defaults to `expense` (the common LLM-caller case), empty string → all kinds, otherwise literal match; `to` must be strictly after `from`. Uses the same `CAST(:p AS varchar)` pattern as the existing `filter()` query because pgjdbc cannot infer a NULL bind type inside `(:p IS NULL OR col = :p)` — captured in repository javadoc. New contracts in `libs/contracts/finance/`: `FinBudgetDto`, `SetBudgetInput`, `BudgetStatusResult`, `SpendingByCategoryInput`, `SpendingByCategoryRow`. Tool count now 10 (was 7). Test-schema.sql mirrors the new table + partial unique index. 5 new integration tests added (11 total): set_budget upsert closes prior + only one active row remains (+ historical row count = 1), get_budget_status computes spent inside current month window (handles in-window + refund + out-of-window), get_budget_status throws when no active budget, set_budget rejects cross-household category and unsupported period in one combined test, spending_by_category aggregates ordering + default-to-expense + explicit income kind. All 11 mcp-finance tests green; full reactor green. **No scheduler wiring in this PR** — the budget-alert recurring registration is the next slice, captured in "Next PRs". Token economy on tool descriptions kept under control — three new `@Tool` descriptions stay terse but cover sign convention, period boundaries, and the cross-household guard.
- **PR25b — `libs/agent-runtime` lift of `ProfileClient` / `NotifierClient` / `MemoryClient`:** Three shared HTTP clients move under `dev.fedorov.ailife.agentruntime.http`; both agents drop their `http/` packages (except calendar's `http/IcsImportClient` which is calendar-only). `agent-runtime/pom.xml` gained `org.springframework:spring-webflux` (the clients use `WebClient` + reactor types) — no Spring Boot auto-config involved, the beans are registered explicitly by `AgentRuntimeConfig`. The `WebClient` beans themselves stay agent-side (`profileServiceWebClient` / `notifierWebClient` / `memoryServiceWebClient` in each agent's `OutboundHttpConfig`) because they bind base URLs from per-agent properties; the shared clients pick them up by `@Qualifier` name. `memory-recall-k` moved from `calendar-agent.*` / `finance-agent.*` to the shared `agent.memory-recall-k` so a single `AgentRuntimeProperties.getMemoryRecallK()` powers both — `CalendarAgentProperties` / `FinanceAgentProperties` no longer carry it. **ProfileClient`.personById`** is kept on the shared client even though finance has no people domain — single method, zero runtime cost, ready when the next person-aware agent ships. All existing tests (calendar 10 + finance 8) green without changes — the qualifier-driven wiring is transparent to test code. Full 20-module reactor green.
- **PR25a — `libs/agent-runtime` lift of AGENT.md / SKILL.md loaders + SkillRegistry:** New shared module `libs/agent-runtime` (depends only on `contracts` + `spring-boot-starter` + `snakeyaml`). Pure-Java parsers (`manifest/ManifestParser`, `skill/SkillParser`) hold the regex + SnakeYAML logic; `skill/{Skill, SkillRegistry}` records moved verbatim; `config/AgentRuntimeConfig` (`@Configuration` + `@EnableConfigurationProperties`) wires the parsed `AgentManifest` + `SkillRegistry` as beans driven by `config/AgentRuntimeProperties` (`agent.{manifest-classpath, skills-classpath}`). Agents opt in with `@Import(AgentRuntimeConfig.class)` on the main class — the runtime's package sits outside their auto-scan root so `@ComponentScan` wouldn't pick it up. **calendar-agent and finance-agent both deleted their local `manifest/` + `skill/` packages** (4 classes each — `ManifestLoader`, `Skill`, `SkillLoader`, `SkillRegistry` — byte-identical between the two before this PR, modulo package + property prefix). Both `pom.xml`s dropped their `snakeyaml` dependency (now transitive through agent-runtime) and added the new lib. Both `application.yml`s replaced `<agent>-agent.manifest-classpath` with the shared `agent.{manifest-classpath, skills-classpath}` keys; `skills-classpath` is set per agent (`classpath*:skills/calendar/*/SKILL.md` vs `classpath*:skills/finance/*/SKILL.md`) — the default in `AgentRuntimeProperties` is the empty string, which makes an unconfigured agent silent rather than accidentally cross-loading another domain's skills. `TriggerController` import lines flipped to the shared package; all existing tests (calendar's 8 + finance's 8) still green without further changes. New 20-module reactor; full reactor green.
- **PR24c — memory enrichment in finance-agent (recall before LLM, soft-fail downtime):** Mirror of calendar-agent's PR17. `FinanceAgentProperties` gained `memory-service-url` + `memory-recall-k` (default `5`); `OutboundHttpConfig` gained a third `WebClient` bean (`.clone()`d). New `http/MemoryClient` — third codebase consumer of memory-service (after orchestrator PR15 + calendar-agent PR17); shape mirrors calendar's verbatim so the inevitable lift to `libs/memory-client` is mechanical. Strict no-throw: 500 ms timeout + any error (network / 5xx / missing required field) → `Mono.just(List.of())`. Recall query is anchored on `payload.categoryName` (`"budget.alert for Кофе"`) when present, falls back to bare kind otherwise — `relations` not pulled because the budget-alert payload has no `personId`. `TriggerController.runSkill` now zips `memory.recall` before the LLM call; non-empty results are injected into the user message under `memories[]` (same key calendar uses, JSON-tree from `RecallMemoryHit`). SKIP / no-householdId short-circuits stay where they were — memory recall still ran but didn't change outcome. `application.yml` gained `finance-agent.{memory-service-url, memory-recall-k}` reading `MEMORY_SERVICE_URL` / `FINANCE_AGENT_MEMORY_RECALL_K`. `.env.example` gained the same. `TriggerControllerTest` reworked: fourth MockWebServer + `MemoryDispatcher` with `volatile recallHits` + `simulate5xx` flags (same pattern calendar's PR17 test uses). 5 tests now (one extra): overshoot test asserts recall query body contains `budget.alert for Кофе` AND the recalled memory text appears in the LLM body under `memories`; new `memoryServiceFailureStillRunsSkillWithoutMemoriesBlock` test flips `simulate5xx`, asserts memory-service was called + 5xx swallowed + LLM body lacks `memories` + profile/notifier still get the alert. `@BeforeEach` drains all four queues + resets the memory dispatcher to avoid context-reuse pollution. Full 19-module reactor green.
- **PR24b — finance-agent fans the generated budget alert to every household member:** Mirrors calendar-agent's PR10. New `config/FinanceAgentProperties` (`finance-agent.{profile-service-url, notifier-url}`) + `config/OutboundHttpConfig` — one `WebClient` per dependency, `.clone()`d off the shared builder to avoid the well-known base-URL leakage bug (same fix calendar-agent + orchestrator already use). `FinanceAgentApplication` gains `@EnableConfigurationProperties(FinanceAgentProperties.class)`. Two new outbound clients in `http/`: `ProfileClient.usersByHousehold(UUID) → Flux<UserDto>` (empty stream when household has no members or is unknown) and `NotifierClient.notify(userId, text) → Mono<Void>` (`POST /v1/notify`). `TriggerController` now: post-LLM, if the response is not `SKIP` and `householdId` is present, calls `profile.usersByHousehold` and chains `notifier.notify` per user; per-user `onErrorResume` so a 5xx for one user doesn't stop the others (logged at WARN, the overall response is still 202). `SKIP`-path stays short-circuited **before** profile-service is touched — uneventful budgets cost one LLM round-trip and nothing else. Recipient policy: every household member, no role filter (same as calendar's PR10 decision — per-user privacy is a higher-layer concern when we have one). `application.yml` gained `finance-agent.profile-service-url` / `finance-agent.notifier-url` reading `PROFILE_SERVICE_URL` / `NOTIFIER_URL` (env vars already exported by gateway-telegram and calendar-agent — same names, no duplication). `.env.example` gained a `# === finance-agent ===` block with `FINANCE_AGENT_PORT` / `FINANCE_AGENT_URL` + a pointer note that the URL vars are shared. `TriggerControllerTest` reworked: three MockWebServers (LLM + profile + notifier) with `Dispatcher`-driven profile (always returns Alice + Bob) and notifier (always 202; `SelectiveFailingNotifierDispatcher` flips Alice to 500 for the partial-failure test). 4 tests: unknown-kind 404 with no downstream calls, overshoot → assertions on the LLM request + the profile lookup + per-user notify body containing Alice + Bob, SKIP → profile + notifier silent, single-user notify failure → second user still gets the message + controller still 202s. Full 19-module reactor green.
- **PR24a — first finance skill (`budget-alerts`) + real LLM-only TriggerController in finance-agent:** Mirrors calendar-agent's PR9c2 (LLM-only, log the result; notifier fan-out is PR24b). New SKILL.md at [skills/finance/budget-alerts/SKILL.md](../skills/finance/budget-alerts/SKILL.md) — frontmatter (`triggers: [budget.alert]`, RU + EN languages) + EN body with explicit ratio thresholds (< 0.8 → emit literal `SKIP`; 0.8 ≤ ratio < 1.0 → soft heads-up; ratio ≥ 1.0 → factual overshoot, not scolding). Wake payload shape: `{categoryName, limit, spent, currency, period}` — agent does not look anything up from the DB in this PR; the scheduler is expected to send pre-computed values (real `get_budget_status` / `spending_by_category` tools in mcp-finance + scheduler-side recurring registration follow when `fin_budget` schema lands). AGENT.md grew a `triggers:` list of maps (`{kind, description}` shape — same as calendar-agent's; `Expected map, got: budget.alert` was the parser error that caught the first attempt at a bare-string list and is captured as a comment-in-test for future maintainers). TriggerController moved from the PR22 stub to the real implementation: lookup skill in `SkillRegistry`, call llm-gateway with `[manifest.body, skill.body]` as system prompts and `{"payload": <wake payload>}` as the user message JSON. **`SKIP` sentinel handling** — the controller recognises the literal text (case-insensitive trim) and stays silent without forwarding to a notifier (which doesn't exist yet). 3 tests: unknown-kind 404 without touching LLM, real `budget.alert` flow asserts AGENT.md + SKILL.md body fragments + payload fields land in the LLM request body, SKIP-path still returns 202. **`@BeforeEach` drain** of any queued MockWebServer requests added — SpringBootTest reuses the context across methods, so leftover takes from a previous method would otherwise pollute the counters (captured PR15's pattern). ManifestControllerTest extended for the new trigger/skill entries (`triggers` is `List<Map<String,String>>` now, not empty). Full 19-module reactor green.
- **PR23 — mcp-money-pro-import (one-shot CSV import, dry-run, idempotent re-import):** New module `mcp/mcp-money-pro-import` (port **8094**, Spring AI MCP webflux + JPA servlet stack — same shape as mcp-finance). Single `@Tool import_moneypro_csv(householdId, csvBase64, encoding?, accountMap, categoryMap?, fileRef?, dryRun?)`. **No new migration** — reuses `finance.fin_transaction` / `finance.fin_account` from PR21's [020-finance.yml](../infra/liquibase/features/020-finance.yml); the `(household, source, external_ref)` unique was already provisioned for exactly this. Schema is owned by mcp-finance — this module is a second JPA view (mirrors mcp-ics-import's `ExternalEvent` pattern). **Encoding autodetect:** strict UTF-8 decode (BOM-stripped), CP-1251 fallback for older Russian Money Pro builds; pass `encoding` to override. **Delimiter autodetect** on the header line — vote between `;` / `,` / `\t`, tie-breaker `;` over `,`. RU + EN header aliases (`Дата` / `Date`, `Счёт` / `Account`, `Сумма` / `Amount`, `Описание` / `Description`, `Категория` / `Category`, `ID` / `Идентификатор`). Required columns: date / account / amount; rest optional. **External ref strategy:** column value when an `ID` column is present, else `sha1(date|account|amount|note)` so re-exports of the same Money Pro DB hit the same key. **Cross-household guard runs first:** every UUID in `accountMap` is verified to belong to `householdId` before any row touches the DB — a single mismatch is a fatal `IllegalArgumentException`, not a row error. Per-row errors (unknown account, unparseable date/amount) are collected into `errors[]` (capped at 20) and don't abort the loop. **Idempotency** is the DB unique on `(household, source='moneypro_import', external_ref)` — the importer also probes existence client-side so `skipped` is meaningful; race-window between probe and insert is acceptable for a single-threaded CSV import. Dry-run reports counts without writing. Amount parser handles US / EU decimal conventions (rightmost of `.` / `,` is the decimal mark; the other is a thousands sep that gets stripped — also strips spaces / NBSP / `'`). Date parser tries `dd.MM.yyyy [HH:mm[:ss]]`, ISO, US slashes; date-only values land at UTC midnight. Hand-rolled CSV splitter (doubled-quote escape only — multi-line quoted fields unsupported; Money Pro doesn't produce them) keeps opencsv off the classpath. New contracts `ImportMoneyProCsvInput` / `ImportMoneyProCsvResult` / `ImportRowError`. New env `MCP_MONEY_PRO_IMPORT_PORT` / `MCP_MONEY_PRO_IMPORT_DB_*`. 4 testcontainers tests cover happy path + idempotent re-import, dry-run no-write, RU CP-1251 + semicolons + unknown account row error, and cross-household accountMap rejection. Full 19-module reactor green. Pre-existing infra/.env.example drift past mcp-caldav (no mcp-finance / mcp-ics-import / memory-service / finance-agent entries) noted but **not** fixed in this PR — out of slice.
- **PR22 — finance-agent skeleton (manifest + intent real, triggers stub):** Mirrors PR9b/9c1 for calendar. New module `agents/finance-agent` (port **8093**, reactive webflux). `AGENT.md` declares `name: finance`, `mcp: [mcp-finance]`, four intent few-shots (record expense, aggregate by category, account balance, set category budget), no triggers yet (triggers section reserved for `budget.alert`, `recurring.due` etc. once skills ship). pom.xml `<resources>` copies `AGENT.md` from module root + `skills/finance/` from repo root (directory doesn't exist yet — Maven tolerates a missing source dir when no `<filtering>` is set). `manifest/ManifestLoader` + `skill/{Skill,SkillLoader,SkillRegistry}` duplicated from calendar-agent verbatim — when a third agent ships, lift to `libs/agent-runtime`; flagged in README. Three real endpoints: `GET /agents/finance/manifest` (parsed AGENT.md), `POST /agents/finance/intent` (LLM call with AGENT.md body as system prompt + user text, on FAST channel default), `POST /agents/finance/triggers/{kind}` (consults `SkillRegistry`, **always 404 in this PR** — no skills shipped; first skill PR replaces this with the real LLM + notifier flow following PR9c2/PR10/PR17). No outbound clients yet (profile/notifier/memory) — they land with the first skill. Orchestrator gains `{name: finance, base-url: ${FINANCE_AGENT_URL:http://finance-agent:8093}}` alongside `calendar`; soft-fail discovery handles the "finance-agent not running yet" case at startup. 4 new tests (`ManifestControllerTest` × 2 + `IntentControllerTest` + `TriggerControllerTest`); existing orchestrator tests still green — `DynamicPropertySource` overriding `agents[0]` replaces the bound list rather than merging, so per-test single-agent setups don't pick up the new finance registration. Full 18-module reactor green.
- **PR21 — finance schema 020 + mcp-finance (CRUD starter):** Stage 2 opener. Split from the originally-bundled PR21 ("schema + mcp-finance + Money Pro import + finance-agent" — too big for one slice; finance-agent → PR22, mcp-money-pro-import → PR23). New feature changelog [020-finance.yml](../infra/liquibase/features/020-finance.yml) creates three tables in the existing `finance` schema: `fin_account` (id, household_id, owner_id?, name, type, currency, opening_balance numeric(18,2), archived, metadata jsonb), `fin_category` (id, household_id, parent_id self-ref, name, kind, icon, metadata jsonb; unique on (household, name, kind)), `fin_transaction` (id, household_id, account_id, category_id?, owner_id?, amount numeric(18,2), currency, ts, note, source default 'manual', external_ref?, metadata jsonb; unique on (household, source, external_ref) for idempotent re-import; indices on (account_id, ts DESC), (household_id, ts DESC), category_id). `fin_budget` / `fin_recurring` / matviews deferred — they land with their tools. Master changelog reordered: 020-finance loads before memory schemas. New `mcp/mcp-finance` module (port **8092**, Spring AI MCP webflux, JPA + servlet stack). 7 `@Tool` methods in `tools/FinanceMcpTools`: `upsertAccount`, `listAccounts`, `upsertCategory`, `listCategories`, `addTransaction`, `listTransactions`, `getBalance`. Sign convention (expense<0, income>0) documented in tool descriptions but **not enforced at DB or MCP layer** — the agent layer owns sign discipline; the MCP stores what it's given so manual corrections / refunds are trivial. Cross-household guard: `addTransaction` rejects an `accountId` whose `householdId` doesn't match the input — only invariant enforced here, everything else relies on DB constraints. `listTransactions` filter uses **native SQL with explicit CAST(:p AS uuid/timestamptz)** because pgjdbc cannot infer the type of a NULL bound parameter inside `(:p IS NULL OR col = :p)` — JPQL form failed with "could not determine data type of parameter $6"; captured in repository javadoc so the next maintainer doesn't relearn it. Hard cap of 200 on list results, default 50. New contract DTOs in `libs/contracts/finance/`: `FinAccountDto`, `FinCategoryDto`, `FinTransactionDto`, `UpsertAccountInput`, `UpsertCategoryInput`, `AddTransactionInput`, `ListTransactionsInput`, `BalanceResult`. New env: `MCP_FINANCE_PORT` / `MCP_FINANCE_DB_URL` / `MCP_FINANCE_DB_USER` / `MCP_FINANCE_DB_PASSWORD`. 6 testcontainers integration tests cover upsert-create-then-update, unique-constraint enforcement, list-filtering by account/category/time-range, opening-balance + signed-sum in `getBalance`, cross-household account rejection, list-scope no-cross-leak. Tests aren't isolated across methods (shared SpringBootTest context + DB) — assertions scope on per-test accounts to stay deterministic; captured in test javadoc. Full 17-module reactor green.

## Done (Stage 3)
- **E2E discipline + backfilled closers (Stage 1/2/3 + inbound):** CLAUDE.md §Test strategy now **mandates an end-to-end test per stage closer / cross-service wire change** (shape: one real Spring context + forwarding MockWebServers, asserting `libs/contracts` DTOs survive each hop — the monorepo's fat-jar packaging blocks an all-real-services module). Before this only Stage 1 had a closer; now: **proactive wake path** — `E2EStage1ClosingFlowTest` (calendar `birthday.greet`), `E2EStage2FinanceWakeFlowTest` (finance `budget.alert`, pins the `{categoryId,period}` payload), `E2EStage3TasksWakeFlowTest` (tasks `weekly.review`, pins PR61's null-payload→`{}` so `householdId` rides the row) — all scheduler → orchestrator → agent. **Inbound path** — `E2EInboundClarifyFlowTest` (tasks-agent, real context): user message → `IntentController`→`IntentRouter`→`inbox-clarify`/`InboxClarifier`→ mcp-tasks `/internal/review` → proposal `IntentResponse`. **Coverage: Stage 1/2/3 ✅** (wake + inbound). **Stage 4** — the intended cross-agent chain isn't built yet (memory pieces are integration/composition-tested); future inter-agent chains get closers per the rule. **Stage 5** — real-model E2E blocked on model access (the deferred Real-Ollama variant).
- **PR64 — orchestrator catch-all inbox routing (`tasks` as classifier fallback — closes Stage 3):** Closes the roadmap's Stage-3 "anything not calendar/finance → tasks-inbox" (the long-deferred global-routing change). New configurable `orchestrator.catch-all-agent` (`AgentRegistryProperties.catchAllAgent`, env `ORCHESTRATOR_CATCH_ALL_AGENT`, default `tasks`) instead of hardcoding a domain in the orchestrator — `LlmIntentClassifier` resolves it to a real, non-echo, **registered** agent (else self-disables to `echo`, so it's safe even when tasks-agent isn't deployed). When active, the classifier prompt is reframed: an actionable message matching no specialized domain → the catch-all agent (GTD capture-first), and `echo` is narrowed to greetings/small-talk; a closing "when unsure, prefer <catchAll>" nudge enforces capture-first. **Deterministic fallbacks stay `echo`** (LLM error, un-parseable output, no remote agents) — those mean "couldn't classify", not "user wants something captured", so they must not pollute the inbox. compose orchestrator gains `TASKS_AGENT_URL` (was missing — relied on the yml default) + `ORCHESTRATOR_CATCH_ALL_AGENT`; `.env.example` + README updated. Tests: 3 new `LlmIntentClassifierTest` (catch-all reframes the prompt + routes an unmatched actionable message to tasks; empty catch-all keeps the original echo-only wording; unknown catch-all agent self-disables to echo). orchestrator 6→9 green. Full 23-module reactor builds clean. **Stage 3 is now closed.**
- **PR63 — `next-action-suggester` skill (reactive, intent-driven next-action ranking):** Eleventh Stage-3 slice — the second intent-invoked skill, mirrors PR62's `inbox-clarify` pattern exactly. The user asks ("что мне сейчас сделать" / "what should I do now") → `IntentRouter` classifies (intent skills already generic since PR62 — no router change) → `IntentController` branches to new `intent/NextActionSuggester`. The flow fetches open next-actions (`status=next`) via a new mcp-tasks **`GET /internal/tasks`** filtered-list passthrough (`web/InternalTaskController`, delegates straight to the `list_tasks` tool — same filters/ordering/cap; reusable by any future flow) through a new `http/NextActionClient`, then LLM with `[AGENT.md body, next-action-suggester SKILL.md, {nextActions, userText}]` on DEFAULT → a ranked shortlist (due → priority → context). Read-only (suggests, never changes tasks); failures degrade to a friendly Russian message (empty list → "Нет открытых next-action"; mcp-tasks unreachable → "Не удалось получить список задач"). New `skills/tasks/next-action-suggester/SKILL.md` (`triggers: []`, RU+EN, ranking rules + edge cases); AGENT.md gains `skills: [..., next-action-suggester]` + an intent few-shot ("What should I do now?"). `IntentController` now dispatches both intent skills by `invokedSkill` name. Tests: 3 new `NextActionSuggesterTest` (ranks items with skill body + items + userText reaching the LLM; empty → no LLM call; fetch failure → degrade) + 1 new `McpTasksIntegrationTest` (`/internal/tasks?status=next` returns only the clarified next-action, not the inbox capture). tasks-agent 25→28, mcp-tasks 18→19 (all green; mcp-tasks via Testcontainers). Full 23-module reactor builds clean. **Note (out-of-scope cleanup candidate):** `IntentController` now has two explicit `invokedSkill` branches — a small `IntentSkillFlow` interface (`skillName()` + `run(msg)`) injected as a `List` would let new intent skills register with zero controller edits.
- **PR62 — `inbox-clarify` skill (reactive, intent-driven GTD clarification — first intent-invoked skill):** Tenth Stage-3 slice. Owner-chosen **reactive intent-flow** (over a proactive trigger): the user asks ("разбери инбокс" / "help me clarify my inbox") → orchestrator routes to tasks-agent → `IntentRouter` classifies → `IntentController` runs the flow. **This introduces the system's first intent-invoked skill** — every prior skill (weekly-review, budget-alerts, birthday-greeter, …) is scheduler-fired via `TriggerController`. Mechanism: an **intent skill** = a SKILL.md with no `triggers` (so `forTrigger` never maps it). `IntentRouter` now injects `SkillRegistry`, advertises intent skills in its classifier prompt alongside MCP tools, and accepts a third action `{"action":"skill","name":"…"}`; `RouterResult` gained `invokedSkill`; the router builds the classifier when **tools OR intent skills** exist (was tools-only). `IntentController` branches: `invokedSkill == inbox-clarify` → new `intent/InboxClarifier`. **InboxClarifier** reuses PR59's `/internal/review` passthrough via the existing `TaskReviewClient` (its `inbox` list is exactly the un-clarified items — **no mcp-tasks change**), then LLM with `[AGENT.md body, inbox-clarify SKILL.md, {inbox, userText}]` on the DEFAULT channel → proposal text. **Proposal-only MVP** — the skill proposes a per-item GTD clarification (actionable? status next/waiting/scheduled? context @home/@calls? project? next action?) but does NOT apply any `clarify_task`; apply-on-confirm is **deferred with the conversation-state layer** (same blocker as receipt-parser's confirm — recorded in Deferred work). Every failure degrades to a friendly Russian message (empty inbox → "Инбокс пуст"; review unreachable → "Не удалось получить инбокс"). New `skills/tasks/inbox-clarify/SKILL.md` (frontmatter `triggers: []`, RU+EN body, proposal-only rules + edge cases); AGENT.md gains `skills: [..., inbox-clarify]` (PR32 loader cross-check covers it) + an intent few-shot ("Help me clarify my inbox") so the orchestrator routes it. Tests: 2 new `IntentRouterTest` cases (routes to a registered intent skill — classifier built from the skill alone with no tools; unknown skill name → falls back to chat) + 3 new `InboxClarifierTest` (proposes from inbox items with the skill body + items + userText reaching the LLM; empty inbox → no LLM call; review failure → friendly degrade); `IntentControllerTest` comment refreshed (classifier path now active because an intent skill is loaded — a non-JSON LLM reply still propagates as chat). tasks-agent tests 20→25 (all mockito/MockWebServer — no Docker). Full 23-module reactor builds clean. **Test gotcha captured:** a bare `new ObjectMapper()` can't serialize `TaskItemDto`'s `Instant` fields (no JavaTimeModule) and silently hits the degrade branch — use `findAndRegisterModules()` to mirror Spring's mapper.
- **PR61 — `enable_weekly_review` / `disable_weekly_review` mcp-tasks tools (per-household `weekly.review` cron):** Ninth Stage-3 slice — closes the proactive weekly-review loop end-to-end (cron → orchestrator → tasks-agent → PR60 enrichment+skill → notifier) without any manual `POST /v1/schedules`. Mirrors finance PR27b, but **no new table / no local schedule-id column**: a weekly review is exactly one-per-household, perfectly identifiable by `(householdId, kind=weekly.review)`, so enable/disable reconcile against scheduler-service's `GET /v1/schedules?householdId=` list endpoint instead of colocating a `schedule_id` (budgets needed the column because alerts are per-category-slot, which scheduler can't distinguish). New `scheduler/SchedulerClient` (synchronous, called from the `@Tool` path): `findWeeklyReview` (list → filter by kind), `register` (POST a recurring `CreateScheduleRequest`, **payload=null** — householdId travels on the schedule row and reaches the agent via `AgentWakeRequest.householdId()`, where PR60's `TriggerController` enriches from `/internal/review`), `delete` (best-effort, 404=success). `register`/`find` errors propagate (no local row to fall back to — a scheduler outage surfaces to the caller); only `delete` soft-fails. New `config/McpTasksProperties` (`mcp-tasks.{scheduler-url, review.{cron,owner-agent,trigger-kind}}`; default cron `0 0 9 * * MON` = Monday 09:00 UTC) — mcp-tasks gained its first `config/` package + `config/HttpConfig` (`schedulerWebClient` bean, `.clone()`d). Two new `@Tool`s in `TasksMcpTools` (9→11): **`enable_weekly_review(householdId, cron?)`** validates the cron (`CronExpression.isValidExpression`), and is **idempotent** — existing schedule on the same cron + enabled → no-op return; different cron → register-new-FIRST then delete-old (flake-safe ordering, same as `set_budget`); **`disable_weekly_review(householdId)`** finds + deletes, returns the removed `ScheduleDto` or null. Reuses existing `ScheduleDto`/`CreateScheduleRequest` contracts — **no new contract, no migration.** application.yml gained the `mcp-tasks.*` block; compose `mcp-tasks` gains `SCHEDULER_URL` + `MCP_TASKS_REVIEW_*` env + `depends_on: scheduler-service (service_healthy)`; `.env.example` mirror; pom gained okhttp `mockwebserver` (test). Tests: 5 new `McpTasksIntegrationTest` cases over a static-init scheduler `MockWebServer` (register-when-none with POST-body assertions; idempotent no-op on same cron — list-only, no POST/DELETE; replace-on-cron-change with register-first-then-delete ordering; invalid-cron reject; disable-then-no-op-when-absent). mcp-tasks tests 13→18 (Testcontainers PG + scheduler MockWebServer, ran green locally). Full 23-module reactor builds clean. **Deferred:** the agent could expose this via intent ("включи еженедельный обзор") — already works through PR58's tool router since both tools are MCP `@Tool`s; no extra wiring needed.
- **PR60 — `weekly-review` skill + real `TriggerController` (first proactive tasks flow):** Eighth Stage-3 slice — turns the tasks-agent triggers stub into a real LLM+notifier flow (mirrors finance PR24a + PR10 + PR27a-enrichment). New `skills/tasks/weekly-review/SKILL.md` (frontmatter `triggers: [weekly.review]`, RU+EN; composes a short GTD nudge from inbox/waiting counts + stuck-project samples; emits literal `SKIP` on a clean week). AGENT.md gains `skills: [weekly-review]` + a `triggers: [{kind: weekly.review, description}]` entry (so PR32's loader cross-check now covers tasks-agent). `TriggerController` rewritten: resolve skill via `SkillRegistry` (unknown kind → 404) → **enrich** `weekly.review` by fetching mcp-tasks' `/internal/review` aggregate (new `http/TaskReviewClient` on the new `mcpTasksWebClient` bean; 2s timeout, errors → `EnrichmentFailedException` → 503 so scheduler retries) → rewrite the wake payload to the `WeeklyReviewResult` shape → run the skill (LLM with `[AGENT.md, SKILL.md]` system prompts + `{payload}`) → on a non-`SKIP` reply, fan out to every household member via `ProfileClient.usersByHousehold` + `NotifierClient.notify` (per-user `onErrorResume` so one failure doesn't block the rest); `SKIP` short-circuits before profile/notifier are touched. New `tasks-agent.mcp-tasks-url` (reuses `MCP_TASKS_URL`) + `mcpTasksWebClient` in `OutboundHttpConfig`; `TasksAgentProperties` gained `mcpTasksUrl` (main + test yml). `TriggerControllerTest` rewritten from the 404-stub to 3 real cases over 4 MockWebServers (llm/mcp-tasks/profile/notifier): weekly.review enriches from `/internal/review` + LLM body carries `inboxCount` + both members notified; `SKIP` suppresses notification; unknown kind 404s without touching the LLM. tasks-agent tests 18→20 (all MockWebServer — **no Docker needed**, ran green locally). **Memory recall enrichment deferred** (finance added it in PR24c — a later mirror). **Scheduler cron registration is PR61** — until then `weekly.review` fires only via a manual `POST /v1/schedules`.
- **PR59 — mcp-tasks `GET /internal/review` passthrough (weekly-review data):** Seventh Stage-3 slice — the read surface the `weekly-review` skill needs (chosen `/internal` REST passthrough over MCP-dispatch, mirrors finance PR27a). New contract `WeeklyReviewResult(householdId, inboxCount, waitingCount, stuckProjectCount, inbox[], waiting[], stuckProjects[])` — full counts + capped (25) sample lists. New `review/ReviewService` (read-only): inbox/waiting counts via new `TaskItemRepository.countByHouseholdIdAndStatus`, samples via the existing `filter()`, and **"stuck" projects** via new native `TaskProjectRepository.findActiveWithoutNextAction` (active projects with `NOT EXISTS` a `task_item` status='next' — the GTD "needs a next action" list). New `web/InternalReviewController` exposes `GET /internal/review?householdId=<uuid>` — mcp-tasks' first web layer (the `@SpringBootApplication` scan already covers `web/`). Tests: 2 new `McpTasksIntegrationTest` cases (13→15) — `ReviewService` aggregates 2 inbox + 1 waiting + 1 stuck project (and a project WITH a next-action is correctly excluded); the endpoint round-trips via `WebTestClient.bindToServer` on `@LocalServerPort`. **Note:** validated via `mvn test-compile` locally (Docker daemon was unresponsive at author time, so the Testcontainers run is delegated to CI — the authority for the full run per CLAUDE.md). README updated (internal passthrough + key classes).
- **PR58 — LLM-driven tool routing in `IntentController` (`IntentRouter`):** Sixth Stage-3 slice — closes the tasks consumer-side stack (PR56 wiring → PR57 dispatcher → PR58 router), mirrors finance PR35. New `intent/IntentRouter`: when `ToolDispatcher.availableToolDefinitions()` is non-empty it builds a system prompt (`AGENT.md body` + available tools `name+description+inputSchema` + a strict JSON output contract `action: tool|chat`), ships it with the user message to llm-gateway, parses the reply. **Tool path:** `dispatch(name, argsJson)` on `Schedulers.boundedElastic` (blocking SSE), the tool's JSON result becomes the response text. **Chat path:** structured `text` field, or — when the LLM returns prose instead of JSON — the raw body, verbatim. **Lenient parsing:** non-JSON / missing `action` / dispatch failure all degrade to a user-facing string (`"Не удалось вызвать инструмент «X»: <reason>"`) rather than bubbling exceptions. **Back-compat short-circuit:** when no tools are wired (`spring.ai.mcp.client.enabled=false` in tests) the router falls through to the pre-PR58 single-system-prompt chat on the **default** channel — so `IntentController` is now a thin wrapper delegating to the router and wrapping the result in `IntentResponse(agent, text, llmModel)` (model propagated from the routing turn, preserving the orchestrator's intent contract). New nested `RouterResult(text, invokedTool, llmModel)` record (`invokedTool` is informational for future audit/confirm flows). `IntentController` no longer injects `LlmClient` directly (the router owns it). Tests: 5 `IntentRouterTest` cases (no-tools fallback uses only 2 messages + never dispatches; tool-dispatch happy path with exact args JSON; chat-action text propagates; non-JSON treated as chat; dispatch failure → friendly Russian error). Existing `IntentControllerTest` updated for the `default` channel (router fallback) — stays green. tasks-agent tests 13→18. Full module build green. **Triggers still 404** — the first skill (`weekly-review`) makes `TriggerController` real next.
- **PR57 — `ToolDispatcher` + `InternalToolsController` (consumer of PR56's MCP wiring):** Fifth Stage-3 slice (mirrors finance PR34). New `tools/ToolDispatcher` — a pure dispatcher: take a tool name + JSON args, find the matching `ToolCallback` in the Spring AI `ToolCallbackProvider`, invoke it. The provider is resolved via `ObjectProvider<>` so the bean stays valid when the MCP client is disabled (`spring.ai.mcp.client.enabled=false`, the test default) — in that mode `availableToolNames()` is empty and `dispatch` throws a clear "MCP client disabled" message rather than NPE'ing. `availableToolDefinitions()` (name+description+schema, alphabetical) is ready for PR58's LLM router prompt. New `web/InternalToolsController` exposes it as `GET /agents/tasks/internal/tools` (list names) + `POST /agents/tasks/internal/tools/{name}` (invoke with raw JSON body → tool's JSON result verbatim; unknown tool → 400 + `{"error":...}`). Blocking `ToolCallback.call` runs on `Schedulers.boundedElastic` so the WebFlux loop stays free. Admin-only by convention (not orchestrator-routed) — a testable seam ahead of PR58's LLM router. Tests: 5 `ToolDispatcherTest` (hand-rolled `ToolCallbackProvider` — dispatch happy-path + arg passthrough, unknown tool → sorted known-names, alphabetical sort, empty provider, MCP-disabled clear message) + 3 `InternalToolsControllerTest` (`@MockBean` dispatcher — list, POST passthrough verbatim, unknown → 400). tasks-agent tests 5→13. Full module build green. **Still no LLM in front** — PR58's `IntentRouter` makes `intent` pick a tool + dispatch; today `intent` is a plain chat and this controller is the only dispatcher caller.
- **PR56 — MCP-client wiring tasks-agent ↔ mcp-tasks (SSE):** Fourth Stage-3 slice (mirrors finance PR33). Adds the Spring AI MCP client transport (`spring-ai-starter-mcp-client-webflux`) to tasks-agent so it can discover + call mcp-tasks's `@Tool` methods. `application.yml` gained a `spring.ai.mcp.client.sse.connections.mcp-tasks.url` block (default `http://mcp-tasks:8095`, env `MCP_TASKS_URL`) + `toolcallback.enabled: true`; auto-config exposes a `ToolCallbackProvider` bean for the future dispatcher. **Eager-connect caveat:** the SDK dials mcp-tasks at context refresh (default 20s timeout), so a missing mcp-tasks surfaces at agent boot — toggle via `TASKS_AGENT_MCP_CLIENT_ENABLED=false`. Test scaffolding: `src/test/resources/application.yml` is now a **full mirror of main** (Spring Boot's test/resources yml REPLACES main, doesn't merge — captured in a comment) with `spring.ai.mcp.client.enabled=false` so the 4 existing SpringBootTests don't pay the 20s connect cost. New `McpClientWiringTest` re-enables MCP with `initialized=false` (registers the connection without dialling) and asserts the `ToolCallbackProvider` bean is wired. tasks-agent tests 4→5. compose `tasks-agent` gains `MCP_TASKS_URL` + `TASKS_AGENT_MCP_CLIENT_ENABLED` env + `depends_on: mcp-tasks (service_healthy)`; `.env.example` gains the toggle. **No tool dispatch yet** — PR57 (`ToolDispatcher`) + PR58 (LLM router in IntentController) make `intent` actually call tools; today it's still a plain chat. Full module build green.
- **PR55 — tasks-agent skeleton (manifest + intent real, triggers stub):** Third Stage-3 slice (mirrors finance's PR22). New module `agents/tasks-agent` (port **8096**, reactive webflux, `@Import(AgentRuntimeConfig.class)`). `AGENT.md` declares `name: tasks`, `mcp: [mcp-tasks]`, four GTD intent few-shots (capture to inbox / list today / complete / capture-under-project), **no skills or triggers yet** (skills classpath `classpath*:skills/tasks/*/SKILL.md`, dir not created yet — empty registry, AGENT.md declares no `skills:` so PR32's loader cross-check opts out). Three real-ish endpoints: `GET /agents/tasks/manifest` (parsed AGENT.md), `POST /agents/tasks/intent` (plain LLM chat with AGENT.md body as system prompt on the **fast** channel — MCP-tool routing is a later PR, same shape finance's IntentController had pre-PR35), `POST /agents/tasks/triggers/{kind}` (consults `SkillRegistry`, **always 404** — no skills shipped). **Gotcha captured:** `@Import(AgentRuntimeConfig.class)` eagerly creates the shared `ProfileClient`/`NotifierClient`/`MemoryClient` beans, which require the qualified `profileServiceWebClient`/`notifierWebClient`/`memoryServiceWebClient` beans to exist — so even the skeleton needs `config/TasksAgentProperties` (`tasks-agent.{profile-service-url,notifier-url,memory-service-url}`) + `config/OutboundHttpConfig` (the three `.clone()`d WebClients), unused until the first skill but mandatory for the context to start. Orchestrator config gains `{name: tasks, base-url: ${TASKS_AGENT_URL:http://tasks-agent:8096}}` alongside calendar/finance — its 6 routing tests stay green (their `@DynamicPropertySource` replaces the bound agents list rather than merging). Deploy surface: docker-compose `tasks-agent` block (depends only on llm-gateway — skeleton calls nothing else yet), `.env.example` `# === tasks-agent ===` block, infra/README port row (8096). 4 tests (`ManifestControllerTest` ×2, `IntentControllerTest` asserts AGENT.md system prompt + user text + `fast` channel hit the LLM + response carries agent=tasks/model, `TriggerControllerTest` asserts an unknown kind 404s). Full 23-module reactor builds clean; orchestrator suite green.
- **PR54 — mcp-tasks GTD transitions (clarify / update / complete / delete / link):** Second Stage-3 slice — completes the mcp-tasks tool surface (4→9 tools). New `@Tool` methods in `TasksMcpTools`: **`clarify_task`** (the GTD inbox→organized move — `id`+`status` required, organizing fields `context`/`projectId`/`dueAt`/`deferUntil`/`priority` applied when non-null; `status` validated against the whitelist `inbox|next|waiting|scheduled|done|dropped`; `projectId` cross-household-guarded — mirrors finance's account/category guard; a shared `applyStatus` helper stamps `completed_at` when status→done and clears it when leaving done); **`update_task`** (partial non-null content edit of title/note/context/projectId/priority/dueAt/deferUntil — status changes deliberately routed through clarify/complete, not here; `projectId` same cross-household guard; household/source/externalRef/createdAt immutable); **`complete_task(id)`** (shortcut: status=done + completed_at stamp); **`delete_task(id)`** (returns the deleted DTO so the agent can confirm/undo — confirm-before-delete is the agent layer's job, matching mcp-finance's `delete_transaction`; throws on unknown id); **`link_task_to_event(id, calendarEventUid)`** (stores the calendar UID only — creating the event is the agent's job via orchestrator→calendar-agent). New contracts `ClarifyTaskInput` + `UpdateTaskInput`. README tool list + key-classes refreshed (nine tools). Tests: 6 new `McpTasksIntegrationTest` cases (11 total, was 5) — clarify organizes an inbox item + makes it filterable by its new state (and out of inbox); clarify rejects a bogus status AND a cross-household project; complete stamps `completed_at` then clarify-away clears it; update applies non-null fields only + leaves status/source; delete returns the row + throws on re-delete; link stores the uid. Full module build green. **No schema change** — reuses 030-tasks.yml. **Next: PR55 — tasks-agent skeleton.**
- **PR53 — `030-tasks.yml` schema + mcp-tasks CRUD starter:** Stage-3 opener (mirrors finance's PR21). New feature changelog [030-tasks.yml](../infra/liquibase/features/030-tasks.yml) creates two tables in the `tasks` schema (already in `init.sql`): **`task_project`** (id, household, owner?, name, status `active|someday|done|dropped` default active, note, metadata, created_at) + **`task_item`** (id, household, owner?, project? FK→task_project, title, status `inbox|next|waiting|scheduled|done|dropped` **default inbox**, context text-tag, priority smallint, due_at?, defer_until?, note, source default manual, external_ref?, **calendar_event_uid?** — no cross-schema FK, calendar/Radicale own event lifecycle, same reasoning as `fin_budget.schedule_id` — schedule_id?, metadata, created_at, completed_at?). Indices: `(household_id, status)`, `project_id`, `due_at`, `(household_id, context)`. Master changelog gains the include (moved out of the reserved-comment block); test-schema.sql mirrors both tables. New module `mcp/mcp-tasks` (port **8095**, Spring AI MCP webflux + JPA servlet stack — same shape as mcp-finance). 4 `@Tool` methods in `tools/TasksMcpTools`: **`add_task`** (capture → status=inbox, source default manual, no project/context — the one-line GTD capture), **`list_tasks`** (optional status/context/projectId/dueBefore filters via native-SQL `CAST(:p AS …)` — same pgjdbc NULL-bind workaround as mcp-finance's `filter()`; ordered `due_at ASC NULLS LAST, created_at DESC`; hard cap 200, default 50), **`upsert_project`** (status default active), **`list_projects`** (optional status filter). GTD transitions (clarify/complete/update/delete/link) are PR54. No scheduler dependency in this slice → no MockWebServer in the test. New contracts in `libs/contracts/tasks/`: `TaskProjectDto`, `TaskItemDto`, `UpsertProjectInput`, `AddTaskInput`, `ListTasksInput`. New env `MCP_TASKS_PORT`/`MCP_TASKS_DB_*`/`MCP_TASKS_URL`; docker-compose `mcp-tasks` service block (no scheduler dep) + infra/README port row (8095) + `.env.example` block. 5 Testcontainers integration tests: upsert-project create-then-update-in-place (+ default status), list-projects status filter, add-task lands-in-inbox-with-defaults, list-tasks status/project filters (non-inbox states need PR54's clarify_task, so this slice asserts filters against inbox captures + empty results), list scope no-cross-household-leak. Full 22-module reactor builds clean.

## Done (Stage 5)
- **PR51 — Langfuse tracing for SSE stream + embeddings (completes the trace surface):** Extends PR50 from chat-only to all three llm-gateway call surfaces. `LangfuseTracer.buildBatch` was generalised (params: traceName, generationName, channel, model, input, output, nullable usage, extra metadata) so the three public methods share one builder; `traceChat` output is byte-identical to PR50 (same trace name, usage, finishReason). New **`traceChatStream(req, output, model, start, end)`** — the SSE stream yields text deltas only, so `ChatController.stream` accumulates them in a `StringBuilder` via `doOnNext` and fires the trace in `doOnComplete`; the model is resolved from the channel (`LlmGatewayProperties.channelModels()`, newly injected into the controller) since the delta stream carries no model, and **usage is omitted** (no token counts on a stream) — trace name `llm-gateway.chat.stream`, metadata `streamed:true`. New **`traceEmbed(req, resp, start, end)`** wired into `EmbedController` (same fire-and-forget `doOnNext` shape) — trace name `llm-gateway.embed`, input = the raw texts, output = a `"N vectors × D dims"` summary, usage from the embed response, metadata `vectorCount`/`dimensions`. Same off-by-default + soft-fail contract as PR50; disabled → `Mono.empty()`, no HTTP. README **Tracing** section now has a per-surface table; the "not traced yet" caveat is gone. Tests: 2 new `LangfuseTracerTest` cases — stream trace carries accumulated output + `streamed:true` and **asserts no `usage` block**; embed trace carries model/channel=embedding/vectorCount/dimensions/input-texts/usage. `ChatControllerTest` (now 3-arg controller ctor) stays green via autowiring. llm-gateway test count 26→28. Full module build green. **Closes the doable-without-a-live-model part of Stage 5** — remaining (golden tests on real models) is blocked on model access, recorded in Now.
- **PR50 — Langfuse chat tracing in llm-gateway:** Stage-5 observability slice (architecture.md §LLM strategy: "tracing via Langfuse"). Until now llm-gateway had Prometheus metrics but no per-call LLM trace; this wires the first one. Greenfield — there was no Langfuse env, compose, or code anywhere yet. New `config/LangfuseProperties` (`@ConfigurationProperties("langfuse")`: enabled/base-url/public-key/secret-key, picked up by the app's existing `@ConfigurationPropertiesScan`) + `trace/LangfuseTracer`. Every non-streaming `POST /v1/chat` now, **after** the provider response is produced, fires `tracer.traceChat(req, resp, start, end).subscribe()` from `ChatController` as a side effect (`doOnNext`) — **fire-and-forget so it never delays or breaks the response**. The tracer POSTs Langfuse's batch ingestion API (`POST /api/public/ingestion`, HTTP Basic public/secret key) with two events: a `trace-create` (`llm-gateway.chat`, channel metadata, input turns, output) + a `generation-create` GENERATION observation carrying `model`, `startTime`/`endTime` (latency), the input turns (role+content, image-count when present), `output`, Langfuse-shaped `usage` (`input`/`output`/`total` + `unit:TOKENS` from `LlmUsage`), and `finishReason`. **Off by default** (`LANGFUSE_ENABLED=false`) — when disabled the tracer builds no WebClient and `traceChat` short-circuits to `Mono.empty()`, so `mock` dev runs and every existing test stay silent (no test-resources override needed; `ChatControllerTest`'s full-context `@SpringBootTest` autowires the disabled bean transparently). **Best-effort by contract:** 5s timeout, any ingestion error (network / 4xx-5xx) logged at DEBUG and swallowed via `onErrorResume(→empty)` — a Langfuse outage must never surface to callers. **Only non-streaming chat traced** — SSE stream + embeddings deferred (delta accumulation / different usage shape), recorded in Remaining Stage 5. `application.yml` gained a `langfuse.*` block; `infra/.env.example` gained `LANGFUSE_ENABLED` + commented base-url/key pair under the llm-gateway block; README gained a **Tracing (Langfuse)** section + two key-class entries. Tests: 3 new `LangfuseTracerTest` cases (MockWebServer) — enabled → POST `/api/public/ingestion` with Basic auth + a 2-event batch whose generation carries model/output/usage(12/3/15)/finishReason and a `traceId` matching the trace's `id`; disabled → zero HTTP; 500 from Langfuse → swallowed (no throw). llm-gateway test count 23→26. Full module build green.
- **PR20 — Stage-1 E2E closer (wire-contract version):** Closes the long-deferred Stage-1 test. New `E2EStage1ClosingFlowTest` in scheduler-service's test set drives a real scheduler-service Spring context (Postgres Testcontainer) + two MockWebServers — one playing orchestrator with a **forwarding dispatcher** that deserialises the wake into `AgentWakeRequest` (from `libs/contracts`) and forwards the raw body to a second MockWebServer playing calendar-agent's trigger endpoint, then 202s the scheduler. The test seeds a due `core.schedules` row, calls `ScheduleTick.tick()` directly, then asserts **both hops**: hop 1 — orchestrator received POST `/v1/agents/wake` with a body whose `agent`/`kind`/`scheduleId`/`householdId`/`payload.personId` all match the seeded row; hop 2 — calendar-agent received POST `/agents/calendar/triggers/birthday.greet` with the **same** `AgentWakeRequest` re-deserialised cleanly on the receiver side (since calendar-agent's `TriggerController` binds the body to `AgentWakeRequest` verbatim, this proves the wire contract survives both serialisation round-trips). Buffer.clone() in the dispatcher preserves the original body so the test can read it from `takeRequest()` afterward — captured in a comment so the next maintainer doesn't relearn it. **Deviation from the original deferred-work plan documented in the test javadoc + STATUS.md:** the planned standalone `tests/e2e-stage1` module would have required flipping `spring-boot-maven-plugin` to `classifier=exec` on scheduler-service + calendar-agent so a downstream module can depend on them as plain jars (today the repackage goal replaces the main artifact with the fat jar) — that refactor touches two Dockerfiles + two READMEs + the docker-compose paths and was deemed out of scope for the closer. The wire-contract version delivers the same value (each individual seam already has its own integration test; this test composes them). Real-Ollama opt-in variant deferred (see "Deferred work" above) — to be written when we see a regression class this closer misses. Full 16-module reactor green.
- **PR19 — openai-compatible LLM provider in llm-gateway:** Closes the embedding-channel gap PR18's Anthropic left open + lands the **free local baseline** the Stage-5 E2E closer needs. New `provider/openai/OpenAiCompatibleProvider` (`@ConditionalOnProperty llm.provider=openai-compatible`) speaks the OpenAI Chat Completions dialect — `POST /chat/completions` (+ SSE), `POST /embeddings` — against any backend that implements it: local Ollama (free), DeepSeek cloud, Together, vLLM, OpenAI proper. `LLM_BASE_URL` is required and must include the dialect's version prefix (e.g. `http://ollama:11434/v1`); `LLM_API_KEY` is **optional** — when set goes through as `Authorization: Bearer <key>`, when blank the header is omitted (Ollama ignores Authorization, so omitting it keeps local dev clean). Request shape: `model` from per-channel config, `messages[]` translated 1:1 (SYSTEM/USER/ASSISTANT pass-through; TOOL rejected with a clear error since ai-life doesn't use it yet), optional `max_tokens` / `temperature` pulled straight from `LlmChatRequest`. SSE stream extracts `choices[0].delta.content` and stops on `data: [DONE]`. Embeddings call `POST /embeddings` with the channel's model (default `bge-m3`) and pack `data[].embedding` into `LlmEmbedResponse`. 7 MockWebServer tests cover request shape + Bearer auth, no-auth Ollama mode, SSE deltas + [DONE] termination, the `stream: true` flag, embed round-trip with 2 inputs, upstream-error propagation, fail-fast on missing base URL. README + `infra/.env.example` updated with both Ollama (free) and DeepSeek (cloud) profiles. Full 16-module reactor green.
- **PR18 — Anthropic LLM provider in llm-gateway:** Stage 5 opener. New `provider/anthropic/AnthropicProvider` (`@ConditionalOnProperty llm.provider=anthropic`) talks to `POST /v1/messages` via WebClient — `x-api-key` + `anthropic-version` headers, 60 s timeout. Translation rules: all `SYSTEM` messages concatenated (blank-line separated) into Anthropic's top-level `system` field (Anthropic refuses `system` entries inside `messages[]`); `USER`/`ASSISTANT` map straight through; `TOOL` rejected fast (unused today). `max_tokens` is required by the API — falls back to new `LLM_MAX_TOKENS` (default 4096) when the caller leaves `LlmChatRequest.maxTokens` null. SSE stream parses `content_block_delta` → `delta.text_delta.text` only, dropping every other event type. **Embeddings deliberately unsupported** — Anthropic has no embedding endpoint; embed() returns `Mono.error(UnsupportedOperationException)` with a pointer to running a second gateway with an embedding-capable provider for the `embedding` channel (PR19's openai-compatible provider closes this gap for Ollama / DeepSeek). Fail-fast on missing/blank `LLM_API_KEY` at construction. `LlmGatewayProperties` gained `anthropicVersion` (default `2023-06-01`) + `maxTokens` (default 4096). Mock stays the default for tests. 7 new tests in `AnthropicProviderTest` via MockWebServer cover request shape (model/system/max_tokens/headers/messages), channel→model resolution, SSE delta parsing, the `stream: true` flag, upstream-error propagation with body in the message, fail-fast on missing key, and embed→Unsupported — all free (no real API calls). README + `infra/.env.example` updated with anthropic profile + no-embeddings note. Full 16-module reactor green.

## Done (Stage 4)
- **PR99 — orchestrator emits inbound text to memory-service `/v1/observations` (MFC-b producer — memory-from-chat now automatic end-to-end):** Closes the chain: a typed message now passively becomes durable memory without any manual call. `MemoryClient` gained `observe(householdId, userId, text, source)` — a **fire-and-forget** POST to memory-service's durable drop-point (PR98), off the response path (`.subscribe()`), 500 ms timeout, soft-fail (any error swallowed) — the exact posture the orchestrator already uses for recall. `IntentController.handle` calls it before `router.route`, so every inbound `/v1/intent` is captured while routing is completely unaffected. No-op when memory disabled / no household / blank text (an attachment-only message — those facts are captured by the agent that processes the attachment; **capture is distributed by design** since no single component sees everything). End-to-end path now live: typed message → orchestrator (observe) → `bus.outbox` → `MessageCaptureHandler` (PR97) → `FactExtractor`+write → recallable memory. The orchestrator stays **DB-less** (the durable outbox lives in memory-service; the orchestrator just HTTP-fires) — the owner-approved resolution to "don't give the thin router a DB." Tests: new `MemoryObserveTest` (echo-only context; mock llm + mock memory-service with a path dispatcher) asserts the inbound text lands as a `POST /v1/observations` with the text + `source=telegram`; `AgentRoutingTest` converted its memory mock to a path dispatcher (recall vs observe) + per-test queue drain so the now-present observe drops don't pollute the recall assertions. orchestrator suite 16→17 green; full reactor compiles clean. README updated. **Next: MFC-c** — relation/graph extraction (the "build the graph" half) + finance-agent as the second producer (receipt facts).
- **PR98 — memory-service `POST /v1/observations` — durable async drop-point for memory-from-chat (MFC-b, ingress):** The "easy push" any DB-less producer (orchestrator, agents) uses to feed capture, closing the consumer (PR97) into a usable end-to-end path. New endpoint on `CaptureController`: body `MessageReceivedEvent` → `OutboxPublisher.publish("message.received", householdId, json)` → **202** immediately; the expensive LLM extraction runs later on the PR97 consumer with at-least-once retry. 400 on missing householdId / blank text. `/v1/capture` (sync extract+write, returns memories) stays for direct/manual/debug use. **Architecture decision (owner-delegated to principles — free / lean / maintainable / scalable):** reuse the **existing Postgres event bus**, do NOT add Redis. Rationale: Postgres already runs (zero new stateful component to deploy/back-up); `bus.outbox` is a status-tracked queue (`PENDING`→`PUBLISHED`), the opposite of the "cache becomes a dump (свалка)" failure mode the owner worried about; and it is exactly the owner's preferred "a service supplying info from a relational DB" shape. The only reason Redis came up — easy writes for producers without a DB — is solved by this one HTTP drop-point (Redis-push ergonomics, durable storage). **Capture is distributed by design:** no single component sees everything (the orchestrator sees typed text but not a receipt's parsed items, which live inside finance-agent), so each component emits what it learns to this drop-point. Tests: `MessageCaptureConsumerIntegrationTest` +2 (`POST /v1/observations` → 202 → consumer captures the `chat-capture` memory; blank text → 400) — memory-service suite 16→18 green; full reactor compiles clean. README updated (both ingress endpoints + key classes). **Next: MFC-b producer** — orchestrator fire-and-forgets to `/v1/observations` on inbound (same soft-fail HTTP pattern as recall), making capture automatic.
- **PR97 — memory-service consumes `message.received` off the bus → capture (MFC-b, consumer side):** Makes memory-from-chat automatic — memory-service is now the bus's **second consumer** (after notifier, C2a). New contract `contracts/message/MessageReceivedEvent{householdId, userId, text, source}` (topic `message.received`) — minimal by design (the fields capture needs; richer surfaces get their own event later). memory-service `@Import`s `EventBusConfig` (producer `OutboxPublisher`) and registers an `EventBusListenerContainer` `@Bean` (`config/EventBusListenerConfig`) whose handler (`bus/MessageCaptureHandler`) drains `message.received` rows from `bus.outbox` → `CaptureService.capture` (MFC-a) off the user's request path. **Retry policy mirrors notifier's `NotifyEventHandler`:** an unparsable payload / missing householdId+text is permanent (logged, row accepted → PUBLISHED); a transient write failure (embed 5xx / DB blip) re-throws so the row stays PENDING for the next poll. Extraction itself (`FactExtractor`) never throws, so an llm-gateway outage during extraction yields zero facts and the row settles PUBLISHED (not requeued for the reasoning step) — documented in the handler. Single-consumer bus → foreign topics (e.g. `schedule.fired`) are drained but ignored. memory-service gained the `event-bus` dep; `test-schema.sql` now mirrors `bus.outbox` (so the listener boots in every memory-service test — existing `MemoryServiceIntegrationTest`/`RelationsIntegrationTest` stay green with the listener active). Test: `MessageCaptureConsumerIntegrationTest` (Testcontainers PG + MockWebServer llm-gateway routing `/v1/chat`→facts + `/v1/embed`→vector, `event-bus.poll-millis=500`): publish a `message.received` via `OutboxPublisher` → await the captured `chat-capture` memory + row PUBLISHED; a foreign `schedule.fired` topic is drained but writes nothing. memory-service suite 11→16 green; full reactor compiles clean. README updated (consumer section + key classes). **Producer side exercised in-test via direct outbox publish — the orchestrator producer is the next slice (MFC-b producer).**
- **PR96 — memory-service `POST /v1/capture` — memory-from-chat extraction+write core (MFC-a):** Opener of the **memory-from-chat** track (the fuel every Coordinator flow consumes — D2's gift-recommender already gathers `memories`/`relations`, but nothing auto-populates them from dialogue yet). New `capture/FactExtractor` wraps `LlmClient.chat` (DEFAULT channel) with a strict-JSON extraction prompt → durable, self-contained fact sentences about the speaker or people mentioned; **best-effort** parsing (strips markdown fences / leading prose; bad reply / parse failure / blank input → empty list, never throws). New `service/CaptureService` runs the extractor then writes each fact via the existing `MemoryService.write` (embed + insert) with the request's scope, tagged `source=chat-capture` so captured memories are distinguishable from explicit writes. New `web/CaptureController` — `POST /v1/capture` (body new contract `CaptureRequest{householdId, userId?, personId?, text}`) → written `MemoryDto[]` (empty when nothing durable found); 400 on missing householdId / blank text. **Design (owner-aligned 2026-06-19):** extraction is memory-reasoning and lives in memory-service (it already owns the schema + recall + the llm-gateway embed hop) rather than a new service or the thin orchestrator; facts arrive from many surfaces (typed message now, parsed receipt later) so the eventual trigger is the **event bus** (MFC-b), keeping capture off the user's request path. This slice is the testable core — no trigger wired yet. **Relation/graph extraction is MFC-c** (this slice captures free-text memories only). Tests: `FactExtractorTest` (6 Mockito unit cases — clean JSON, fenced JSON, leading prose, empty facts, non-JSON→empty, blank input skips the LLM) + `MemoryServiceIntegrationTest` gained 2 (capture→2 stored `chat-capture` memories→exact-text recall round-trip; blank text→400; its llm-gateway mock dispatcher now routes `/v1/chat` → facts JSON alongside `/v1/embed`). memory-service suite 3→11 green (Testcontainers PG + MockWebServer llm-gateway); full reactor compiles clean. README updated (endpoint + key classes). **Next: MFC-b** — orchestrator emits a `message.received` bus event, memory-service consumes it → calls capture automatically.
- **PR95 — calendar-agent `gift.recommend` rebuilt on the `Coordinator` (Track D / D2c — D2 COMPLETE, FIRST real Coordinator flow):** The closer of the budget-aware gift-recommender — and the first flow to exercise the PR91 `Coordinator` substrate end-to-end on a real vertical (calendar → finance → memory). New `flow/GiftRecommender`: on a `gift.recommend` wake it builds a gather map and hands it to the injected `Coordinator` — three sources run **in parallel**: **`budget`** = finance-agent's `get_gift_budget` (D2b) called through the orchestrator's `/v1/agents/invoke` hub via new `http/OrchestratorInvokeClient` (mirrors tasks-agent's C1e client; the locked inter-agent path — agents never call each other directly), filtered to `ok` results; **`memories`** = memory-service recall on the person; **`relations`** = the person's graph edges (emitted only when non-empty). The `Coordinator` folds the successful, non-empty sources into a `context` object and synthesizes from `[AGENT.md body, gift-recommender SKILL.md] + {payload(person), context}` on the DEFAULT channel, then the flow fans the text out to every household member via `ProfileClient` + `NotifierClient` (same delivery as the generic path). **Per-step soft-fail is the whole point:** a finance/orchestrator outage drops the `budget` step (the coordinator omits it) and the suggestion still ships — just without a price constraint. `web/TriggerController` now branches: `gift.recommend` → `GiftRecommender`; every other kind keeps the generic recall→LLM→notify `runSkill` path. **`gift-recommender` SKILL.md upgraded** to consume the new shape (`payload.person`, `context.budget`/`memories`/`relations`): respect `context.budget.remaining` (fallback `amount`) + `currency` and name a rough price range when a budget is present; ideas-only (no prices) when `hasGiftBudget` is false or no budget gathered — **clears the skill's "finance integration ships later / do NOT name a budget" note.** Wiring: new `calendar-agent.orchestrator-url` (`ORCHESTRATOR_URL`, default `http://orchestrator:8083`) + `orchestratorWebClient` bean; relies on the yml default in compose (mirrors PR76 — no compose/`.env` change, orchestrator doesn't depend on agents so there's no cycle). Tests: `TriggerControllerTest` gained an orchestrator MockWebServer + the existing gift test rewritten as `giftRecommendWakeCoordinatesBudgetAndMemoryThenFansOut` — asserts the coordinator POSTs `/v1/agents/invoke` with `{action:get_gift_budget, targetAgent:finance, householdId}`, the gathered budget envelope (`hasGiftBudget`, `RUB`) + person fields + memory hit + relation all reach the LLM body, and the household member gets the notify. calendar-agent suite 12→13 green; full reactor compiles clean. READMEs (calendar-agent endpoints/env/key-classes/tests) + SKILL.md updated. **D2 done → next: memory-from-chat (the fuel), then D3.**
- **PR94 — finance-agent `POST /agents/finance/actions/get_gift_budget` inter-agent action (Track D / D2b):** Second slice of the budget-aware gift-recommender flow — the **consumer side of the orchestrator invoke primitive**, finance's **first `/actions/*` endpoint** (mirrors calendar's `create_event`, C1c/PR74). New `web/ActionController` — `POST /agents/finance/actions/{action}`; recognises only `get_gift_budget`, **forces `householdId` from the envelope** (never trusts args), reads PR93's `GET /internal/gift-budget` via new `http/GiftBudgetClient`, and **always returns a structured `AgentActionResult` (never an HTTP error):** present budget → `ok({hasGiftBudget:true, amount, currency, remaining?})`; no Gifts category / no active monthly budget (404) → `ok({hasGiftBudget:false})` (a valid "no budget set" state — the coordinator can still propose gifts unconstrained); missing `householdId` or mcp-finance down/timeout → `ok=false` with a reason. `GiftBudgetClient` reuses the existing `mcpFinanceWebClient` bean (no config change) and mirrors `BudgetStatusClient`'s per-status policy (200→present, 404→empty, 5xx/timeout→propagated). Test: `ActionControllerTest` (4 cases, MockWebServer for mcp-finance + a `@BeforeEach` drain of the shared static server's recorded-request queue): present budget → `{hasGiftBudget:true, amount=200, currency=EUR, remaining=150}` + asserts the `GET /internal/gift-budget?householdId=` call; 404 → `hasGiftBudget=false` with no amount; unknown action → `ok=false`; missing household → `ok=false`. finance-agent suite 33→37 green; full reactor compiles clean. README endpoints/env/key-classes updated. **No AGENT.md change** (action routing is path+targetAgent, not manifest-driven — same as calendar's C1c). **Next: D2c** — calendar-agent's `gift.recommend` rebuilt on the `Coordinator`, gathering `{interests: memory recall, budget: finance invoke get_gift_budget}`.
- **PR93 — mcp-finance `GET /internal/gift-budget` read passthrough (Track D / D2a):** First slice of the budget-aware `gift-recommender` flow (D2) — the deterministic finance read the coordinator gathers. New `web/InternalGiftBudgetController` — `GET /internal/gift-budget?householdId=<uuid>` → new contract `GiftBudgetResult{amount, currency, remaining?}` (200) | 404. **The MVP gift-spending envelope = the active *monthly* budget on the household's `Gifts` expense category** (matched case-insensitively): `amount` is the limit, `remaining` = limit − spent this month (may be negative when over budget). New non-`@Tool` `FinanceMcpTools.getGiftBudget(householdId)` **composes** the existing `findByHouseholdIdOrderByName` category lookup + `getBudgetStatus(.., "month")` read — no new persistence, no new MCP tool surface. Throws `IllegalArgumentException` (→ 404) when there's no Gifts category or no active monthly budget on it; the calling agent (D2b) treats 404 as "no budget set", never an error. Mirrors `InternalBudgetController` (PR27a) shape/reasoning. The `Gifts` category name is a fixed constant for the MVP — **D3 turns gift-budget rules into editable data** (per the routing-doctrine "editable rules = data"). Test: `McpFinanceIntegrationTest.internalGiftBudgetReturnsMonthlyEnvelopeOver404WhenUnset` (fresh household for determinism: no category → 404; lowercase `gifts` category but no budget → still 404, exercising the case-insensitive match; set a 200 EUR monthly budget + a −50 in-window expense → `{amount=200, currency=EUR, remaining=150}`). mcp-finance suite 31→32 green; full reactor compiles clean. README "Internal REST passthroughs" updated. **Next: D2b** (finance-agent `/actions/get_gift_budget` consumes this via the invoke primitive).
- **PR77 — notifier-service: first event-bus consumer (`notify.requested`) (Track C / C2a):** Turns notifier into the system's **first bus consumer** — the consumer-in-a-service half PR71 deferred to C2. notifier now `@Import`s `EventBusConfig` and registers an `EventBusListenerContainer` `@Bean` (`config/EventBusListenerConfig`) whose handler (`bus/NotifyEventHandler`) consumes `notify.requested` events off `bus.outbox` and delivers them through the **same** resolve→`/internal/send` path the synchronous `POST /v1/notify` uses — extracted into a shared `notify/NotifySender` (the `NotifyController` is now a thin wrapper). New contract `NotifyRequestedEvent{userId, text, source?}` (topic constant `notify.requested`) in `libs/contracts/notify`. **Owner-chosen generic event** (over a domain-specific `birthday.upcoming`): notifier stays domain-agnostic (text only); a producer wanting a whole household emits one event per member (household membership is a domain concern, not notifier's). **Retry policy** maps the at-least-once bus onto delivery: a transient failure (profile/gateway 5xx, timeout, network) re-throws so the row stays `PENDING` for the next drain; a permanent failure (404 unknown user / 422 no telegram / unparsable payload) is logged + accepted so it can't head-of-line-block the single-consumer drain (a known B1 limitation). notifier currently drains **all** topics; non-`notify.requested` events are ignored (and thereby marked `PUBLISHED`) — consistent with B1's single-consumer model. notifier gained a JDBC datasource (`spring-boot-starter-jdbc` + postgresql driver, `NOTIFIER_DB_*`) to reach `bus.outbox`; existing `NotifyControllerTest` sets `event-bus.enabled=false` (no DB in that slice — Hikari connects lazily). New `NotifyEventConsumerIntegrationTest` (Testcontainers PG + profile/gateway MockWebServers, `test-schema.sql` mirrors `bus.outbox`): publishes `notify.requested` via `OutboxPublisher` → asserts gateway `/internal/send` + the outbox row flips `PUBLISHED`; a 404 user is permanently dropped (no send, row still `PUBLISHED`, not stuck `PENDING`); a foreign `schedule.fired` topic is drained but ignored (no gateway call). notifier suite 4→7 green. compose + `.env.example` gain the DB + `EVENT_BUS_CHANNEL` env + `depends_on: postgres/liquibase`. README updated. **Producer side exercised in-test via direct outbox publish — the real agent producer is C2b.** **Note:** notifier is the first service to run `PostgresEventBusListener` under a Spring lifecycle; with Testcontainers the DB container can stop before the cached context closes, so the listener may log one ERROR at teardown (guarded to `running==true`) — benign test-teardown noise, not a production path.
- **PR76 — tasks-agent task-to-event flow (Track C / C1e — closes C1, first full inter-agent chain):** The closer — the agent-to-agent **call path** the Deferred `task-to-event` item said was missing. New `flow/TaskToEventService`: given a hard-deadline task, builds an `AgentActionRequest(targetAgent=calendar, action=create_event, args={summary, dtstart=dueAt}, requestingAgent=tasks)`, calls the orchestrator's `/v1/agents/invoke` (PR72) via new `http/OrchestratorInvokeClient`, extracts `eventUid` from the `AgentActionResult`, and records it on the task via mcp-tasks `/internal/link-event` (PR75) through new `http/LinkEventClient`. **Always resolves to an `AgentActionResult`:** a calendar error or missing `eventUid` propagates as `ok=false` with **no link written**; a link failure *after* the event was created is surfaced (event exists, task not stamped) so the caller can retry. Exposed at `POST /agents/tasks/internal/task-to-event` (`web/InternalTaskToEventController`, body new contract `TaskToEventRequest{taskId, householdId, summary, dueAt}`) — **internal/admin, not on the intent path** (the user-facing trigger — auto-offer on a hard-deadline clarify — is a deliberate follow-up). New `tasks-agent.orchestrator-url` (`ORCHESTRATOR_URL`) + `orchestratorWebClient` bean; test yml mirrors it. **This is the first real agent→orchestrator→agent→back chain in the system** (tasks → calendar → tasks), proving the C1a–C1d primitives compose. Tests: 3 `TaskToEventFlowTest` cases (mock orchestrator + mock mcp-tasks: happy path asserts invoke `/v1/agents/invoke` create_event body + link `/internal/link-event` body + `{eventUid, taskId}`; calendar `ok=false` propagates and skips link; missing `dueAt` → `ok=false`). tasks-agent suite 31→34 green. READMEs (tasks-agent) + tasks.md updated. **Clears the Deferred `task-to-event` item.** **Next: C2** (async chain over the bus → notifier consumer).
- **PR75 — mcp-tasks `POST /internal/link-event` passthrough (Track C / C1d):** Fourth slice of task-to-event — the deterministic write path that records a calendar UID on a task. New `web/InternalLinkEventController` — `POST /internal/link-event` (body new contract `LinkTaskToEventInput{id, calendarEventUid}`) delegates straight to the `link_task_to_event` tool (required-field + unknown-id guards apply identically), returns the updated `TaskItemDto` or 400. Same shape/reasoning as `POST /internal/clarify` (MCP-SSE isn't MockWebServer-testable, so deterministic agent calls go over `/internal/*`). New contract `LinkTaskToEventInput` in `libs/contracts/tasks`. Test: `McpTasksIntegrationTest.internalLinkEventStoresUidAnd400OnUnknown` (link stores the uid + returns the DTO; unknown id → 400). README updated. **Latent until C1e** — the caller (tasks-agent's task-to-event flow) is next. **Next: C1e** (tasks-agent reads a task → orchestrator-invoke `create_event` → this; closes C1 + clears the Deferred `task-to-event` item).
- **PR74 — calendar-agent `create_event` action (Track C / C1c):** Third slice of task-to-event and the **first agent `/actions/*` endpoint** (the consumer side of PR72's invoke primitive). New `web/ActionController` — `POST /agents/calendar/actions/{action}`; for `create_event` it deserializes the invoke `args` (JsonNode) → `CreateEventInput`, **forces `householdId` from the request envelope** (scope authoritative over anything in args), validates `householdId`+`summary`+`dtstart`, calls mcp-caldav `POST /internal/event` (PR73) via new `http/CaldavEventClient`, and returns `AgentActionResult.ok({eventUid: dto.calendarUid()})`. **Always replies an `AgentActionResult`, never an HTTP error** — bad args / unknown action / downstream failure all become a structured `ok=false` so the calling agent gets a clean signal (mcp-caldav failure → `onErrorResume`). New `calendar-agent.mcp-caldav-url` (`MCP_CALDAV_URL`, already in compose from the MCP-SSE binding) + `mcpCaldavWebClient` bean. No contract/schema change. Tests: 3 `ActionControllerTest` cases (create_event maps args → POST `/internal/event` with envelope `householdId` + returns `{eventUid}`; unknown action → `ok=false`; missing `dtstart` → `ok=false` before any caldav call). calendar-agent suite 10→13 green. README updated. **Latent until C1d** — no agent invokes calendar yet. **Next: C1d** (tasks-agent calls orchestrator-invoke `create_event` → `link_task_to_event`; closes C1 + clears the Deferred `task-to-event` item).
- **PR73 — mcp-caldav `POST /internal/event` passthrough (Track C / C1b):** Second slice of the task-to-event chain — gives calendar a **deterministic, non-LLM event-create path** the way mcp-finance got `POST /internal/transaction` (PR39). New `web/InternalEventController` — `POST /internal/event` (body `CreateEventInput`) delegates straight to `CalendarMcpTools.createEvent`, so the write-through-Radicale + cache-mirror behaviour is identical to the MCP tool; returns the persisted `CalendarEventDto` (whose `calendarUid` is the event uid C1d will link) or 400 on `IllegalArgumentException`. mcp-caldav gains its **first web layer** (the `@SpringBootApplication` component-scan already covers `web/`). No contract change (reuses `CreateEventInput` / `CalendarEventDto`), no schema change, MCP tool surface untouched. Test: `McpCaldavIntegrationTest` switched to `WebEnvironment.RANDOM_PORT` + a `WebTestClient`; new `internalEventPostCreatesEventAndMirrorsCache` (POST → 200 + DTO with a non-blank `calendarUid` + a `calendar.events_cache` row); the existing 2 tool-level tests stay green (3 total). **Latent** — the caller (calendar-agent's `create_event` action) is C1c. README updated. **Next: C1c** (calendar-agent `POST /agents/calendar/actions/create_event` maps the invoke `args` → `CreateEventInput` and calls this), then **C1d** (tasks-agent end-to-end + clears the Deferred `task-to-event` item).
- **PR72 — orchestrator inter-agent sync primitive `/v1/agents/invoke` (Track C / C1a):** First slice of C1 (task-to-event) and the **first real agent→agent path** — the locked decision made concrete (architecture.md §Decisions: agents never call each other directly, only via the orchestrator sync hub or the bus). New endpoint `POST /v1/agents/invoke` (`web/AgentInvokeController`): an agent POSTs an `AgentActionRequest{targetAgent, action, householdId, userId?, requestingAgent?, args}`; the orchestrator looks up `targetAgent` in the dispatch map and forwards to its `POST /agents/<name>/actions/<action>`, relaying the `AgentActionResult{ok, result?, error?}` verbatim (unknown target → 404). Mirrors the existing wake pattern (`/v1/agents/wake` → `/agents/<name>/triggers/<kind>`). `Agent` gained `invoke(AgentActionRequest)` — default returns an "unsupported action" error result (`ok=false`) so local agents (echo) degrade cleanly; `RemoteAgent.invoke` forwards over HTTP. New contracts `AgentActionRequest` + `AgentActionResult` (with `ok(result)` / `error(msg)` factories) in `libs/contracts/agent`. **Foundational + latent** — no agent exposes an `/actions/*` endpoint yet (that's C1b: calendar-agent `create_event`), so production behaviour is unchanged; fully tested in isolation. Tests: 2 `AgentInvokeControllerTest` cases (mock calendar-agent discovered at startup → invoke forwards to `/agents/calendar/actions/create_event` with the args + relays `{eventUid}`; unknown target → 404). orchestrator suite 14→16 green. Full reactor builds clean. README updated. **Next: C1b** (calendar-agent gains the `create_event` action via a new mcp-caldav `POST /internal/event` passthrough), then **C1c** (tasks-agent wires the chain end-to-end + clears the Deferred `task-to-event` item).
- **PR71 — event-bus Spring-bean API + scheduler-service `schedule.fired` producer (Track B / B2):** Turns B1's raw adapter into a wired-into-a-service capability and ships the **first reference producer**. New bean API in `libs/event-bus`: `EventBusConfig` (`@Configuration` a service `@Import`s — auto-provides the `OutboxPublisher` producer bean via `@ConditionalOnMissingBean`), `EventBusProperties` (`event-bus.{enabled, channel, poll-millis}`; `enabled` gates the listener only — the producer is always available), and `EventBusListenerContainer` (a `SmartLifecycle` wrapper a **consumer** service registers as a `@Bean` with its handler; starts/stops the listener with the context, honors `enabled`). **scheduler-service is the first producer:** `SchedulerApplication` now `@Import(EventBusConfig.class)`; `ScheduleTick` injects `OutboxPublisher` + `ObjectMapper` and, after each successful orchestrator wake + row advance, publishes a `schedule.fired` event — **best-effort soft-fail** (the wake side-effect already happened and the row already advanced, so a bus failure is logged and swallowed, never rolled back — rolling back would duplicate the wake). New contract `ScheduleFiredEvent(scheduleId, householdId, ownerAgent, kind, firedAt)`. scheduler `application.yml` gains an `event-bus.channel` block + `EVENT_BUS_CHANNEL` env; scheduler `test-schema.sql` mirrors `bus.outbox`. **Producer-only here** — the consumer-in-a-service half (an `EventBusListenerContainer` `@Bean`) lands with the first async chain (C2 → notifier). Tests: `libs/event-bus` 3→5 (`EventBusListenerContainer` start/stop drives delivery; disabled container doesn't start); `SchedulerIntegrationTest` now asserts the tick wrote a `schedule.fired` outbox row with the right `scheduleId`/`kind`. All 3 scheduler E2E wake closers stay green (context now imports `EventBusConfig`). Full reactor builds clean. READMEs (event-bus + scheduler) updated.
- **PR70 — `libs/event-bus` LISTEN/NOTIFY adapter + `bus.outbox` (Track B / B1):** Fills the long-stubbed `libs/event-bus` (was package-info only) with the async inter-agent transport — the locked decision (architecture.md §Decisions: agents talk only via orchestrator sync or this bus async; no new broker infra). New migration [007-bus.yml](../infra/liquibase/features/007-bus.yml) (uncommented from the reserved master-changelog slot) creates the `bus` schema + `bus.outbox` `(id, topic, household_id?, payload jsonb, status PENDING/PUBLISHED, created_at, published_at?)` with `ix_outbox_pending (status, created_at)`. **household_id has NO FK on purpose** — the log is append-only, may be system-wide, and must never block on a household mid-delete (captured in the YAML). **Transactional outbox + LISTEN/NOTIFY:** `OutboxPublisher.publish(topic, householdId, payloadJson)` INSERTs a PENDING row **and** fires `pg_notify` on the same `JdbcTemplate`, so the durable row + wake-up commit atomically inside the caller's business transaction. `PostgresEventBusListener` (`AutoCloseable`) holds one dedicated connection LISTENing on `EventBus.DEFAULT_CHANNEL` (`ailife_events`); on every wake (a NOTIFY **or** the poll timeout) it drains PENDING rows oldest-first — one transaction per row, claimed with `FOR UPDATE SKIP LOCKED`, dispatched to a `Consumer<EventBusMessage>` handler, marked PUBLISHED on success. **At-least-once:** the periodic poll delivers even if a NOTIFY is lost (listener was down); a handler that throws rolls back → row stays PENDING for the next poll. New envelope `EventBusMessage(id, topic, householdId, payload, occurredAt)` (`payload` = raw JSON string, bus stays payload-agnostic). pom gained test deps (spring-boot-starter-test + testcontainers postgresql/junit-jupiter). 3 `EventBusIntegrationTest` cases (Testcontainers `postgres:16`, `test-schema.sql` mirrors the migration): NOTIFY-driven delivery + mark PUBLISHED; a row published before the listener starts is still drained (at-least-once via start/poll drain, NOTIFY lost); a failing handler leaves the row PENDING and retries. **Known B1 limitations (deferred):** single handler + single PENDING/PUBLISHED status (no per-consumer offsets), and a poison handler head-of-line-blocks its drain pass (no dead-letter / retry cap). **No service wired yet** — the Spring-bean producer/consumer API + first real producer/consumer (scheduler or notifier) is **B2**. README rewritten. event-bus module tests green.
- **PR69 — inbox-clarify apply-on-confirm (Track A / A4 — second producer, closes Track A):** Second real producer of the conversation-state mechanism; multi-turn confirmation now works end-to-end on tasks. The `inbox-clarify` SKILL.md switched from prose to a **strict-JSON proposal contract** (`{"proposals":[{taskId,title,status,context?,projectId?}]}`, version 0.2.0). `InboxClarifier` now: fetches the inbox → LLM returns structured proposals → renders a human "Предлагаю … Применить?" summary + stashes the proposals as a `pendingAction` `{flow: inbox-clarify-apply, proposals}` (no apply yet). New `web/ResumeController` (`POST /agents/tasks/resume`, dispatches on `pendingAction.flow`) → `InboxClarifier.resume`: an affirmative ("да"/…) applies every proposal via new `http/ClarifyClient` → new mcp-tasks **`POST /internal/clarify`** passthrough (`web/InternalClarifyController` → `clarify_task` tool; validation → 400) — per-item soft-fail so one bad row doesn't abort the rest, reports "Разобрал N из M задач."; anything else cancels. Both replies carry no `pendingAction` → orchestrator clears the lock. No new contract (reuses `ClarifyTaskInput` + the A3 contracts), no schema change. READMEs (mcp-tasks + tasks-agent) updated; the inbound E2E (`E2EInboundClarifyFlowTest`) updated to assert the confirm + pendingAction. Tests: mcp-tasks 19→20 (`/internal/clarify` apply + 400), tasks-agent 28→31 (propose→pendingAction; resume-да applies both proposals; resume-нет cancels). Full 24-module reactor builds clean. **Track A complete.**
- **PR68 — receipt-parser confirm-before-write (Track A / A4 — first real producer):** First end-to-end use of the Stage-4 lock/resume mechanism, and it closes the **owner-flagged deferred debt** (receipt-parser was write-immediately). `ReceiptParser`: the photo path now parses the draft, resolves the account, and replies **"Записать: … (счёт «…»)? Ответьте «да»"** with a `pendingAction` `{flow: receipt-confirm, input: <AddTransactionInput>, accountName}` — it **no longer writes**. The orchestrator (A2/A3) locks the conversation to finance; the user's reply hits new `web/ResumeController` (`POST /agents/finance/resume`, dispatches on `pendingAction.flow`) → `ReceiptParser.resume`: an affirmative ("да"/"yes"/"ок"/… case-insensitive set) deserializes the stashed `AddTransactionInput` and writes via the existing `TransactionClient.add` (category null → categorizer one-shot still fires); anything else cancels ("Отменил…"). Either reply carries no `pendingAction`, so the orchestrator clears the lock. Failures degrade to a friendly message (no pendingAction → no lock). No mcp-finance / contract / schema change — reuses the existing `/internal/transaction` write path and the A3 contracts. AGENT.md + README updated (write-immediately → confirm). Tests: `ReceiptFlowTest` rewritten 1→3 (photo → confirm + pendingAction + **no** write; resume "да" → writes the stashed draft; resume "нет" → cancels, no write); finance-agent suite 31→33 green. Full 24-module reactor builds clean. **Deferred receipt-confirm debt is now cleared.**
- **PR67 — conversation lock lifecycle + resume plumbing (Track A / A3):** The orchestrator now owns the full route-lock lifecycle (write/resume/clear); agents declare confirmation intent via a new contract field. `IntentResponse` gained `pendingAction` (JsonNode, NON_NULL) + a **back-compat 3-arg constructor** so every existing `new IntentResponse(agent,text,model)` caller compiles unchanged. New contract `ResumeRequest(message, pendingAction)`. `Agent` gained `resume(ResumeRequest)` (default = re-`handle` the message — fine for echo/local); `RemoteAgent.resume` POSTs `/agents/<name>/resume`. `ConversationStateClient` gained `lock(...)` (PUT) + `clear(...)` (DELETE), both soft-fail. `IntentRouter` rewritten: on an active lock → `agent.resume(message, storedPendingAction)` (bypass classify); after any agent reply → if `pendingAction != null` **lock** to that agent, else on a resume turn **clear** (resolved); a fresh turn with no pendingAction writes nothing. All lock writes/clears are soft-fail (a confirmation that can't persist just won't survive — never a user-facing error). **Latent by design** — no agent returns `pendingAction` or exposes `/resume` yet (that's A4), so production behaviour is unchanged; fully unit-tested in isolation. Tests: `IntentRouterLockTest` 3→5 (active lock → resume + clear-on-resolve; resume returning pendingAction → re-lock; no-lock classify writes nothing; handle returning pendingAction → lock; unknown-agent lock → classify). orchestrator suite 12→14 green. Full 24-module reactor builds clean (the `IntentResponse` contract change is back-compat across every consumer).
- **PR66 — orchestrator route-lock check (Track A / A2):** The orchestrator now consults conversation-service **before** intent classification. New `conversation/ConversationStateClient` (reactive WebClient over `GET /v1/conversation-state`, **strict no-throw** like `MemoryClient`: disabled / missing key / 204 / network / 500 ms timeout all → `Mono.empty()`) + `conversation/ConversationProperties` (`orchestrator.conversation.{enabled,url}`, registered in `OrchestratorApplication`). `routing/IntentRouter.route` rewritten: `activeState(household,user,channel)` → if a `routeLock` names a **known** agent, route the message straight to it (bypassing the LLM classifier); else (`switchIfEmpty`) the existing classify-and-dispatch path runs. So a reply to an agent's open question returns to that agent; everything else classifies normally. **Read-path only** — nothing writes locks yet (A3), so in production this is currently a no-op until agents start setting pending actions; it's fully unit-tested in isolation. application.yml gained the `orchestrator.conversation` block; compose orchestrator gains `CONVERSATION_SERVICE_URL`; `.env.example` + README updated. Tests: 3 `IntentRouterLockTest` cases (active lock → routes to locked agent, classifier never called; no lock → classifies; lock to unknown agent → falls through to classify) + existing `AgentRoutingTest`/`IntentControllerTest` set `orchestrator.conversation.enabled=false` so the route-lock check is skipped (no stray connect). orchestrator 9→12 green. Full 24-module reactor builds clean.
- **PR65 — conversation-service (Track A / A1: conversation-state foundation):** First slice of the open inter-agent half. New platform service `platform/conversation-service` (port **8089**, servlet + JPA, mirrors scheduler/memory) owning **short-term conversation control state** — the route-lock + pending-action that make multi-turn dialog and confirmations possible. Owner-chosen home (over folding into orchestrator or memory-service): a dedicated narrow service keeps the orchestrator stateless and single-responsibility, consistent with the platform-service-per-bounded-context architecture. New migration [009-conversation-state.yml](../infra/liquibase/features/009-conversation-state.yml) — `core.conversation_state` (id, household_id FK, user_id, channel, route_lock, pending_action jsonb, expires_at TTL, created_at, updated_at) with a **unique key on `(household_id, user_id, channel)`** so an upsert replaces the one active row per conversation. **Distinct from `core.conversations`** (message history) — this is routing/confirmation control. REST: `PUT /v1/conversation-state` (upsert lock + pending action, TTL'd — default 1800s), `GET /v1/conversation-state?householdId=&userId=&channel=` (active/unexpired → 200 `ConversationStateDto`, else 204), `DELETE …` (clear after resolve → 204). **Expiry enforced on read** (a stale lock reads as absent — a forgotten confirmation ages out, the next message classifies normally; no sweeper needed). New contracts `ConversationStateDto` + `SetConversationStateRequest` in `libs/contracts/conversation/`. Deploy surface wired: compose service block (postgres + liquibase deps), `.env.example`, infra/README port row (8089), Dockerfile. 4 Testcontainers tests (`set`→`getActive` + upsert-replaces-same-key; expired-not-returned; clear-removes; REST round-trip PUT/GET/DELETE via `TestRestTemplate`). Full 24-module reactor builds clean. **No orchestrator/agent wiring yet** — that's A2 (orchestrator route-lock check before classify) + A3 (agents set/resolve pending actions via `IntentResponse` + a resume endpoint).
- **PR17 — first cross-agent chain (calendar-agent → memory-service recall + relations → skill LLM call):** Stage 4 closer. `calendar-agent` now zips `POST /v1/memories/recall` and `GET /v1/graph/person/{id}/relations` **before** the LLM call for every skill trigger; non-empty results are added to the user-message JSON as `memories[]` and `relations`. Both are gated **soft-fail** in `agents/calendar/http/MemoryClient` (500 ms timeout, any error → empty list / empty `PersonRelationsResponse`) so memory-service downtime degrades enrichment without blocking the skill. System triggers (`ics.pull`) are unchanged — they don't go through `runSkill`. `MemoryClient` is the **second consumer** in the codebase after orchestrator's; when a third lands, lift to `libs/memory-client` (note left in the class). `buildRecallQuery` anchors the recall query on the person's display name when present (`"<kind> for <displayName>"`); fallback uses kind + householdId. Tests: `TriggerControllerTest` grew to 7, adding a `MemoryDispatcher` (parallel to `ProfileDispatcher`) that seeds canned hits + relations per case OR flips to 500 to test soft-fail. Existing birthday/gift tests now also assert the recalled memory text + relation `object_label` appear in the LLM body; new test asserts memory 500 → skill still runs and the body has no `memories`/`relations` block. New env: `MEMORY_SERVICE_URL` / `CALENDAR_AGENT_MEMORY_RECALL_K`. Full 16-module reactor green.
- **PR16 — single-hop relations + `/v1/graph/person/{id}/relations` (Apache AGE deferred):** **Architectural pivot from the original plan** — instead of bringing up Apache AGE now, we ship a plain SQL `memory.relations` table that answers the single-hop questions PR17 actually needs ("what does Maria like?"). AGE upgrade is gated on explicit promotion criteria in `platform/memory-service/README.md` §Why a SQL table instead of Apache AGE (need for multi-hop traversal, graph algorithms, or ~100k+ rows). Reasoning: roadmap §Risks already calls the AGE image unstable; PR17 only needs single-hop; AGE pays off on traversal we don't have. New migration [005-memory-relations.yml](../infra/liquibase/features/005-memory-relations.yml) _(was 041 — renumbered to the cross-cutting 001-009 range)_ — `(household_id, subject_type/id, edge, object_type/id, object_label, confidence, source, metadata)` with double indexing on subject side and object side. `object_label` is always present even when `object_id` is null, so a free-text edge like "Maria likes loose-leaf tea" displays cleanly. New contracts `RelationDto` / `WriteRelationRequest` / `PersonRelationsResponse`. memory-service gains `RelationRepository` + `RelationService` + `RelationController` with three endpoints: `POST /v1/relations`, `DELETE /v1/relations/{id}`, `GET /v1/graph/person/{personId}/relations?householdId=…` (returns newest-first outgoing + incoming scoped to household; cross-household leakage explicitly tested-against). 3 testcontainers tests cover the scoped query + forget + missing-field rejection. Full 16-module reactor green.
- **PR15 — orchestrator pre-routing recall (memory → classifier prompt):** orchestrator now calls memory-service's `POST /v1/memories/recall` before the FAST-channel intent classification and injects the top-k hits as a **second system message** (`"Relevant long-term context for this user (most similar first):\n- …"`). New `memory/MemoryProperties` (`orchestrator.memory.{enabled, url, recall-k}`, default k=3) and `memory/MemoryClient` (reactive POST, 500 ms timeout, **strict no-throw** — any error / disabled / missing household collapses to `Mono.just(List.of())` so routing never blocks on memory). `LlmIntentClassifier` consumes it; the echo short-circuit (when only `echo` is registered) skips recall too — echo doesn't read context. Test coverage: `AgentRoutingTest` grew to 2 cases — (a) memory returns two hits → POST `/v1/memories/recall` made with right `householdId`/`userId`/`k=3` and the recalled memory text appears in the classification LLM body; (b) memory returns `[]` → classification proceeds and the LLM body has **no** "Relevant long-term context" block. Counter reset moved to `@BeforeEach` so adding tests doesn't leak state. New env vars: `MEMORY_SERVICE_URL`, `ORCHESTRATOR_MEMORY_ENABLED`, `ORCHESTRATOR_MEMORY_RECALL_K`. Full 16-module reactor green.
- **PR14 — memory-service skeleton (pgvector recall, no graph yet):** Stage 4 opener. New `platform/memory-service` (port 8087, servlet stack — `LlmClient.embed().block()` requires it; reactive event-loop crashed). Schema `memory.memories` with `vector(384)` + HNSW cosine index ([004-memory.yml](../infra/liquibase/features/004-memory.yml) _— was 040, renumbered_). API: `POST /v1/memories` (embed via llm-gateway then store), `POST /v1/memories/recall` (top-k cosine within `household_id` required + optional `user_id`/`person_id` scope; NULL = broader), `DELETE /v1/memories/{id}`. JPA deliberately skipped — JdbcTemplate + `domain/Vectors.toLiteral` → `::vector` cast handles pgvector wire format without a custom Hibernate type. New contract DTOs in `libs/contracts/memory/`. Dim mismatch (Stage 5 = bge-m3 = 1024) fails fast with a clear error in `MemoryService`. 3 testcontainers tests cover write→forget, top-k cosine scoped to household (rejects cross-household leakage), person-scoped narrowing. Full 16-module reactor green.

## Done (Stage 1)
- **PR13 — scheduler-driven hourly re-pull of ICS subscriptions + new "system trigger" pattern in calendar-agent:** New nullable column `calendar.ics_subscriptions.schedule_id uuid` (`012-ics-subscriptions-schedule-id.yml`) — no FK so a stale scheduler row just makes cleanup a no-op. mcp-ics-import gains an outbound `SchedulerClient` (sync WebClient → scheduler-service); `add_subscription` now also POSTs `/v1/schedules` with `cron=0 0 * * * *`, `kind=ics.pull`, `owner_agent=calendar`, payload `{subscriptionId}`, stores the returned schedule id on the subscription row. `remove_subscription` DELETEs that schedule first (404 = success). Scheduler failures on add are tolerated (`schedule_id` stays null) so a flaky scheduler can't break sync. mcp-ics-import also exposes `POST /internal/pull/{subscriptionId}` (`InternalPullController`) — a non-MCP REST passthrough so cron-driven callers don't pay the LLM tax. **New pattern in calendar-agent:** `SystemTriggerHandler` interface + `SystemTriggerRegistry`, checked by `TriggerController` **before** `SkillRegistry`. System triggers run without the LLM (and without notifier fan-out). First impl: `IcsPullTriggerHandler` — parses `subscriptionId` from payload, forwards to mcp-ics-import via new `IcsImportClient` (`MCP_ICS_IMPORT_URL`, defaults to `http://mcp-ics-import:8091`). AGENT.md gains `ics.pull` trigger declaration. Tests: mcp-ics-import grew to 7 (added scheduler MockWebServer asserting POST cron+kind+payload on add, DELETE on remove; added two `/internal/pull` tests using `WebTestClient` against a `LocalServerPort`-bound Tomcat); calendar-agent grew to 6 (`ics.pull` forwards to ics-import without touching LLM or notifier; downstream 500 still returns 202). Full 15-module reactor green.
- **PR12 — mcp-ics-import (read-only Apple/Google subscriptions):** New `mcp/mcp-ics-import` module (port 8091, Spring AI MCP webflux). New table `calendar.ics_subscriptions` (`011-ics-subscriptions.yml`: id, household_id, name, slug, url, last_synced_at, last_error, unique(household_id, slug)). 4 MCP tools — `add_subscription(householdId, name, url)` registers + pulls immediately (slug derived from name; non-ASCII → `feed-<uuid8>` fallback); `pull_calendar(subscriptionId)` fetches the ICS, parses VEvents via ical4j, PUTs each into a per-subscription Radicale collection `external-<slug>`, mirrors into `events_cache` and **diff-deletes** events that vanished from the feed; `list_subscriptions(householdId)`; `remove_subscription(id)` drops the row + mirrored events + best-effort collection cleanup. Fetch/parse failures land in `last_error` and the result carries the error string — no exception bubbles up. `webcal://` URLs are normalised to `https://`. ICS body capped at `ICS_IMPORT_MAX_BYTES` (default 5 MB). CalDAV write client duplicated from mcp-caldav for now (will lift to `libs/caldav-client` if a third consumer appears). New `IcsSubscriptionDto` + `AddSubscriptionInput` + `PullCalendarResult` in `libs/contracts`. Testcontainers integration test (pgvector PG + tomsquest/docker-radicale + MockWebServer) covers add→upsert→diff-remove→error-capture→remove flow; 5 tests green. Full 15-module reactor green. Scheduler-driven hourly re-pull is the next PR — this one ships the MCP surface only.
- **PR11 — gift-recommender skill + payload person resolution:** Second SKILL.md at `skills/calendar/gift-recommender/SKILL.md` bound to trigger `gift.recommend` (3–5 ideas grounded in `core.people.interests` + `notes`; no budget yet — finance integration deferred). Calendar-agent now **resolves `personId` from the wake payload before the LLM call**: `ProfileClient.personById(uuid)` fetches the `PersonDto`, `TriggerController` builds the user message as `{"payload": <original>, "person": <PersonDto or absent>}`. 404 / unreachable profile = empty `person`, skill prompts handle that. Refactor uses `defaultIfEmpty(Optional.empty())` to avoid the `switchIfEmpty`-on-`Mono<Void>` foot-gun (first attempt fired the skill twice and timed out the LLM stub — captured here so we don't relearn it). `TriggerControllerTest` reworked to a `Dispatcher`-driven profile-service mock that serves both `/v1/people/{id}` and `/v1/users/by-household/{id}`; new `gift.recommend` test asserts the LLM body contains the resolved interests + notes. 7 calendar-agent tests green; full reactor green.
- **PR10 — birthday-greeter-notify: greeting reaches the user:** Closes the Stage 1 proactive birthday loop. New endpoint `GET /v1/users/by-household/{id}` on profile-service (ordered by `display_name`). Calendar-agent gains two outbound clients: `ProfileClient` (resolve `householdId` → users) and `NotifierClient` (POST `/v1/notify`). After the LLM produces the greeting, `TriggerController` fans the same text out to **every** user in the household — per the explicit recipient-policy decision, no role filter. Per-user notifier failures are logged + swallowed (one bad user doesn't block the others; trigger still returns 202). New `CalendarAgentProperties` (`profile-service-url`, `notifier-url`) + `OutboundHttpConfig` (one `WebClient` per dependency, `.clone()` to avoid the shared-builder bug). New env vars `PROFILE_SERVICE_URL`, `NOTIFIER_URL` for calendar-agent. 1 new profile-service test (`usersByHouseholdListsMembersOrderedByName`) + reworked `TriggerControllerTest` (now spins up three MockWebServers: LLM + profile + notifier; asserts both household members got their notify POST with the greeting). Full reactor 14 modules green.
- **PR9c2 — wake dispatch + first SKILL.md:** Orchestrator: `Agent` interface gains `wake(AgentWakeRequest)` (default no-op). `RemoteAgent.wake()` POSTs `<baseUrl>/agents/<name>/triggers/<kind>` with the wake payload. `AgentWakeController` is no longer a 202 stub — it looks up the agent in the dispatch map and awaits the agent's wake reply; unknown agent → 404, success → 202. New `WakeDispatchTest` with MockWebServer pretending to be calendar-agent. **SKILL.md convention introduced:** `skills/<domain>/<name>/SKILL.md` at the repo root, frontmatter (`name`, `description`, `version`, `domain`, `triggers`, `languages`, `inputs`) + EN body (the skill prompt). First skill: `skills/calendar/birthday-greeter/SKILL.md`. Calendar-agent: pom `<resources>` copies `skills/calendar/` from the repo root into the classpath under `skills/calendar/`. `SkillLoader` scans `classpath*:skills/calendar/*/SKILL.md` at startup (SnakeYAML), parses to `Skill` records, exposes them via `SkillRegistry` (indexed by trigger kind). `TriggerController` is now real: looks up the skill bound to the wake's `kind`, calls llm-gateway with `[manifest.body, skill.body]` as system prompts and the wake payload (as JSON text) as the user message; generated greeting is **logged only** for now (notifier wiring is the next PR). 3 calendar-agent tests + 1 orchestrator wake test added; full reactor green.
- **PR9c1 — intent routing end-to-end:** `AgentManifest` lifted to `libs/contracts`. Calendar-agent: manifest at `/agents/calendar/manifest`; `IntentController` calls llm-gateway with `AGENT.md` body as system prompt. Orchestrator: `AgentRegistryProperties` (config-driven `{name, baseUrl}`), `AgentDiscovery` fetches `/agents/<name>/manifest` at startup (3s timeout, soft-fail). `RemoteAgent` POSTs `/agents/<name>/intent`. `LlmIntentClassifier` uses FAST channel with few-shot from manifests' `intents[]`; lenient parsing + echo fallback. `WebClient.Builder.clone()` per agent to avoid shared-state bug. New `CALENDAR_AGENT_URL` env.
- **PR9b — calendar-agent module skeleton + AGENT.md convention:** New module `agents/calendar-agent` (port 8086). AGENT.md frontmatter + EN body — canonical example of the convention. Maven build copies `AGENT.md` from the module root onto the classpath; `ManifestLoader` parses frontmatter (SnakeYAML) + body at startup. Manifest endpoint moved to `/agents/calendar/manifest` in PR9c1. Intent/trigger endpoints were 202 stubs; intent became real in PR9c1, trigger remains a stub until PR9c2. **AGENT.md convention rule:** every agent module mirrors this layout — file at module root, pom resources section copies to classpath, `ManifestLoader` reads at startup. SKILL.md convention will be exemplified when the first skill ships in PR9c2.
- **PR9a — `core.people` + People CRUD + orchestrator `/v1/agents/wake` stub:** New `core.people` table (`infra/liquibase/features/003-people.yml`) — household-scoped contacts for birthdays/gifts/greetings, distinct from `core.users`. `pg_trgm` enabled; GIN(interests jsonb_path_ops) + GIN(display_name gin_trgm_ops) + btree(household_id). Profile-service gains Person entity + repo, `PeopleController` (POST / GET / GET by-household / PATCH partial), `PersonDto` shared contract. Orchestrator gains `POST /v1/agents/wake` — looks up agent by name in the existing router registry, returns 202 if registered, 404 otherwise. Closes the dispatch gap for PR8: scheduler now hits a real endpoint instead of 404. Actual wake dispatch (calling an agent's handler with the payload) lands in PR9c. 3 profile-service tests + 2 orchestrator wake tests green; full reactor green.
- **PR8 — scheduler-service + `core.schedules` / `core.shedlock`:** `platform/scheduler-service` (8085). `@EnableScheduling` + ShedLock (JDBC `core.shedlock`). Fixed-delay tick (default 30s, `scheduler.tick-millis`) wrapped in `@SchedulerLock("scheduler-tick")` reads due rows from `core.schedules` and POSTs orchestrator `/v1/agents/wake` (`AgentWakeRequest{scheduleId, householdId, agent, kind, payload}`). Cron recurring vs one-shot `runAt`; cron uses Spring's `CronExpression` (UTC). Per-row tx so a failed wake leaves the row at its current `next_run_ts` for retry. `TickRunner` is the production fixed-delay loop; `ScheduleTick` is the testable unit (tests invoke it directly with `scheduler.tick-millis=3600000`). REST API: create / list-by-household / pause / resume / delete. New feature changelog `infra/liquibase/features/002-scheduling.yml`. ShedLock 5.16.0. Integration test: pgvector PG + OkHttp MockWebServer as orchestrator, 4 tests green. Note: orchestrator's `/v1/agents/wake` endpoint is implemented in PR9 — until then dev wake-ups 404 and the row sits in due state, which is the intended retry behaviour.
- **PR7 — notifier-service + gateway-telegram `/internal/send`:** `platform/notifier-service` (8084) resolves `userId → telegram_user_id` via profile-service and forwards to gateway-telegram's `POST /internal/send` (Bearer `${GATEWAY_INTERNAL_API_TOKEN}`). Bot token stays in gateway. New contracts `NotifyRequest` + `InternalSendRequest`. `TelegramClient` lifted to a conditional Spring bean so both `BotRegistration` and `InternalSendController` share it via `ObjectProvider`. 12 modules, 21 tests green.
- **PR6 — calendar schema + `mcp-caldav`:** Spring AI 1.0.8 webflux MCP starter, ical4j 4.2.5. 5 tools (create/update/delete/list/search) over Radicale, write-through with PG mirror in `calendar.events_cache`. Testcontainers integration test (pgvector PG + tomsquest/docker-radicale).
  - **Radicale gotchas captured here so we don't relearn them:**
    1. Principal `/{household}/` must be MKCOL'd before MKCALENDAR `/{household}/{calendar}/`, otherwise PUT events get 409 "Conflict in the request". `CalDavClient.ensureCollection` does both, idempotent (treats 405/409 as "already exists").
    2. `tomsquest/docker-radicale` image has no bash, so Testcontainers' default port listening check no-ops and the wait returns instantly. Use `Wait.forHttp("/.web/").forStatusCodeMatching(c -> c >= 200 && c < 500)` instead — the `/.web/` UI endpoint returns 200 only after the Python server has bound.
    3. Custom Radicale config mounts to `/etc/radicale/config` (not `/config/config`). Tests mount a copy with `[rights] type = none` so anonymous (auth=none) requests can create/write collections.

## Next PRs
- **Third finance skill — `receipt-parser`** (LLM-vision draft → confirm → add). Needs a vision-capable LLM channel in `llm-gateway` (Anthropic + openai-compatible providers both support it) + a multipart-image entry path through gateway-telegram → orchestrator → finance-agent. Confirm-before-write semantics: skill produces a draft `AddTransactionInput`, the user confirms in chat, finance-agent then calls `mcp-finance.add_transaction`. Two open questions: (a) where the photo lives between draft and confirm — `media` schema vs in-memory session blob; (b) does confirm happen via a separate intent or via a stateful conversation thread.
- **Register `mcp-money-pro-import` in finance-agent** — first real MCP-client in an agent; today agents only call llm-gateway + HTTP-passthroughs. PR23 shipped the MCP surface but it has no caller; an `intent`-side flow ("импортируй CSV из Money Pro") needs the import tool registered alongside `mcp-finance`. Closes the PR23 dangler and sets the pattern for any future per-agent MCP-client wiring (likely a lift to `libs/agent-runtime`).
- Later: `libs/memory-client` for orchestrator's recall path if a fourth consumer warrants it.
- Once two agents share the same `manifest/ManifestLoader` + `skill/Skill*` + `web/TriggerController` shape: lift to `libs/agent-runtime`.
- Schema follow-up: `022-fin-recurring.yml` for `fin_recurring` + matviews (`fin_mv_monthly_by_category`, `fin_mv_account_balance`) once their tools land.
- Backfill: register `mcp-money-pro-import` in `libs/mcp-client`'s server registry / orchestrator MCP map and wire finance-agent to call it (this PR shipped the MCP surface only — the agent has no skill that uses it yet).
- Apache AGE upgrade: still deferred — gated on the promotion criteria in `platform/memory-service/README.md` (multi-hop, graph algos, or ~100k+ rows).
- Real-Ollama opt-in E2E variant: see "Deferred work" section above.

## Workflow reminder
Run only the relevant test class while iterating; full suite once before PR (CI is the authority). Don't paste full logs — extract failing assertion + ~3 lines. Auto-merge squash on green, delete branch. Start a fresh Claude Code session after each merged PR.
