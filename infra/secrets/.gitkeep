# rclone off-site replication config lives here: rclone.conf (Yandex Disk OAuth token and/or the
# `tailscale` sftp remote) + any SSH private key (e.g. tailscale_key). Bind-mounted read-write to
# /config in the rclone-offsite service. Contents are gitignored.
